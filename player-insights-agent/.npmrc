# Pin the public registry so a clone installs anywhere.
# Without this, a machine with a private registry in ~/.npmrc rewrites the
# "resolved" URLs in package-lock.json on the next `npm install`, and the
# lockfile stops working for everyone else.
registry=https://registry.npmjs.org/
