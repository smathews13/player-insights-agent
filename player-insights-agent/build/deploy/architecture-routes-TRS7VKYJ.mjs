
import{appServicePrincipal,resolveExperimentConfiguration,withDeadline}from"./chunk-RQPBWCZS.mjs";import"./chunk-66G4LGYE.mjs";import{normalizeWorkspaceHost}from"./chunk-3KZTQDF5.mjs";import"./chunk-OMC625AH.mjs";import"./chunk-A7SHUGSC.mjs";function fromEnvironment(variable){return{value:process.env[variable]?.trim()??"",variable}}var SEMANTIC_INDEX_VARIABLE="PLAYER_INSIGHTS_SEMANTIC_INDEX";var ARCHITECTURE_EXPERIMENT_TIMEOUT_MS=2e3;async function architectureExperimentId(appkit){try{const configuration=await withDeadline(resolveExperimentConfiguration(appkit),ARCHITECTURE_EXPERIMENT_TIMEOUT_MS,`Experiment lookup did not answer within ${ARCHITECTURE_EXPERIMENT_TIMEOUT_MS} ms.`);return configuration.id}catch{return process.env.PLAYER_INSIGHTS_EXPERIMENT_ID?.trim()??""}}function semanticIndexReport(){return{decidedBy:SEMANTIC_INDEX_VARIABLE,reason:"It is resolved when the model is logged and baked into the artifact, so changing it means logging a new model version rather than editing a setting here. A version logged before the endpoint reported this setting cannot say whether it has an index."}}async function architecturePayload(appkit){const workspaceHost=normalizeWorkspaceHost(process.env.DATABRICKS_HOST);return{workspaceHost,canDeepLink:Boolean(workspaceHost),servingEndpoint:fromEnvironment("DATABRICKS_SERVING_ENDPOINT_NAME"),appWarehouse:fromEnvironment("DATABRICKS_SQL_WAREHOUSE_ID"),experimentId:await architectureExperimentId(appkit),appServicePrincipal:appServicePrincipal()??"",appBuildSha:process.env.PLAYER_INSIGHTS_BUILD_SHA?.trim()??"",semanticIndex:semanticIndexReport(),readAt:new Date().toISOString()}}function setupArchitectureRoutes(appkit){appkit.server.extend(app=>{app.get("/api/architecture",async(_req,res)=>{res.json(await architecturePayload(appkit))})})}export{ARCHITECTURE_EXPERIMENT_TIMEOUT_MS,architecturePayload,semanticIndexReport,setupArchitectureRoutes};
