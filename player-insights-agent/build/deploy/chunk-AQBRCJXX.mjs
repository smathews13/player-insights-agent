
import{derivedSemanticIndexName,resolveSemanticIndexValue}from"./chunk-SRWET4YN.mjs";import{UNDETERMINED,declaredUserApiScopes,freshSignIn,quotedScopes,tokenScopeVerdict}from"./chunk-ICGOJW6Q.mjs";import{looksLikeMissingScope,scopesFromToken}from"./chunk-TD25SRE2.mjs";import{normalizeWorkspaceHost}from"./chunk-3KZTQDF5.mjs";function declares(declared,scope){return declared.includes(scope)}var WHERE_THE_LISTS_ARE="The Connected as section of the Connections page lists what your sign-in carries and what this app asks for.";function scopeRefusalDiagnosis(input){const{declarable,namedByWorkspace,declared,tokenScopes,scopeHeld}=input;const scope=declarable||namedByWorkspace;const alias=namedByWorkspace&&namedByWorkspace!==scope?` The workspace calls it \`${namedByWorkspace}\`. The app declares the same permission as \`${scope}\`.`:"";const carried=tokenScopes?`The sign-in this request carried lists ${quotedScopes(tokenScopes)}.`:"The sign-in this request carried does not list its own permissions.";if(!declared){return{cause:UNDETERMINED,evidence:`The refusal named \`${scope}\`. ${carried} This deployment was not told which permissions it declares.`,explanation:`The workspace refused this call over the \`${scope}\` permission. The call stopped there, so nothing was established about whether ${input.tokenScopes?"you":"this identity"} can reach the object. Whether this app asks for that permission was not established either. This deployment was not told what it declares.`,remedy:null,grantIsMissing:false}}if(declares(declared,scope)){if(scopeHeld===true){return{cause:"workspace-refused-a-held-scope",evidence:`The app declares ${declared.length} permissions including \`${scope}\`, and the sign-in this request carried lists it. The refusal named \`${namedByWorkspace||scope}\`.`,explanation:`Your sign-in carries \`${scope}\`, and the workspace refused this call anyway.${alias} That rules the permission out and leaves a grant on the object itself, which an admin adds. A new sign-in will not move this one. ${WHERE_THE_LISTS_ARE}`,remedy:null,grantIsMissing:true}}if(scopeHeld===null){return{cause:"declared-scope-refused",evidence:`The app declares ${declared.length} permissions including \`${scope}\`. ${carried} The refusal named \`${namedByWorkspace||scope}\`.`,explanation:`This app asks for \`${scope}\`, and the workspace refused this call over it.${alias} Whether the sign-in this request carried holds that permission was not established, so this is either a sign-in older than the permission or a grant you are missing on the object. The call stopped before it could tell. ${WHERE_THE_LISTS_ARE}`,remedy:freshSignIn(),grantIsMissing:false}}return{cause:"token-lacks-declared-scope",evidence:`The app declares ${declared.length} permissions including \`${scope}\`. ${carried} The refusal named \`${namedByWorkspace||scope}\`.`,explanation:`Your sign-in to this app does not carry \`${scope}\`, which the app asks for.${alias} The call stopped there, so nothing was established about whether you can reach the object. This is not a grant you are missing. ${WHERE_THE_LISTS_ARE}`,remedy:freshSignIn(),grantIsMissing:false}}return{cause:"app-declares-no-such-scope",evidence:`The app declares ${quotedScopes(declared)}. \`${scope}\` is not among them, and the refusal named \`${namedByWorkspace||scope}\`. ${carried}`,explanation:`This app does not ask for \`${scope}\`, so no sign-in it hands out can carry it.${alias} The call stopped there, so nothing was established about whether you can reach the object. A new sign-in will not move this one, and there is nothing you can do about it from the browser. ${WHERE_THE_LISTS_ARE}`,remedy:{kind:"cli",statement:`# Check the Apps API accepts the NAME before declaring it. It validates against a
# narrower list than the workspace's OAuth metadata, and one rejected name fails the
# whole deploy. This answers without deploying and creates nothing:
databricks api post /api/2.0/apps \\
  --json '{"name":"<an-app-that-already-exists>","user_api_scopes":["${scope}"]}'
# "already exists" means the name is good. "not a valid scope" means it is not.
#
# Then add it to app_user_api_scopes in databricks.yml, and restart. Scopes are read
# when the app STARTS, so a redeploy on its own leaves the change inert:
databricks apps stop <app-name>
databricks apps start <app-name>`,guidance:"Anyone already signed in keeps their old permissions after the restart, and needs a new sign-in to pick this up.",run_by:"Run by whoever deploys this app. A workspace admin cannot fix this and a GRANT will not"},grantIsMissing:false}}var PROBE_TIMEOUT_MS=15e3;var SCOPE_BY_API_PREFIX={"/api/2.1/unity-catalog/catalogs/":"catalog.catalogs:read","/api/2.1/unity-catalog/schemas/":"catalog.schemas:read","/api/2.1/unity-catalog/tables/":"catalog.tables:read","/api/2.1/unity-catalog/model-services/":"model-serving","/api/2.0/vector-search/indexes/":"vectorsearch.vector-search-indexes:read","/api/2.0/vector-search/endpoints/":"vectorsearch.vector-search-endpoints:read","/api/2.0/serving-endpoints/":"serving.serving-endpoints","/api/2.0/genie/":"dashboards.genie","/api/2.0/sql/":"sql"};function scopeForPath(path){let best="";let longest=0;for(const[prefix,scope]of Object.entries(SCOPE_BY_API_PREFIX)){if(path.startsWith(prefix)&&prefix.length>longest){best=scope;longest=prefix.length}}return best}function text(value){return typeof value==="string"?value.trim():""}function record(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}function compactFacts(entries){return Object.fromEntries(Object.entries(entries).filter(([,value])=>value!==null&&value!==void 0&&(typeof value!=="string"||value.trim().length>0)))}function servingFacts(body){const state=record(body.state);const entities=Array.isArray(body.served_entities)?body.served_entities.map(record):[];const routes=Array.isArray(record(body.traffic_config).routes)?record(body.traffic_config).routes.map(record):[];const servedModel=entities.map(entity=>{const name=text(entity.entity_name)||text(entity.name);const version=text(entity.entity_version);return[name,version&&`v${version}`].filter(Boolean).join(" ")}).filter(Boolean).join(", ");const traffic=routes.map(route=>{const name=text(route.served_model_name);const percentage=typeof route.traffic_percentage==="number"&&Number.isFinite(route.traffic_percentage)?`${route.traffic_percentage}%`:"";return[name,percentage].filter(Boolean).join(" ")}).filter(Boolean).join(", ");return compactFacts({readiness:text(state.ready),served_model:servedModel,traffic})}function sql(statement,guidance=""){return{kind:"sql",statement,guidance}}function cli(statement,guidance=""){return{kind:"cli",statement,guidance}}function granteeFor(principal){return principal?`\`${principal}\``:"`<the signed-in user>`"}var READS_METADATA="GET, as the signed-in user";var SERVING_ENDPOINT_KIND="serving-endpoint";var SERVING_ENDPOINTS=[{id:"agent-endpoint",label:"Orchestrator serving endpoint",onAnswerPath:true,note:"Every question the app asks goes through this endpoint."},{id:"llm-endpoint",label:"Foundation model",onAnswerPath:true,note:"The orchestrator reasons and writes with this endpoint."},{id:"judge-endpoint",label:"Benchmark judge model",onAnswerPath:false,note:"The Benchmark Lab scores answers with this endpoint. It is never on the answer path."}];var ANSWER_PATH_ENDPOINT_IDS=SERVING_ENDPOINTS.filter(endpoint=>endpoint.onAnswerPath).map(endpoint=>endpoint.id);function connectionSubjects(input){const value=id=>(input.configured[id]??"").trim();const subjects=[];const warehouse=value("sql-warehouse");if(warehouse){subjects.push({id:"sql-warehouse",kind:"sql-warehouse",name:warehouse,label:`SQL warehouse · ${warehouse}`,path:`/api/2.0/sql/warehouses/${encodeURIComponent(warehouse)}`,proves:"It does not prove a statement would run: CAN_USE on the warehouse and SELECT on the tables are separate grants, and a stopped warehouse still answers this call.",observe:body=>{const state=text(body.state);const name=text(body.name);return[name&&`named “${name}”`,state&&`state ${state}`].filter(Boolean).join(", ")},displayName:body=>text(body.name),facts:body=>compactFacts({display_name:text(body.name),state:text(body.state),warehouse_type:text(body.warehouse_type),cluster_size:text(body.cluster_size)}),grant:principal=>cli(`databricks permissions update warehouses ${warehouse} --json '{"access_control_list":[{"user_name":"${principal||"<the signed-in user>"}","permission_level":"CAN_USE"}]}'`)})}for(const[id,label]of[["genie-data","Data Genie space"],["genie-dictionary","Dictionary Genie space"]]){const space=value(id);if(!space)continue;subjects.push({id,kind:"genie-space",name:space,label:`${label} · ${space}`,path:`/api/2.0/genie/spaces/${encodeURIComponent(space)}`,proves:"It does not prove a question would be answered: a space someone can open can still be backed by tables they cannot read, and CAN RUN is a separate grant from CAN VIEW.",observe:body=>{const title=text(body.title);return title?`titled “${title}”`:""},displayName:body=>text(body.title),facts:body=>{const dataSources=record(body.data_sources);const tables=Array.isArray(dataSources.tables)?dataSources.tables.length:Array.isArray(body.tables)?body.tables.length:null;return compactFacts({display_name:text(body.title),warehouse_id:text(body.warehouse_id),table_count:tables})},grant:principal=>cli(`databricks permissions update genie ${space} --json '{"access_control_list":[{"user_name":"${principal||"<the signed-in user>"}","permission_level":"CAN_RUN"}]}'`)})}const catalog=value("catalog");if(catalog){subjects.push({id:"catalog",kind:"catalog",name:catalog,label:`Catalog · ${catalog}`,path:`/api/2.1/unity-catalog/catalogs/${encodeURIComponent(catalog)}`,proves:"Every schema and table inside it is granted separately, so this covers the container only.",observe:body=>{const owner=text(body.owner);return owner?`owned by ${owner}`:""},grant:principal=>sql(`GRANT USE CATALOG ON CATALOG ${catalog} TO ${granteeFor(principal)};`)})}const schema=value("schema");if(catalog&&schema){const full=`${catalog}.${schema}`;subjects.push({id:"schema",kind:"schema",name:full,label:`Schema · ${full}`,path:`/api/2.1/unity-catalog/schemas/${encodeURIComponent(full)}`,proves:"Each table inside it is granted separately, so this covers the container only.",observe:body=>{const owner=text(body.owner);return owner?`owned by ${owner}`:""},grant:principal=>sql(`GRANT USE SCHEMA ON SCHEMA ${full} TO ${granteeFor(principal)};`)})}for(const table of input.tables){subjects.push({id:`table:${table}`,kind:"table",name:table,label:table,path:`/api/2.1/unity-catalog/tables/${encodeURIComponent(table)}`,proves:"It reads the table’s definition, not its rows. Row filters and column masks are applied when data is read and are invisible to this call.",observe:body=>{const columns=Array.isArray(body.columns)?body.columns.length:0;return columns?`${columns} columns`:""},grant:principal=>sql(`GRANT SELECT ON TABLE ${table} TO ${granteeFor(principal)};`,"This is not enough on its own: the holder also needs USE CATALOG and USE SCHEMA on the two containers above it.")})}for(const{id,label,note}of SERVING_ENDPOINTS){const endpoint=value(id);if(!endpoint||endpoint.includes("/"))continue;subjects.push(servingEndpointSubject(id,label,endpoint,note))}const index=resolveSemanticIndexValue(value("semantic-index"),value("catalog"),value("schema"));if(index&&index.includes(".")){subjects.push({id:"semantic-index",kind:"vector-index",name:index,label:`Vector Search index · ${index}`,path:`/api/2.0/vector-search/indexes/${encodeURIComponent(index)}`,proves:"It does not prove a search would return anything: an index that exists can still be empty or behind on its sync.",observe:body=>{const endpoint=text(body.endpoint_name);const state=text(body.status?.detailed_state);return[endpoint&&`served by ${endpoint}`,state&&`state ${state}`].filter(Boolean).join(", ")},displayName:body=>text(body.name),facts:body=>{const status=record(body.status);const delta=record(body.delta_sync_index_spec);return compactFacts({display_name:text(body.name),endpoint:text(body.endpoint_name),state:text(status.detailed_state),index_type:text(body.index_type)||text(delta.pipeline_type),source_table:text(delta.source_table)})},contentAt:body=>indexContentAt(body),grant:principal=>sql(`GRANT SELECT ON TABLE ${index} TO ${granteeFor(principal)};`,"This is not enough on its own: the holder also needs USE CATALOG and USE SCHEMA on the catalog and schema the index sits in.")})}return subjects}function indexContentAt(body){const status=body.status??{};for(const key of["triggered_update_status","continuous_update_status"]){const stamp=text(status[key]?.last_processed_commit_timestamp);if(stamp&&!Number.isNaN(new Date(stamp).getTime()))return stamp}return""}function servingEndpointSubject(id,label,name,note){return{id,kind:SERVING_ENDPOINT_KIND,name,label:`${label} · ${name}`,path:`/api/2.0/serving-endpoints/${encodeURIComponent(name)}`,proves:`${note} Seeing an endpoint is not being allowed to call it: CAN_VIEW and CAN_QUERY are separate grants, and this call needs only the first.`,observe:body=>{const state=body.state??{};const ready=text(state.ready);return ready?`state ${ready}`:""},facts:servingFacts,grant:principal=>cli(`databricks permissions update serving-endpoints ${name} --json '{"access_control_list":[{"user_name":"${principal||"<the signed-in user>"}","permission_level":"CAN_QUERY"}]}'`)}}function vectorEndpointSubject(indexBody){const endpoint=text(indexBody.endpoint_name);if(!endpoint)return null;const endpointId=text(indexBody.endpoint_id);return{id:"semantic-index-endpoint",kind:"vector-endpoint",name:endpoint,label:`Vector Search endpoint · ${endpoint}`,path:`/api/2.0/vector-search/endpoints/${encodeURIComponent(endpoint)}`,proves:"It does not prove a search would return anything; it says the endpoint serving the index exists.",observe:body=>{const status=body.endpoint_status??{};const state=text(status.state);return state?`state ${state}`:""},facts:body=>{const status=record(body.endpoint_status);return compactFacts({state:text(status.state),endpoint_type:text(body.endpoint_type)})},grant:endpointId?principal=>cli(`databricks permissions update vector-search-endpoints ${endpointId} --json '{"access_control_list":[{"user_name":"${principal||"<the signed-in user>"}","permission_level":"CAN_USE"}]}'`,"This endpoint is not a Unity Catalog securable, so no GRANT reaches it."):void 0}}function refused(status,code){return status===403||code==="PERMISSION_DENIED"}function absent(status,code){return status===404||code.endsWith("_DOES_NOT_EXIST")||code==="NOT_FOUND"}var GRANT_MARKERS=["permission_denied","does not have permission","is not accessible","not authorized","insufficient privileges","insufficient_permissions"];function scopesFromRefusal(message){const named=/required scopes?:\s*([^[\]\n]+)/i.exec(message);if(!named)return[];return named[1].split(/[,\s]+/).map(entry=>entry.trim().replace(/[.;]+$/,"")).filter(Boolean)}function refusalCause(input){const named=scopesFromRefusal(input.message);if(named.length>0||looksLikeMissingScope(input.message)){return{kind:"scope",scope:named[0]||input.scope,evidence:`the workspace said so: ${input.message}`}}if(input.scope&&input.scopeHeld===false){return{kind:"scope",scope:input.scope,evidence:`the response did not say why, but the forwarded token lists its own scopes and \`${input.scope}\` is not among them`}}const wording=input.message.toLowerCase();const saysPermission=input.code==="PERMISSION_DENIED"||GRANT_MARKERS.some(marker=>wording.includes(marker));if(saysPermission){return{kind:"grant",evidence:input.scopeHeld?`the workspace named a permission, and the forwarded token does carry \`${input.scope}\`, so the scope is not what was missing`:"the workspace answered in terms of a permission rather than of a scope"}}if(input.scopeHeld===true){return{kind:"grant",evidence:`the workspace gave no reason, but the forwarded token carries \`${input.scope}\`, so the scope is ruled out and a grant is what is left`}}return{kind:"undetermined"}}function check(subject,over){const built={id:subject.id,kind:subject.kind,name:subject.name,label:subject.label,status:"unverified",stopped:"unasked",detail:"",checked_with:`${READS_METADATA}: ${subject.path}`,duration_ms:0,error:"",remedy:null,...over};if(built.status!=="unverified")delete built.stopped;return built}function probeVerdict(input){const{subject,outcome,principal}=input;const durationMs=input.durationMs??0;const who=principal?` as ${principal}`:"";if(outcome.kind==="timeout"){return check(subject,{status:"unverified",stopped:"unreachable",duration_ms:durationMs,detail:`The workspace did not answer within ${outcome.afterMs} ms, so whether this identity can reach it is unknown rather than settled. A slow answer is not a refusal.`,error:`no answer within ${outcome.afterMs} ms`})}if(outcome.kind==="unreachable"){return check(subject,{status:"unverified",stopped:"unreachable",duration_ms:durationMs,detail:"The workspace could not be asked about this one, so nothing was established either way. This says nothing about the object and everything about the call.",error:outcome.message})}const{status,body}=outcome;const code=text(body.error_code);const message=text(body.message);if(status>=200&&status<300){const observed=subject.observe?.(body)??"";const contentAt=subject.contentAt?subject.contentAt(body):"";const freshness=!subject.contentAt?"":contentAt?`It last took content from its source at ${contentAt}. `:"The workspace reported no time for when it last took content from its source, so the age of what it serves is not established here. ";return check(subject,{status:"ok",display_name:subject.displayName?.(body)||void 0,facts:subject.facts?.(body),duration_ms:durationMs,content_at:contentAt,detail:`The workspace answered${who}${observed?`: ${observed}`:""}. ${freshness}That is a metadata read. ${subject.proves}`})}if(refused(status,code)){const scope=scopeForPath(subject.path);const tokenScopes=input.tokenScopes??null;const scopeHeld=tokenScopes===null?null:tokenScopeVerdict(tokenScopes,scope);const cause=refusalCause({message,code,scope,scopeHeld});const refusal=`HTTP ${status}${code?` ${code}`:""}`;if(cause.kind==="scope"){const declarable=scope||cause.scope;const diagnosis=scopeRefusalDiagnosis({declarable,namedByWorkspace:cause.scope,declared:input.declaredScopes??null,tokenScopes,scopeHeld});if(diagnosis.grantIsMissing){return check(subject,{status:"failed",duration_ms:durationMs,detail:`${refusal}. ${diagnosis.explanation}`,error:message||refusal,remedy:subject.grant?.(principal)??null})}return check(subject,{status:"unverified",stopped:"refused",scope:declarable,duration_ms:durationMs,detail:`${refusal}. ${diagnosis.explanation}`,error:message||refusal,remedy:diagnosis.remedy})}if(cause.kind==="undetermined"){return check(subject,{status:"unverified",stopped:"refused",duration_ms:durationMs,detail:`The workspace refused the call${who} and did not say why: ${refusal}, with no message and no scopes readable off the token. That is one of two things and this cannot tell which: either ${principal||"the signed-in user"} lacks a grant on the object, or the app's forwarded token lacks the \`${scope||"required"}\` scope. Check the token's scopes first -- it is the cheaper of the two to rule out, and it is the one that would make every other row on this page wrong in the same way.`,error:message||refusal,remedy:null})}return check(subject,{status:"failed",duration_ms:durationMs,detail:`The workspace refused this identity${who}: ${refusal}. The object exists as far as this call can tell; what was established is that this identity cannot reach it -- ${cause.evidence}. A grant fixes this.`,error:message||refusal,remedy:subject.grant?.(principal)??null})}if(absent(status,code)){return check(subject,{status:"failed",duration_ms:durationMs,detail:`The workspace has no such object: HTTP ${status}${code?` ${code}`:""}. This is missing rather than forbidden, so no grant repairs it: either the value this deployment was configured with is wrong, or the object was removed.`,error:message||`HTTP ${status}${code?` ${code}`:""}`,remedy:null})}if(status===401){return check(subject,{status:"unverified",stopped:"refused",duration_ms:durationMs,detail:"The workspace rejected the token this call was made with, so nothing was established about this object. That is a problem with the sign-in rather than with the resource.",error:message||`HTTP ${status}`})}if(status===429||status>=500){return check(subject,{status:"unverified",stopped:"unreachable",duration_ms:durationMs,detail:`The workspace answered HTTP ${status}, which is about the workspace rather than about this object. Nothing was established either way.`,error:message||`HTTP ${status}`})}return check(subject,{status:"failed",duration_ms:durationMs,detail:`The workspace refused the request: HTTP ${status}${code?` ${code}`:""}. A malformed identifier answers this way, so the configured value is the first thing to read.`,error:message||`HTTP ${status}`})}function unaskedChecks(subjects,reason){return subjects.map(subject=>check(subject,{status:"unverified",stopped:"unasked",detail:`${reason} So this is unchecked rather than unreachable: nobody asked.`,error:""}))}async function runProbe(subject,options){const call=options.fetchImpl??fetch;const timeoutMs=options.timeoutMs??PROBE_TIMEOUT_MS;const started=Date.now();const tokenScopes=options.token?scopesFromToken(options.token):null;const declaredScopes=options.declaredScopes===void 0?declaredUserApiScopes():options.declaredScopes;const finish=outcome=>probeVerdict({subject,outcome,principal:options.principal,durationMs:Date.now()-started,tokenScopes,declaredScopes});try{const probeTimeout=AbortSignal.timeout(timeoutMs);const signal=options.signal?AbortSignal.any([options.signal,probeTimeout]):probeTimeout;const response=await call(`${options.host}${subject.path}`,{method:"GET",headers:{authorization:`Bearer ${options.token??""}`},signal});const body=await response.json().catch(()=>({}));return finish({kind:"answered",status:response.status,body:body??{}})}catch(error){const name=error?.name;if(name==="TimeoutError"||name==="AbortError")return finish({kind:"timeout",afterMs:timeoutMs});return finish({kind:"unreachable",message:error?.message??String(error)})}}async function runProbes(subjects,options){if(subjects.length===0)return[];const limit=Math.max(1,Math.min(subjects.length,Math.floor(options.concurrency??subjects.length)));const results=Array.from({length:subjects.length});let next=0;const workers=Array.from({length:limit},async()=>{while(next<subjects.length){const index=next;next+=1;results[index]=await runProbe(subjects[index],options)}});await Promise.allSettled(workers);return results.map((result,index)=>result??probeVerdict({subject:subjects[index],outcome:{kind:"unreachable",message:"The bounded probe worker stopped before this check ran."},principal:options.principal,tokenScopes:options.token?scopesFromToken(options.token):null,declaredScopes:options.declaredScopes===void 0?declaredUserApiScopes():options.declaredScopes}))}function withDerivedSemanticIndex(configured){const next={...configured};const raw=(next["semantic-index"]??"").trim();const derived=derivedSemanticIndexName(next.catalog??"",next.schema??"");if(!raw){if(!derived)return{configured:next,indexIsCandidate:false};next["semantic-index"]=derived;return{configured:next,indexIsCandidate:true}}const resolved=resolveSemanticIndexValue(raw,next.catalog??"",next.schema??"");if(resolved.includes("."))next["semantic-index"]=resolved;return{configured:next,indexIsCandidate:false}}async function probeConnections(input){const resolved=withDerivedSemanticIndex(input.configured);const configured=resolved.configured;const subjects=connectionSubjects({...input,configured});if(subjects.length===0)return[];const settle=checks2=>{let next=checks2;if(resolved.indexIsCandidate){const indexCheck2=next.find(entry=>entry.id==="semantic-index");if(indexCheck2?.status!=="ok"){next=next.filter(entry=>entry.id!=="semantic-index"&&entry.id!=="semantic-index-endpoint");return withSemanticFollowUps(withManifestRollup(next),input.configured)}}return withSemanticFollowUps(withManifestRollup(next),configured)};if(!input.host){return settle(unaskedChecks(subjects,"The app container was given no DATABRICKS_HOST, so it does not know which workspace to ask."))}if(!input.token){return settle(unaskedChecks(subjects,"This request carried no signed-in user token, and these answers are about the signed-in user’s own grants rather than the app’s."))}const options={host:input.host,token:input.token,principal:input.principal,declaredScopes:input.declaredScopes,fetchImpl:input.fetchImpl,timeoutMs:input.timeoutMs,signal:input.signal,concurrency:input.concurrency};const checks=await runProbes(subjects,options);const indexCheck=checks.find(entry=>entry.id==="semantic-index");if(indexCheck?.status==="ok"){const indexSubject=subjects.find(subject=>subject.id==="semantic-index");if(indexSubject){const body=await readIndexBody(indexSubject,options);const endpointSubject=body?vectorEndpointSubject(body):null;if(endpointSubject)checks.push(await runProbe(endpointSubject,options))}}return settle(checks)}function withSemanticFollowUps(checks,configured){const index=(configured["semantic-index"]??"").trim();if(!index)return checks;const answered=id=>checks.some(check2=>check2.id===id);const added=[];if(!index.includes(".")&&!answered("semantic-index")){added.push({id:"semantic-index",kind:"vector-index",name:"",label:"Vector Search index",status:"unverified",stopped:"unasked",detail:`This release searches an index, but the served model version reports the setting as “${index}” rather than as the resolved three-level name, so there is no object to ask about. Re-logging the model reports the name it resolved.`,checked_with:"the orchestrator’s reported configuration",duration_ms:0,error:"",remedy:null})}const indexVerdict=checks.find(check2=>check2.id==="semantic-index");if(!answered("semantic-index-endpoint")&&indexVerdict?.status!=="ok"){added.push({id:"semantic-index-endpoint",kind:"vector-endpoint",name:"",label:"Vector Search endpoint",status:"unverified",stopped:indexVerdict?.stopped??"unasked",detail:"Only the index names the endpoint serving it, and the index did not answer, so there was nothing to ask about. Whichever statement clears the index above clears this one with it.",checked_with:"derived from the index check above",duration_ms:0,error:"",remedy:null})}return added.length>0?[...checks,...added]:checks}function withManifestRollup(checks){const tables=checks.filter(entry=>entry.kind==="table");if(tables.length===0)return checks;const failed2=tables.filter(entry=>entry.status==="failed");const unverified=tables.filter(entry=>entry.status==="unverified");const status=failed2.length>0?"failed":unverified.length>0?"unverified":"ok";const stopped=unverified.some(entry=>entry.stopped==="refused")?"refused":unverified.find(entry=>entry.stopped)?.stopped??"unasked";const refusedScopes=new Set(unverified.map(entry=>(entry.scope??"").trim()));const scope=failed2.length===0&&refusedScopes.size===1&&unverified.length===tables.length?[...refusedScopes][0]:"";return[...checks,{id:"declared-manifest",kind:"manifest",name:"",label:`Declared tables · ${tables.length}`,status,...status==="unverified"?{stopped}:{},...scope?{scope}:{},detail:failed2.length>0?`${failed2.length} of ${tables.length} declared tables could not be read by this identity. Each one is listed under Unity Catalog tables with what the workspace said about it.`:unverified.length>0?`${tables.length-unverified.length} of ${tables.length} declared tables answered, and ${unverified.length} did not, so the manifest as a whole is unconfirmed rather than clear.`:`All ${tables.length} declared tables answered a metadata read by this identity. Row filters and column masks are applied when data is read and are not covered.`,checked_with:"derived from the table checks above",duration_ms:0,error:"",remedy:null}]}async function readIndexBody(subject,options){try{const call=options.fetchImpl??fetch;const response=await call(`${options.host}${subject.path}`,{method:"GET",headers:{authorization:`Bearer ${options.token??""}`},signal:options.signal?AbortSignal.any([options.signal,AbortSignal.timeout(options.timeoutMs??PROBE_TIMEOUT_MS)]):AbortSignal.timeout(options.timeoutMs??PROBE_TIMEOUT_MS)});if(!response.ok)return null;return await response.json()}catch{return null}}import{createHash}from"node:crypto";function browseScopeUnavailableDetail(scope){return`Browsing is unavailable because your sign-in does not carry \`${scope}\`. Nothing was established about which assets exist. Enter the value by hand, or sign in again after this app asks for that permission.`}function browseAppsHasNoScopeDetail(family){return`Browsing is unavailable because Databricks Apps has no ${family} scope to forward. Nothing was established about which assets exist. Enter the value by hand.`}var DISCOVERY_MAX_CONCURRENCY=4;var DISCOVERY_CACHE_TTL_MS=1e4;var DISCOVERY_CACHE_MAX_ENTRIES=128;function abortError(signal){return signal.reason instanceof Error?signal.reason:new DOMException("Aborted","AbortError")}var DiscoveryLimiter=class{constructor(concurrency=DISCOVERY_MAX_CONCURRENCY){this.concurrency=concurrency}active=0;queue=[];run(signal,work){if(signal?.aborted)return Promise.reject(abortError(signal));return new Promise((resolve,reject)=>{const queued={signal,run:work,resolve,reject};if(signal){const abort=()=>{const index=this.queue.indexOf(queued);if(index>=0)this.queue.splice(index,1);reject(abortError(signal))};signal.addEventListener("abort",abort,{once:true});queued.removeAbort=()=>signal.removeEventListener("abort",abort)}this.queue.push(queued);this.drain()})}drain(){while(this.active<this.concurrency&&this.queue.length>0){const queued=this.queue.shift();if(queued.signal?.aborted){queued.removeAbort?.();queued.reject(abortError(queued.signal));continue}queued.removeAbort?.();this.active+=1;void queued.run().then(queued.resolve,queued.reject).finally(()=>{this.active-=1;this.drain()})}}};var discoveryLimiter=new DiscoveryLimiter;var DiscoveryPageCache=class{constructor(maxEntries=DISCOVERY_CACHE_MAX_ENTRIES,ttlMs=DISCOVERY_CACHE_TTL_MS){this.maxEntries=maxEntries;this.ttlMs=ttlMs}entries=new Map;get(key,now=Date.now()){const entry=this.entries.get(key);if(!entry)return void 0;if(entry.expiresAt<=now){this.entries.delete(key);return void 0}this.entries.delete(key);this.entries.set(key,entry);return structuredClone(entry.value)}set(key,value,now=Date.now()){this.entries.delete(key);this.entries.set(key,{expiresAt:now+this.ttlMs,value:structuredClone(value)});while(this.entries.size>this.maxEntries){const oldest=this.entries.keys().next().value;if(oldest===void 0)break;this.entries.delete(oldest)}}clear(){this.entries.clear()}get size(){return this.entries.size}};var BROWSE_TIMEOUT_MS=15e3;var BROWSE_PAGE_SIZE=100;var BROWSE_PAGE_LIMIT=5;var UC_SEARCH_RESULT_LIMIT=100;var UC_SEARCH_CATALOG_SCAN_LIMIT=24;var UC_SEARCH_SCHEMA_SCAN_LIMIT=32;var BROWSE_SCOPE_BY_PATH={"/api/2.1/unity-catalog/catalogs":"catalog.catalogs:read","/api/2.1/unity-catalog/schemas":"catalog.schemas:read","/api/2.1/unity-catalog/tables":"catalog.tables:read","/api/2.0/sql/warehouses":"sql","/api/2.0/genie/spaces":"dashboards.genie","/api/2.0/workspace/list":"workspace.workspace:read","/api/2.0/workspace/get-status":"workspace.workspace:read","/api/2.0/workspace/export":"workspace.workspace:read","/api/2.0/serving-endpoints":"serving.serving-endpoints","/api/2.0/vector-search/endpoints":"vectorsearch.vector-search-endpoints:read","/api/2.0/vector-search/indexes":"vectorsearch.vector-search-indexes:read","/api/2.0/postgres/projects":"postgres"};function scopeForBrowsePath(path){const exact=BROWSE_SCOPE_BY_PATH[path];if(exact)return exact;if(path.startsWith("/api/2.0/postgres/"))return"postgres";return scopeForPath(path.endsWith("/")?path:`${path}/`)}function text2(value){return typeof value==="string"?value.trim():""}function unavailable(kind,scope){return{status:"unavailable",kind,reason:"scope_not_carried",scope,detail:browseScopeUnavailableDetail(scope)}}function unavailableNoAppsScope(kind,family){return{status:"unavailable",kind,reason:"apps_has_no_scope",scope:"",detail:browseAppsHasNoScopeDetail(family)}}function failed(kind,detail,error="",incompleteReason="failed"){return{status:"failed",kind,detail,error,incomplete_reason:incompleteReason}}function ok(kind,items,nextPageToken="",path="",page=1){const capped=page>=BROWSE_PAGE_LIMIT&&Boolean(nextPageToken);return{status:"ok",kind,items,next_page_token:capped?"":nextPageToken,path,pagination:{complete:!nextPageToken,incomplete_reason:nextPageToken?capped?"page_cap":"more_available":"",page,page_limit:BROWSE_PAGE_LIMIT,page_size:BROWSE_PAGE_SIZE,returned:items.length}}}var pageCache=new DiscoveryPageCache;function cacheKey(pathAndQuery,options){if(options.skipCache)return"";const principal=options.principal?.trim().toLocaleLowerCase()??"";if(!principal)return"";const tokenHash=createHash("sha256").update(options.token).digest("base64url").slice(0,16);return`${principal}\0${tokenHash}\0${pathAndQuery}`}function combinedBrowseSignal(options){const timeout=AbortSignal.timeout(options.timeoutMs??BROWSE_TIMEOUT_MS);return options.signal?AbortSignal.any([options.signal,timeout]):timeout}async function workspaceGet(pathAndQuery,options){const call=options.fetchImpl??fetch;const key=cacheKey(pathAndQuery,options);const cached=key?pageCache.get(key):void 0;if(cached)return cached;const signal=combinedBrowseSignal(options);try{const response=await discoveryLimiter.run(signal,()=>call(`${options.host}${pathAndQuery}`,{method:"GET",headers:{authorization:`Bearer ${options.token}`},signal}));const body=await response.json().catch(()=>({}));const answer={kind:"http",status:response.status,body:body??{}};if(key&&response.status>=200&&response.status<300)pageCache.set(key,answer);return answer}catch(error){if(signal.aborted){const reasonName=signal.reason?.name;return reasonName==="TimeoutError"?{kind:"timeout"}:{kind:"cancelled"}}const name=error?.name;if(name==="TimeoutError"||name==="AbortError")return{kind:"timeout"};return{kind:"unreachable",message:error?.message??String(error)}}}function browseBlockedByScope(input){const scope=scopeForBrowsePath(input.apiPath);if(!scope)return null;const declared=input.declaredScopes===void 0?declaredUserApiScopes():input.declaredScopes;if(declared&&!declared.includes(scope)){return scope}const held=scopesFromToken(input.token);if(held===null)return null;const verdict=tokenScopeVerdict(held,scope);if(verdict===false)return scope;return null}function interpretBrowseAnswer(input){const{kind,apiPath,answer}=input;if(answer.kind==="timeout"){return failed(kind,"The workspace did not answer in time, so nothing about this list was established.","timeout","deadline")}if(answer.kind==="cancelled"){return failed(kind,"Resource discovery was cancelled, so this list is incomplete.","cancelled","cancelled")}if(answer.kind==="unreachable"){return failed(kind,"The workspace could not be asked for this list, so nothing was established.",answer.message)}const{status,body}=answer;if(status>=200&&status<300){const parsed=input.itemsFromBody(body);return ok(kind,parsed.items,parsed.next_page_token,input.path??"",input.page??1)}const code=text2(body.error_code);const message=text2(body.message);const scope=scopeForBrowsePath(apiPath);const named=scopesFromRefusal(message);const looksScope=named.length>0||looksLikeMissingScope(message);const tokenScopes=input.tokenScopes??null;const scopeHeld=tokenScopes===null?null:tokenScopeVerdict(tokenScopes,scope);if(status===403||code==="PERMISSION_DENIED"){if(scopeHeld===true&&!looksScope){return failed(kind,`The workspace refused this list: HTTP ${status}${code?` ${code}`:""}. Your sign-in carries the API permission, so this is likely a grant on the object.`,message||`HTTP ${status}`)}if(looksScope||scope.startsWith("catalog.")&&scopeHeld!==true){return unavailable(kind,scope||named[0]||"required")}const cause=refusalCause({message,code,scope,scopeHeld});if(cause.kind==="scope"){return unavailable(kind,cause.scope||scope||"required")}return failed(kind,`The workspace refused this list: HTTP ${status}${code?` ${code}`:""}. That may be a grant on the object rather than a missing sign-in permission.`,message||`HTTP ${status}`)}return failed(kind,`The workspace refused this list: HTTP ${status}${code?` ${code}`:""}.`,message||`HTTP ${status}`)}function pageQuery(pageToken,pageSize=BROWSE_PAGE_SIZE){const parts=[`max_results=${pageSize}`];if(pageToken)parts.push(`page_token=${encodeURIComponent(pageToken)}`);return parts.join("&")}async function listWithGuard(kind,apiPath,pathAndQuery,options,itemsFromBody,listedPath=""){const page=options.page??(pathAndQuery.includes("page_token=")?2:1);if(!Number.isInteger(page)||page<1||page>BROWSE_PAGE_LIMIT){return failed(kind,`Resource discovery is limited to ${BROWSE_PAGE_LIMIT} pages per list.`,"page cap reached")}if(!options.host){return failed(kind,"This app was given no workspace host, so it does not know where to browse.")}if(!options.token){return failed(kind,"This request carried no signed-in user token, so browsing as you is not possible.")}const blocked=browseBlockedByScope({apiPath,token:options.token,declaredScopes:options.declaredScopes});if(blocked)return unavailable(kind,blocked);const answer=await workspaceGet(pathAndQuery,options);return interpretBrowseAnswer({kind,apiPath,answer,itemsFromBody,path:listedPath,tokenScopes:scopesFromToken(options.token),page})}function catalogItems(body){const rows=Array.isArray(body.catalogs)?body.catalogs:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const name=text2(row.name);if(!name)continue;items.push({id:name,label:name,secondary:"",expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function schemaItems(body){const rows=Array.isArray(body.schemas)?body.schemas:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const fullName=text2(record2.full_name);const name=text2(record2.name);const schemaName=name.includes(".")?name.slice(name.indexOf(".")+1):name||(fullName.includes(".")?fullName.slice(fullName.indexOf(".")+1):fullName);if(!schemaName)continue;items.push({id:schemaName,label:schemaName,secondary:fullName,expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function tableItems(body){const rows=Array.isArray(body.tables)?body.tables:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const fullName=text2(record2.full_name)||text2(record2.name);if(!fullName)continue;const short=fullName.includes(".")?fullName.split(".").pop():fullName;const tableType=text2(record2.table_type);items.push({id:fullName,label:short,secondary:tableType,...tableType?{asset_type:/VIEW/i.test(tableType)?"view":"table"}:{},expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function warehouseItems(body){const rows=Array.isArray(body.warehouses)?body.warehouses:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const id=text2(record2.id);if(!id)continue;items.push({id,label:text2(record2.name)||id,secondary:text2(record2.state),expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function genieItems(body){const rows=Array.isArray(body.spaces)?body.spaces:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const id=text2(record2.space_id)||text2(record2.id);if(!id)continue;items.push({id,label:text2(record2.title)||id,secondary:"",expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function servingEndpointItems(body){const rows=Array.isArray(body.endpoints)?body.endpoints:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const name=text2(record2.name);if(!name)continue;const state=record2.state;const ready=state&&typeof state==="object"?text2(state.ready):"";items.push({id:name,label:name,secondary:text2(record2.task)||ready,expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function notebookItems(body){const rows=Array.isArray(body.objects)?body.objects:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const path=text2(record2.path);if(!path)continue;const objectType=text2(record2.object_type).toUpperCase();if(objectType!=="DIRECTORY"&&objectType!=="NOTEBOOK")continue;const label=path.includes("/")?path.slice(path.lastIndexOf("/")+1):path;items.push({id:path,label:label||path,secondary:objectType==="NOTEBOOK"?text2(record2.language):"Directory",expandable:objectType==="DIRECTORY"})}return{items,next_page_token:""}}async function listCatalogs(options){const apiPath="/api/2.1/unity-catalog/catalogs";const query=pageQuery(options.pageToken);return listWithGuard("catalogs",apiPath,`${apiPath}?${query}`,options,catalogItems)}async function listSchemas(options){const catalog=options.catalog.trim();if(!catalog){return failed("schemas","A catalog name is required to list schemas.")}const apiPath="/api/2.1/unity-catalog/schemas";const query=`catalog_name=${encodeURIComponent(catalog)}&${pageQuery(options.pageToken)}`;return listWithGuard("schemas",apiPath,`${apiPath}?${query}`,options,schemaItems)}async function listTables(options){const catalog=options.catalog.trim();const schema=options.schema.trim();if(!catalog||!schema){return failed("tables","A catalog and schema are required to list tables.")}const apiPath="/api/2.1/unity-catalog/tables";const query=`catalog_name=${encodeURIComponent(catalog)}&schema_name=${encodeURIComponent(schema)}&${pageQuery(options.pageToken)}`;return listWithGuard("tables",apiPath,`${apiPath}?${query}`,options,tableItems)}async function validateUnityCatalogAsset(options){const value=options.value.trim();const apiPath=options.resourceType==="catalog"?"/api/2.1/unity-catalog/catalogs":options.resourceType==="schema"?"/api/2.1/unity-catalog/schemas":"/api/2.1/unity-catalog/tables";if(!options.host||!options.token){return{ok:false,detail:"The signed-in user context needed to validate this asset is unavailable."}}const blocked=browseBlockedByScope({apiPath,token:options.token,declaredScopes:options.declaredScopes});if(blocked)return{ok:false,detail:blocked};const answer=await workspaceGet(`${apiPath}/${encodeURIComponent(value)}`,options);if(answer.kind==="http"&&answer.status>=200&&answer.status<300)return{ok:true};if(answer.kind==="http"){return{ok:false,detail:`Unity Catalog could not validate ${value}: HTTP ${answer.status}.`}}return{ok:false,detail:answer.kind==="timeout"?`Validation timed out for ${value}.`:`Validation was cancelled before ${value} could be confirmed.`}}function normalizedUnityCatalogSearch(value){const words=value.trim().toLocaleLowerCase().replace(/[._\-\s]+/g," ").replace(/\s+/g," ");return{words,compact:words.replace(/\s+/g,"")}}function matchesUnityCatalogSearch(value,query){const candidate=normalizedUnityCatalogSearch(value);return candidate.words.includes(query.words)||candidate.compact.includes(query.compact)}async function browsePages(first,next,maxPages){let response=await first();let items=[];let page=1;const tokens=new Set;while(response.status==="ok"){items=[...new Map([...items,...response.items].map(item=>[item.id,item])).values()];if(!response.next_page_token||page>=maxPages||tokens.has(response.next_page_token)){return{items,response,more:Boolean(response.next_page_token)}}tokens.add(response.next_page_token);page+=1;response=await next(response.next_page_token,page)}return{items,response,more:false}}async function searchUnityCatalogAssets(options,rawQuery){const query=normalizedUnityCatalogSearch(rawQuery);if(query.compact.length<2)return{status:"ok",items:[],more_results:false};const catalogs=await browsePages(()=>listCatalogs({...options,page:1}),(pageToken,page)=>listCatalogs({...options,pageToken,page}),BROWSE_PAGE_LIMIT);if(catalogs.response.status!=="ok"){return{status:catalogs.response.status,items:[],detail:catalogs.response.detail,more_results:false}}const results=catalogs.items.filter(item=>matchesUnityCatalogSearch(item.id,query)).map(item=>({resource_type:"catalog",value:item.id,label:item.label}));const orderedCatalogs=[...catalogs.items].sort((a,b)=>Number(matchesUnityCatalogSearch(b.id,query))-Number(matchesUnityCatalogSearch(a.id,query)));const scannedCatalogs=orderedCatalogs.slice(0,UC_SEARCH_CATALOG_SCAN_LIMIT);let more=catalogs.more||orderedCatalogs.length>scannedCatalogs.length;const schemaLists=await Promise.all(scannedCatalogs.map(async catalog=>{const listed=await browsePages(()=>listSchemas({...options,catalog:catalog.id,page:1}),(pageToken,page)=>listSchemas({...options,catalog:catalog.id,pageToken,page}),2);return{catalog:catalog.id,...listed}}));const schemas=[];for(const listed of schemaLists){more||=listed.more||listed.response.status!=="ok";for(const item of listed.items){const value=item.secondary||`${listed.catalog}.${item.id}`;schemas.push({catalog:listed.catalog,item,value});if(matchesUnityCatalogSearch(value,query)){results.push({resource_type:"schema",value,label:item.label})}}}const orderedSchemas=[...schemas].sort((a,b)=>Number(matchesUnityCatalogSearch(b.value,query))-Number(matchesUnityCatalogSearch(a.value,query)));const scannedSchemas=orderedSchemas.slice(0,UC_SEARCH_SCHEMA_SCAN_LIMIT);more||=orderedSchemas.length>scannedSchemas.length;const tableLists=await Promise.all(scannedSchemas.map(async({catalog,item})=>{const listed=await browsePages(()=>listTables({...options,catalog,schema:item.id,page:1}),(pageToken,page)=>listTables({...options,catalog,schema:item.id,pageToken,page}),1);return listed}));for(const listed of tableLists){more||=listed.more||listed.response.status!=="ok";for(const item of listed.items){if(matchesUnityCatalogSearch(item.id,query)){results.push({resource_type:"table",value:item.id,label:item.label,asset_type:/view/i.test(item.secondary)?"view":"table"})}}}const deduped=[...new Map(results.map(item=>[`${item.resource_type}:${item.value.toLocaleLowerCase()}`,item])).values()];return{status:"ok",items:deduped.slice(0,UC_SEARCH_RESULT_LIMIT),more_results:more||deduped.length>UC_SEARCH_RESULT_LIMIT}}async function listWarehouses(options){const apiPath="/api/2.0/sql/warehouses";const parts=[`page_size=${BROWSE_PAGE_SIZE}`];if(options.pageToken)parts.push(`page_token=${encodeURIComponent(options.pageToken)}`);return listWithGuard("warehouses",apiPath,`${apiPath}?${parts.join("&")}`,options,warehouseItems)}async function listGenieSpaces(options){const apiPath="/api/2.0/genie/spaces";const parts=[`page_size=${BROWSE_PAGE_SIZE}`];if(options.pageToken)parts.push(`page_token=${encodeURIComponent(options.pageToken)}`);return listWithGuard("genie-spaces",apiPath,`${apiPath}?${parts.join("&")}`,options,genieItems)}async function listServingEndpoints(options){const apiPath="/api/2.0/serving-endpoints";const parts=[];if(options.pageToken)parts.push(`page_token=${encodeURIComponent(options.pageToken)}`);const query=parts.length?`?${parts.join("&")}`:"";return listWithGuard("serving-endpoints",apiPath,`${apiPath}${query}`,options,servingEndpointItems)}async function listNotebooks(options){const path=options.path.trim();if(!path){return failed("notebooks","A workspace path is required to list notebooks.")}const apiPath="/api/2.0/workspace/list";const query=`path=${encodeURIComponent(path)}`;return listWithGuard("notebooks",apiPath,`${apiPath}?${query}`,options,notebookItems,path)}async function validateNotebookPath(pathInput,options){const path=pathInput.trim();if(!path.startsWith("/")||path.length>1024){return{ok:false,status:400,detail:"Choose an absolute workspace notebook path."}}const apiPaths=["/api/2.0/workspace/get-status","/api/2.0/workspace/export"];for(const apiPath of apiPaths){const missingScope=browseBlockedByScope({apiPath,token:options.token,declaredScopes:options.declaredScopes});if(missingScope){return{ok:false,status:403,detail:`Your sign-in does not carry ${missingScope}, so this notebook cannot be validated.`}}const suffix=apiPath.endsWith("/export")?`path=${encodeURIComponent(path)}&format=SOURCE`:`path=${encodeURIComponent(path)}`;const answer=await workspaceGet(`${apiPath}?${suffix}`,options);if(answer.kind!=="http"){return{ok:false,status:503,detail:answer.kind==="timeout"?"The workspace did not answer before notebook validation timed out.":"The workspace could not be reached to validate this notebook."}}if(answer.status===404||text2(answer.body.error_code)==="RESOURCE_DOES_NOT_EXIST"){return{ok:false,status:404,detail:"No workspace notebook exists at that path."}}if(answer.status===401||answer.status===403){return{ok:false,status:403,detail:"Your sign-in may not read that workspace notebook."}}if(answer.status<200||answer.status>=300){return{ok:false,status:503,detail:`The workspace refused notebook validation with HTTP ${answer.status}.`}}if(apiPath.endsWith("/get-status")&&text2(answer.body.object_type).toUpperCase()!=="NOTEBOOK"){return{ok:false,status:400,detail:"Choose a notebook, not a workspace folder."}}}return{ok:true,path}}function volumeItems(body){const rows=Array.isArray(body.volumes)?body.volumes:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const fullName=text2(record2.full_name)||text2(record2.name);if(!fullName)continue;const short=fullName.includes(".")?fullName.split(".").pop():fullName;items.push({id:short,label:short,secondary:text2(record2.volume_type)||fullName,expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}async function listVolumes(options){const catalog=options.catalog.trim();const schema=options.schema.trim();const page=options.page??(options.pageToken?2:1);if(!Number.isInteger(page)||page<1||page>BROWSE_PAGE_LIMIT){return failed("volumes",`Resource discovery is limited to ${BROWSE_PAGE_LIMIT} pages per list.`,"page cap reached")}if(!catalog||!schema){return failed("volumes","A catalog and schema are required to list volumes.")}const blocked=browseBlockedByScope({apiPath:"/api/2.1/unity-catalog/schemas",token:options.token,declaredScopes:options.declaredScopes});if(blocked)return unavailable("volumes",blocked);const apiPath="/api/2.1/unity-catalog/volumes";const query=`catalog_name=${encodeURIComponent(catalog)}&schema_name=${encodeURIComponent(schema)}&${pageQuery(options.pageToken)}`;if(!options.host){return failed("volumes","This app was given no workspace host, so it does not know where to browse.")}if(!options.token){return failed("volumes","This request carried no signed-in user token, so browsing as you is not possible.")}const answer=await workspaceGet(`${apiPath}?${query}`,options);return interpretBrowseAnswer({kind:"volumes",apiPath,answer,itemsFromBody:volumeItems,tokenScopes:scopesFromToken(options.token),page})}function vectorSearchEndpointItems(body){const rows=Array.isArray(body.endpoints)?body.endpoints:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const name=text2(record2.name);if(!name)continue;const status=record2.endpoint_status;const state=status&&typeof status==="object"?text2(status.state):"";const count=typeof record2.num_indexes==="number"?`${record2.num_indexes} indexes`:"";items.push({id:name,label:name,secondary:[state,count].filter(Boolean).join(", "),expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function vectorSearchIndexItems(body){const rows=Array.isArray(body.vector_indexes)?body.vector_indexes:Array.isArray(body.indexes)?body.indexes:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const name=text2(record2.name);if(!name)continue;const short=name.includes(".")?name.split(".").pop():name;items.push({id:name,label:short,secondary:text2(record2.index_type)||text2(record2.endpoint_name),expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}async function listVectorSearchEndpoints(options){const apiPath="/api/2.0/vector-search/endpoints";const parts=[];if(options.pageToken)parts.push(`page_token=${encodeURIComponent(options.pageToken)}`);const query=parts.length?`?${parts.join("&")}`:"";return listWithGuard("vector-search-endpoints",apiPath,`${apiPath}${query}`,options,vectorSearchEndpointItems)}async function listVectorSearchIndexes(options){const endpoint=options.endpoint.trim();if(!endpoint){return failed("vector-search-indexes","A Vector Search endpoint name is required to list indexes.")}const apiPath="/api/2.0/vector-search/indexes";const parts=[`endpoint_name=${encodeURIComponent(endpoint)}`];if(options.pageToken)parts.push(`page_token=${encodeURIComponent(options.pageToken)}`);return listWithGuard("vector-search-indexes",apiPath,`${apiPath}?${parts.join("&")}`,options,vectorSearchIndexItems)}function lakebaseProjectItems(body){const rows=Array.isArray(body.projects)?body.projects:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const name=text2(record2.name);if(!name)continue;const status=record2.status;const display=status&&typeof status==="object"?text2(status.display_name):"";const short=name.startsWith("projects/")?name.slice("projects/".length):name;items.push({id:name,label:display||short,secondary:display&&display!==short?short:"",expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function lakebaseBranchItems(body){const rows=Array.isArray(body.branches)?body.branches:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const name=text2(record2.name);if(!name)continue;const short=name.includes("/branches/")?name.slice(name.lastIndexOf("/branches/")+"/branches/".length):name;const status=record2.status;const state=status&&typeof status==="object"?text2(status.state):"";items.push({id:name,label:short,secondary:state,expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function lakebaseDatabaseItems(body){const rows=Array.isArray(body.databases)?body.databases:[];const items=[];for(const row of rows){if(!row||typeof row!=="object")continue;const record2=row;const name=text2(record2.name);if(!name)continue;const short=name.includes("/databases/")?name.slice(name.lastIndexOf("/databases/")+"/databases/".length):name;items.push({id:name,label:short,secondary:name,expandable:false})}return{items,next_page_token:text2(body.next_page_token)}}function lakebaseProjectParent(project){const trimmed=project.trim();if(!trimmed)return"";return trimmed.startsWith("projects/")?trimmed:`projects/${trimmed}`}async function listLakebaseProjects(options){const apiPath="/api/2.0/postgres/projects";const parts=[`page_size=${BROWSE_PAGE_SIZE}`];if(options.pageToken)parts.push(`page_token=${encodeURIComponent(options.pageToken)}`);return listWithGuard("lakebase-projects",apiPath,`${apiPath}?${parts.join("&")}`,options,lakebaseProjectItems)}async function listLakebaseBranches(options){const parent=lakebaseProjectParent(options.project);if(!parent){return failed("lakebase-branches","A Lakebase project is required to list branches.")}const apiPath=`/api/2.0/postgres/${parent}/branches`;const parts=[`page_size=${BROWSE_PAGE_SIZE}`];if(options.pageToken)parts.push(`page_token=${encodeURIComponent(options.pageToken)}`);return listWithGuard("lakebase-branches","/api/2.0/postgres/projects",`${apiPath}?${parts.join("&")}`,options,lakebaseBranchItems)}async function listLakebaseDatabases(options){const branch=options.branch.trim();if(!branch){return failed("lakebase-databases","A Lakebase branch is required to list databases.")}const parent=branch.startsWith("projects/")?branch:"";if(!parent||!parent.includes("/branches/")){return failed("lakebase-databases","A full branch resource name (projects/.../branches/...) is required to list databases.")}const apiPath=`/api/2.0/postgres/${parent}/databases`;const parts=[`page_size=${BROWSE_PAGE_SIZE}`];if(options.pageToken)parts.push(`page_token=${encodeURIComponent(options.pageToken)}`);return listWithGuard("lakebase-databases","/api/2.0/postgres/projects",`${apiPath}?${parts.join("&")}`,options,lakebaseDatabaseItems)}async function validateLakebaseDatabase(databaseInput,options){const database=databaseInput.trim();if(!/^projects\/[^/]+\/branches\/[^/]+\/databases\/[^/]+$/.test(database)){return{ok:false,status:400,detail:"Choose a full Lakebase database name: projects/.../branches/.../databases/...."}}if(!options.host||!options.token){return{ok:false,status:403,detail:"The signed-in user context needed to validate this Lakebase database is unavailable."}}const scopePath="/api/2.0/postgres/projects";const blocked=browseBlockedByScope({apiPath:scopePath,token:options.token,declaredScopes:options.declaredScopes});if(blocked){return{ok:false,status:403,detail:`Your sign-in does not carry ${blocked}, so this Lakebase database cannot be validated.`}}const answer=await workspaceGet(`/api/2.0/postgres/${database}`,{...options,skipCache:true});if(answer.kind!=="http"){return{ok:false,status:503,detail:answer.kind==="timeout"?"Lakebase validation timed out. Nothing was staged.":"The workspace could not be reached to validate this Lakebase database. Nothing was staged."}}if(answer.status>=200&&answer.status<300)return{ok:true};if(answer.status===404||text2(answer.body.error_code)==="RESOURCE_DOES_NOT_EXIST"){return{ok:false,status:404,detail:"That Lakebase database no longer exists. Nothing was staged."}}if(answer.status===401||answer.status===403){return{ok:false,status:403,detail:"Your sign-in may no longer read that Lakebase database. Nothing was staged."}}return{ok:false,status:503,detail:`The workspace refused Lakebase validation with HTTP ${answer.status}. Nothing was staged.`}}function listExperiments(_options={host:"",token:""}){return Promise.resolve(unavailableNoAppsScope("experiments","MLflow"))}function browseRequestContext(input){return{host:normalizeWorkspaceHost(input.host??process.env.DATABRICKS_HOST??""),token:input.token?.trim()??"",principal:input.principal?.trim().toLocaleLowerCase(),signal:input.signal,page:input.page}}async function discoverConnectionTypes(options){const roots=await Promise.all([listCatalogs(options),listWarehouses(options),listGenieSpaces(options),listServingEndpoints(options),listVectorSearchEndpoints(options)]);const catalogs=roots[0];const vectorEndpoints=roots[4];const byKind=new Map(roots.map(response=>[response.kind,response]));const definitions=[{id:"catalog",label:"Catalog",rootKind:"catalogs"},{id:"schema",label:"Schema",rootKind:"catalogs"},{id:"table",label:"Table or view",rootKind:"catalogs"},{id:"volume",label:"Volume",rootKind:"catalogs"},{id:"sql-warehouse",label:"SQL warehouse",rootKind:"warehouses"},{id:"genie-space",label:"Genie space",rootKind:"genie-spaces"},{id:"serving-endpoint",label:"Serving endpoint",rootKind:"serving-endpoints"},{id:"vector-search-endpoint",label:"Vector Search endpoint",rootKind:"vector-search-endpoints"},{id:"vector-search-index",label:"Vector Search index",rootKind:"vector-search-endpoints"}];const available=definitions.filter(definition=>{if(definition.id==="schema"||definition.id==="table"||definition.id==="volume"){return catalogs.status==="ok"&&catalogs.items.length>0}if(definition.id==="vector-search-index"){return vectorEndpoints.status==="ok"&&vectorEndpoints.items.length>0}const response=byKind.get(definition.rootKind);return response?.status==="ok"&&response.items.length>0});const unavailable2=roots.filter(response=>response.status!=="ok"||response.items.length===0).map(response=>({rootKind:response.kind,status:response.status==="unavailable"?"denied":response.status==="failed"?"failed":"empty",detail:response.status==="ok"?"No visible resources were returned.":response.detail}));return{available,unavailable:unavailable2,discovery:{mode:"lazy",complete:false,incomplete_reason:"children_not_enumerated",root_calls:roots.length,concurrency_limit:DISCOVERY_MAX_CONCURRENCY}}}export{discoveryLimiter,DiscoveryPageCache,PROBE_TIMEOUT_MS,SERVING_ENDPOINT_KIND,ANSWER_PATH_ENDPOINT_IDS,probeConnections,listCatalogs,listSchemas,listTables,validateUnityCatalogAsset,searchUnityCatalogAssets,listWarehouses,listGenieSpaces,listServingEndpoints,listNotebooks,validateNotebookPath,listVolumes,listVectorSearchEndpoints,listVectorSearchIndexes,listLakebaseProjects,listLakebaseBranches,listLakebaseDatabases,validateLakebaseDatabase,listExperiments,browseRequestContext,discoverConnectionTypes};
