
import{EMPTY_LAB_STATE,LabStateSchema,PRODUCTION_PROMPT_ALIAS,agreementFromPairs,agreementLine,alignmentRewritePrompt,commitDatasetVersion,distillGuidelinesFromPairs,extractJudgeContent,pairLabelsWithCases,parseAlignedGuidelines,parseLabState,promptRegistryUri}from"./chunk-ABFNFM5T.mjs";import{appTable}from"./chunk-OMC625AH.mjs";var JUDGE_ALIGN_PATHS=["/api/2.0/mlflow/genai/judges/align","/api/2.0/mlflow/judges/align"];async function loadCasesForAlignment(client,runIds){const cases=[];for(const id of runIds){const trimmed=id.trim();if(!trimmed)continue;try{const result=await client.lakebase.query(`SELECT metrics_json FROM ${appTable("benchmark_runs")} WHERE id = $1`,[trimmed]);const raw=result?.rows?.[0]?.metrics_json;const metrics=typeof raw==="string"?JSON.parse(raw):raw;const listed=Array.isArray(metrics?.cases)?metrics.cases:[];cases.push(...listed)}catch{}}return cases}function guidelinesFromAlignBody(body){if(!body||typeof body!=="object")return"";const record=body;const instructions=record.instructions??record.guidelines??record.judge?.instructions??record.judge?.guidelines;return typeof instructions==="string"?instructions.trim():""}async function tryMLflowJudgeAlign(client,input){let lastError=new Error("No judge-align path answered.");for(const path of JUDGE_ALIGN_PATHS){try{const body=await client.request({method:"POST",path,payload:{name:"guidelines",instructions:input.guidelines,experiment_id:input.experimentId.trim()||void 0,assessments:input.pairs.map(pair=>({name:"guidelines",question:pair.question,human:pair.human,judge:pair.judge}))}});const next=guidelinesFromAlignBody(body);if(next)return next}catch(error){lastError=error}}throw lastError instanceof Error?lastError:new Error(String(lastError))}async function rewriteGuidelinesWithJudge(invoke,base,pairs){const raw=await invoke({messages:[{role:"user",content:alignmentRewritePrompt(base,pairs)}],temperature:0,max_tokens:2048});const next=parseAlignedGuidelines(extractJudgeContent(raw)??"");if(!next)throw new Error("The judge model did not return replacement guidelines.");return next.slice(0,4e3)}async function alignGuidelinesToHumans(input){const pairs=pairLabelsWithCases(input.rows,input.cases);const agreement=agreementFromPairs(pairs);if(pairs.length===0){return{guidelinesText:input.base.trim(),agreement,method:"distill",note:"Label at least one row before aligning.",pairs}}if(input.alignClient){try{const next=await tryMLflowJudgeAlign(input.alignClient,{experimentId:input.experimentId??"",guidelines:input.base,pairs});return{guidelinesText:next.slice(0,4e3),agreement,method:"mlflow",note:`MLflow aligned the guidelines judge. ${agreementLine(agreement)}`,pairs}}catch{}}if(input.invokeJudge){try{const next=await rewriteGuidelinesWithJudge(input.invokeJudge,input.base,pairs);return{guidelinesText:next,agreement,method:"rewrite",note:`Guidelines were rewritten to match human verdicts. ${agreementLine(agreement)}`,pairs}}catch(error){const distilled2=distillGuidelinesFromPairs(input.base,input.rows,pairs);return{guidelinesText:distilled2,agreement,method:"distill",note:`The judge model could not rewrite the rubric (${error.message}). Distilled replacement guidelines from the labels instead. ${agreementLine(agreement)}`,pairs}}}const distilled=distillGuidelinesFromPairs(input.base,input.rows,pairs);return{guidelinesText:distilled,agreement,method:"distill",note:`Replacement guidelines distilled from human labels. ${agreementLine(agreement)} MLflow judge.align was not available from this app.`,pairs}}var PROMPT_REGISTER_PATHS=["/api/2.0/mlflow/unity-catalog/prompts","/api/2.0/mlflow/prompts"];function promptTemplateFromPromote(input){const guidelines=input.guidelines.trim();const winner=input.endpoint.trim()||input.side.trim()||"the promoted agent";return[`Operating guidance for the promoted Ask path (${winner}).`,guidelines||"Stay accurate, professional, and within the governed data the question asked about."].join("\n")}function parsePromptVersion(body){if(!body||typeof body!=="object")return"";const record=body;const version=record.version??record.prompt_version??record.prompt?.version;if(version===void 0||version===null)return"";if(typeof version==="string")return version;if(typeof version==="number"||typeof version==="boolean"||typeof version==="bigint"){return String(version)}return typeof version==="object"?JSON.stringify(version):""}function parsePromptTemplate(body){if(!body||typeof body!=="object")return"";const record=body;const template=record.template??record.prompt_template??record.prompt?.template;return typeof template==="string"?template:""}async function tryFirstPath(client,method,paths,payload){let lastError=new Error("No Prompt Registry path answered.");for(const path of paths){try{const body=await client.request({method,path,payload});return{path,body}}catch(error){lastError=error}}throw lastError instanceof Error?lastError:new Error(String(lastError))}async function registerPromptVersion(client,name,template){const created=await tryFirstPath(client,"POST",PROMPT_REGISTER_PATHS,{name,template,commit_message:"Promoted from Player Insights Benchmarking."});return{version:parsePromptVersion(created.body),template:parsePromptTemplate(created.body)||template}}async function setProductionAlias(client,name,version){const encoded=encodeURIComponent(name);const alias=PRODUCTION_PROMPT_ALIAS;const paths=[`/api/2.0/mlflow/unity-catalog/prompts/${encoded}/aliases/${encodeURIComponent(alias)}`,`/api/2.0/mlflow/prompts/${encoded}/aliases/${encodeURIComponent(alias)}`];await tryFirstPath(client,"PATCH",paths,{version})}async function loadProductionPrompt(client,name){const encoded=encodeURIComponent(name);const alias=PRODUCTION_PROMPT_ALIAS;const paths=[`/api/2.0/mlflow/unity-catalog/prompts/${encoded}/aliases/${encodeURIComponent(alias)}`,`/api/2.0/mlflow/prompts/${encoded}/aliases/${encodeURIComponent(alias)}`];const loaded=await tryFirstPath(client,"GET",paths);return{version:parsePromptVersion(loaded.body),template:parsePromptTemplate(loaded.body)}}async function promotePromptAlias(client,input){const name=input.name.trim();const template=input.template.trim();if(!name){return{name:"",alias:PRODUCTION_PROMPT_ALIAS,version:"",uri:"",template,status:"skipped",note:"No Prompt Registry name is set. Next Ask still uses the saved guidance from this promote. Type a catalog.schema.prompt name to move the production alias."}}try{const registered=await registerPromptVersion(client,name,template);const version=registered.version;if(version){await setProductionAlias(client,name,version)}const loaded=version?{version,template:registered.template}:await loadProductionPrompt(client,name);return{name,alias:PRODUCTION_PROMPT_ALIAS,version:loaded.version||version,uri:promptRegistryUri(name),template:loaded.template||registered.template||template,status:"moved",note:`Moved the ${PRODUCTION_PROMPT_ALIAS} alias${loaded.version?` to version ${loaded.version}`:""}. The next Ask loads ${promptRegistryUri(name)} without a code change.`}}catch(error){return{name,alias:PRODUCTION_PROMPT_ALIAS,version:"",uri:promptRegistryUri(name),template,status:"blocked",note:`The production alias was not moved: ${error.message} Apps often cannot write Prompt Registry. The next Ask still uses the guidance saved from this promote.`}}}var KEY="effective";var BENCHMARK_LAB_TABLE=appTable("benchmark_lab");var BENCHMARK_LAB_DDL=`CREATE TABLE IF NOT EXISTS ${BENCHMARK_LAB_TABLE} (
  id TEXT PRIMARY KEY,
  state JSONB NOT NULL,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_by TEXT NOT NULL
)`;var cache=new WeakMap;var BENCHMARK_LAB_TTL_MS=5e3;function forgetLabState(){cache=new WeakMap}async function readLabState(client,options={}){const now=options.now??Date.now();const cached=cache.get(client);if(cached&&now-cached.at<(options.maxAgeMs??BENCHMARK_LAB_TTL_MS))return cached.value;try{const result=await client.lakebase.query(`SELECT state FROM ${BENCHMARK_LAB_TABLE} WHERE id = $1`,[KEY]);const raw=result?.rows?.[0]?.state;const parsed=raw===void 0?EMPTY_LAB_STATE:parseLabState(raw);cache.set(client,{value:parsed,at:now});return parsed}catch(error){console.warn("[benchmark-lab] Falling back to empty lab state:",error.message);return EMPTY_LAB_STATE}}async function writeLabState(client,state,updatedBy){const parsed=LabStateSchema.parse(state);await client.lakebase.query(`INSERT INTO ${BENCHMARK_LAB_TABLE} (id, state, updated_by, updated_at)
     VALUES ($1, $2::jsonb, $3, now())
     ON CONFLICT (id) DO UPDATE SET
       state = EXCLUDED.state, updated_by = EXCLUDED.updated_by, updated_at = now()`,[KEY,JSON.stringify(parsed),updatedBy]);forgetLabState();return parsed}async function patchLabState(client,patch,updatedBy){const current=await readLabState(client,{maxAgeMs:0});return writeLabState(client,{...current,...patch},updatedBy)}async function snapshotWorkingCopy(client,rows,actor,extra={}){const current=await readLabState(client,{maxAgeMs:0});const committed=commitDatasetVersion({state:{...current,...extra},rows,actor});return writeLabState(client,committed.state,actor)}export{loadCasesForAlignment,alignGuidelinesToHumans,promptTemplateFromPromote,promotePromptAlias,readLabState,patchLabState,snapshotWorkingCopy};
