
import{readDeclaredConnections}from"./chunk-ASETBQDH.mjs";import{DATA_GENIE_TABLES,qualifyDataContractTables}from"./chunk-QBE6Z7UM.mjs";import{deploymentOwnerEmail,parseOrganizationMappings,userEmail}from"./chunk-XPNJBQA6.mjs";import"./chunk-Q7N2ZVLF.mjs";import{SP_GRANT_ACTIONS,SP_GRANT_MATRIX,SP_GRANT_RESOURCE_TYPES,SpAssignmentWriteSchema,SpPersonaConnectionWriteSchema,SpPersonaDefinitionPatchSchema,SpPersonaDefinitionWriteSchema,SpPersonaPatchSchema,SpPersonaWriteSchema,deleteSpPersona,deleteSpPersonaDefinition,deleteSpPersonaStatus,describeSpTokenMinting,forgetSpTokens,insertSpPersona,insertSpPersonaDefinition,listSpAssignments,listSpPersonaDefinitions,listSpPersonaStatuses,listSpPersonas,mintPersonaToken,readSpPersonaByDefinitionId,readSpPersonaDefinition,resolveSemanticIndexValue,spGrantKey,spGrantSummary,updateSpPersona,updateSpPersonaDefinition,upsertSpPersonaForDefinition,writeSpAssignment,writeSpPersonaStatus}from"./chunk-2SBMSOUU.mjs";import"./chunk-Q4IWDYLT.mjs";import{invalidAdminEmail,normalizeAdminEmail,readRoster,recordAdminAction}from"./chunk-N2MF6JF6.mjs";import"./chunk-66G4LGYE.mjs";import{accountConsoleUrlForWorkspace,normalizeWorkspaceHost}from"./chunk-3KZTQDF5.mjs";import"./chunk-3LJPB2Y3.mjs";import"./chunk-P7U7VA46.mjs";import"./chunk-W7NKQP7B.mjs";import"./chunk-ZMBYP4AX.mjs";import{external_exports}from"./chunk-DDLERORI.mjs";import"./chunk-OMC625AH.mjs";import"./chunk-A7SHUGSC.mjs";var SP_GRANT_RESOURCE_LIMIT=500;function candidate(type,id,label,source="configured"){const value=id?.trim()??"";return value?{type,id:value,label,source}:null}function unityCatalogType(value){const parts=value.split(".");if(parts.length===1)return"CATALOG";if(parts.length===2)return"SCHEMA";if(parts.length===3)return"TABLE";return null}var DECLARED_GRANT_RESOURCE_TYPES={catalog:"CATALOG",schema:"SCHEMA",table:"TABLE","sql-warehouse":"SQL_WAREHOUSE","serving-endpoint":"SERVING_ENDPOINT","genie-space":"GENIE_SPACE","vector-search-endpoint":"VECTOR_SEARCH_ENDPOINT","vector-search-index":"VECTOR_SEARCH_INDEX",volume:"VOLUME"};function declaredGrantResourceType(input){if(input.resourceType)return DECLARED_GRANT_RESOURCE_TYPES[input.resourceType];switch(input.kind){case"agent":case"model":return"SERVING_ENDPOINT";case"genie-space":return"GENIE_SPACE";case"sql-warehouse":return"SQL_WAREHOUSE";case"unity-catalog":return unityCatalogType(input.value);case"volume":return"VOLUME";case"vector-search":return/endpoint/i.test(`${input.id} ${input.label}`)?"VECTOR_SEARCH_ENDPOINT":"VECTOR_SEARCH_INDEX";default:return null}}async function discoverSpGrantResources(client,env=process.env){const catalog2=env.PLAYER_INSIGHTS_CATALOG??"";const schema2=env.PLAYER_INSIGHTS_SCHEMA??"";const semanticIndex2=resolveSemanticIndexValue(env.PLAYER_INSIGHTS_SEMANTIC_INDEX??"",catalog2,schema2);const configured=[candidate("SERVING_ENDPOINT",env.DATABRICKS_SERVING_ENDPOINT_NAME,"Orchestrator serving endpoint"),candidate("SERVING_ENDPOINT",env.PLAYER_INSIGHTS_LLM_ENDPOINT,"Foundation model endpoint"),candidate("SERVING_ENDPOINT",env.PLAYER_INSIGHTS_JUDGE_ENDPOINT,"Benchmark judge endpoint"),candidate("SQL_WAREHOUSE",env.DATABRICKS_SQL_WAREHOUSE_ID,"SQL warehouse"),candidate("CATALOG",catalog2,"App catalog"),candidate("SCHEMA",catalog2&&schema2?`${catalog2}.${schema2}`:void 0,"App schema"),...qualifyDataContractTables(catalog2,schema2,DATA_GENIE_TABLES).map(id=>{const parts=id.split(".");return candidate("TABLE",id,parts[parts.length-1]??id)}),candidate("GENIE_SPACE",env.PLAYER_INSIGHTS_DATA_GENIE_ID,"Data Genie space"),candidate("GENIE_SPACE",env.PLAYER_INSIGHTS_DICTIONARY_GENIE_ID,"Dictionary Genie space"),candidate("VECTOR_SEARCH_INDEX",semanticIndex2.split(".").length===3?semanticIndex2:void 0,"Vector Search index")];const declared=await readDeclaredConnections(client);const rows=configured.filter(entry=>Boolean(entry));for(const entry of declared){if(entry.state!=="declared")continue;const type=declaredGrantResourceType(entry);if(!type)continue;rows.push({type,id:entry.value.trim(),label:entry.label||entry.value,source:"declared"})}const unique=new Map;for(const row of rows){if(!row.id)continue;const key=`${row.type}\0${row.id.toLocaleLowerCase()}`;if(!unique.has(key))unique.set(key,row)}return[...unique.values()].sort((left,right)=>left.type.localeCompare(right.type)||left.label.localeCompare(right.label))}function boundedSpGrantResources(resources,limit=SP_GRANT_RESOURCE_LIMIT){const bounded=resources.slice(0,Math.max(0,limit));return{resources:bounded,pagination:{complete:bounded.length===resources.length,returned:bounded.length,limit,incompleteReason:bounded.length===resources.length?"":"result_cap"}}}var CHECK_DEADLINE_MS=8e3;var REQUEST_TIMEOUT_MS=2500;var CHECK_CONCURRENCY=4;var UC_SECURABLE={CATALOG:"catalog",SCHEMA:"schema",TABLE:"table",FUNCTION:"function",REGISTERED_MODEL:"registered_model",VOLUME:"volume"};var WORKSPACE_ACL={SERVING_ENDPOINT:"serving-endpoints",SQL_WAREHOUSE:"warehouses"};function defaultStatus(detail){return{connection:{state:"not_connected",checkedAt:null,detail},sync:{state:"not_synced",checkedAt:null,definitionRevision:null,detail:"Run Check status after the service principal is connected.",checks:[]}}}function statusForSpPersonaDefinition(definition,persona,record){if(!persona||persona.definitionId!==definition.id){return defaultStatus("No credential reference is linked to this definition.")}if(!record?.connectionOk){return{...defaultStatus("The stored credential reference has not passed a token-mint check."),connection:{state:"not_connected",checkedAt:record?.checkedAt??null,detail:"The stored credential reference has not passed a token-mint check."},sync:{state:"not_synced",checkedAt:record?.checkedAt??null,definitionRevision:record?.definitionRevision??null,detail:record?.detail||"Fix the connection, then run Check status again.",checks:record?.checks??[]}}}const currentRevision=definition.revision??1;const current=record.definitionRevision===currentRevision;const allVerified=record.checks.length>0&&record.checks.every(check=>check.state==="verified");return{connection:{state:"connected",checkedAt:record.checkedAt,detail:"The stored credential reference minted a service-principal token."},sync:{state:current&&allVerified?"synced":"not_synced",checkedAt:record.checkedAt,definitionRevision:record.definitionRevision,detail:!current?"Permissions changed after the last check. Run Check status again.":allVerified?"Every configured permission was verified under the connected service principal.":record.detail||"One or more configured permissions could not be verified.",checks:record.checks}}}function unsupported(grant){return{key:spGrantKey(grant),label:spGrantSummary(grant),state:"unsupported",nextAction:`Verify ${grant.privilege} in Account Console; this resource has no safe read-only grant probe.`}}function privilegesFromEffectivePermissions(body,principal){if(!body||typeof body!=="object")return null;const assignments=body.privilege_assignments;if(!Array.isArray(assignments))return null;const entries=assignments;const wanted=principal.trim().toLocaleLowerCase();const match=entries.find(entry=>{const named=entry.principal;return typeof named==="string"&&named.trim().toLocaleLowerCase()===wanted});if(!match)return[];const privileges=match.privileges;if(!Array.isArray(privileges))return null;return privileges.flatMap(entry=>{if(typeof entry==="string")return[entry.toUpperCase().replace(/_/g," ")];const named=entry.privilege;return typeof named==="string"?[named.toUpperCase().replace(/_/g," ")]:[]})}function privilegesFromWorkspaceAcl(body,principal){if(!body||typeof body!=="object")return null;const acl=body.access_control_list;if(!Array.isArray(acl))return null;const entries=acl;const wanted=principal.trim().toLocaleLowerCase();const match=entries.find(entry=>{const named=entry.service_principal_name;return typeof named==="string"&&named.trim().toLocaleLowerCase()===wanted});if(!match)return[];const permissions=match.all_permissions;if(!Array.isArray(permissions))return null;return permissions.flatMap(entry=>{const named=entry.permission_level;return typeof named==="string"?[named.toUpperCase().replace(/_/g," ")]:[]})}async function fetchJsonWithin(url,token,fetchImpl,timeoutMs){const controller=new AbortController;const timer=setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetchImpl(url,{headers:{authorization:`Bearer ${token}`,accept:"application/json"},signal:controller.signal});return{ok:response.ok,status:response.status,body:response.ok?await response.json().catch(()=>null):null,timedOut:false}}catch{return{ok:false,status:0,body:null,timedOut:controller.signal.aborted}}finally{clearTimeout(timer)}}async function verifyGrant(input){const{grant,persona}=input;const ucType=UC_SECURABLE[grant.resourceType];const aclType=WORKSPACE_ACL[grant.resourceType];if(!ucType&&!aclType)return unsupported(grant);const path=ucType?`/api/2.1/unity-catalog/effective-permissions/${ucType}/${encodeURIComponent(grant.resource)}?principal=${encodeURIComponent(persona.clientId)}`:`/api/2.0/permissions/${aclType}/${encodeURIComponent(grant.resource)}`;const response=await fetchJsonWithin(`${input.host}${path}`,input.token,input.fetchImpl,Math.max(1,input.timeoutMs));const nextAction=response.timedOut?"Retry Check status; the Databricks permission probe timed out.":response.status===404?"Confirm the resource identifier, then retry Check status.":"Verify this permission in Account Console, then retry Check status.";if(!response.ok){return{key:spGrantKey(grant),label:spGrantSummary(grant),state:"unverified",nextAction}}const privileges=ucType?privilegesFromEffectivePermissions(response.body,persona.clientId):privilegesFromWorkspaceAcl(response.body,persona.clientId);if(privileges===null){return{key:spGrantKey(grant),label:spGrantSummary(grant),state:"unverified",nextAction}}const verified=privileges.includes(grant.privilege.toUpperCase())||privileges.includes("ALL PRIVILEGES");return{key:spGrantKey(grant),label:spGrantSummary(grant),state:verified?"verified":"mismatch",nextAction:verified?"":`Grant ${grant.privilege} on this resource, then retry Check status.`}}async function boundedMap(values,concurrency,work){const output=new Array(values.length);let next=0;await Promise.all(Array.from({length:Math.min(concurrency,values.length)},async()=>{while(next<values.length){const index=next++;output[index]=await work(values[index])}}));return output}async function settleWithin(work,timeoutMs){let timer;try{return await Promise.race([work,new Promise(resolve=>{timer=setTimeout(()=>resolve(null),Math.max(1,timeoutMs))})])}finally{if(timer)clearTimeout(timer)}}async function checkSpPersonaDefinitionStatus(definition,persona,deps={}){const now=deps.now??Date.now;const startedAt=now();const checkedAt=new Date(startedAt).toISOString();const deadlineMs=deps.deadlineMs??CHECK_DEADLINE_MS;const minted=await settleWithin((deps.mint??mintPersonaToken)(persona,{env:deps.env}),Math.min(deadlineMs,5e3));if(!minted?.ok){return{definitionId:definition.id,checkedAt,definitionRevision:definition.revision??1,connectionOk:false,checks:[],detail:minted?"Token mint failed. Confirm the Application ID, secret reference, and app access to that secret.":"Token mint timed out. Confirm the secret reference, then retry Check status."}}const host=normalizeWorkspaceHost((deps.env??process.env).DATABRICKS_HOST);if(!host){return{definitionId:definition.id,checkedAt,definitionRevision:definition.revision??1,connectionOk:true,checks:[],detail:"The connection worked, but this app has no workspace URL for permission checks."}}const fetchImpl=deps.fetchImpl??fetch;const checks=await boundedMap(definition.grants??[],CHECK_CONCURRENCY,async grant=>{const remaining=deadlineMs-(now()-startedAt);if(remaining<=0){return{key:spGrantKey(grant),label:spGrantSummary(grant),state:"unverified",nextAction:"Retry Check status; the overall permission-check deadline elapsed."}}return verifyGrant({grant,persona,token:minted.token,host,fetchImpl,timeoutMs:Math.min(deps.requestTimeoutMs??REQUEST_TIMEOUT_MS,remaining)})});for(const capability of definition.legacyCapabilities??[]){checks.push({key:`legacy:${capability}`,label:capability,state:"unsupported",nextAction:"Convert this legacy permission to a structured permission, then retry Check status."})}const allVerified=checks.length>0&&checks.every(check=>check.state==="verified");return{definitionId:definition.id,checkedAt,definitionRevision:definition.revision??1,connectionOk:true,checks,detail:allVerified?"Every configured permission was verified.":"Connection succeeded, but one or more permissions need action."}}var analysisTables=["gold_player_180d_summary","gold_title_daily_summary","silver_gameplay_activity","silver_player_profiles"];var marketingTables=[...analysisTables,"silver_purchases"];function single(resourceType,action,privilege,label,choiceLabel,optional=false){return{resourceType,action,privilege,optional,selector:{match:"single",labels:[label],choiceLabel}}}function curatedTables(idSuffixes,choiceLabel){return{resourceType:"TABLE",action:"READ",privilege:"SELECT",selector:{match:"all",sources:["configured","declared"],idSuffixes:[...idSuffixes],choiceLabel}}}var warehouse=single("SQL_WAREHOUSE","USE","CAN USE","SQL warehouse","Analysis SQL warehouse");var catalog=single("CATALOG","USE","USE CATALOG","App catalog","Governed analysis catalog");var schema=single("SCHEMA","USE","USE SCHEMA","App schema","Governed analysis schema");var dataGenie=single("GENIE_SPACE","USE","CAN RUN","Data Genie space","Data Genie space");var dictionaryGenie=single("GENIE_SPACE","USE","CAN RUN","Dictionary Genie space","Dictionary Genie space");var serving=single("SERVING_ENDPOINT","USE","CAN QUERY","Orchestrator serving endpoint","Player Insights Agent serving endpoint");var semanticIndex=single("VECTOR_SEARCH_INDEX","READ","SELECT","Vector Search index","Metadata search index",true);var analystGrants=[warehouse,catalog,schema,curatedTables(analysisTables,"Curated performance and player-analysis tables"),dataGenie,serving];var marketingScientistGrants=[warehouse,catalog,schema,curatedTables(marketingTables,"Curated audience, marketing, purchase, and player-profile tables"),dataGenie,dictionaryGenie,serving];var DEFAULT_SP_PERSONA_TEMPLATES=[{id:"business-analyst",displayName:"Business Analyst",roleSummary:"Read-only analyst for governed performance and player investigation.",purpose:"Investigate curated performance and player trends through Player Insights Agent without changing source data or administering platform resources.",duties:["Run governed analytical questions and validate results against curated tables.","Use the Data Genie space for approved exploratory analysis.","Query the Player Insights Agent serving endpoint to obtain governed answers."],dataBoundaries:["Only resources configured or declared by this Player Insights Agent deployment may be selected.","Table access is limited to the exact curated performance and player-analysis tables in the product data contract.","Metadata search is optional, read-only, and does not grant access to table rows."],exclusions:["No MODIFY, WRITE, CREATE, EDIT, MANAGE, or ownership privileges.","No uncurated catalogs, schemas, tables, Genie spaces, or endpoints.","No account, workspace, identity, secret, or permission administration."],keyCapabilities:["Run governed SQL and Data Genie analysis","Read only exact curated performance and player tables","Optionally add read-only metadata search"],variants:[{id:"least-privilege",label:"least-privilege profile",description:"Read-only SQL, curated tables, Data Genie, and Player Insights Agent query access.",leastPrivilege:true,grants:analystGrants},{id:"semantic-discovery",label:"Add permissions template",description:"Adds read-only Vector Search access so Player Insights Agent can find relevant table and column metadata; it does not grant access to table rows.",leastPrivilege:false,grants:[...analystGrants,semanticIndex]}]},{id:"marketing-scientist",displayName:"Marketing Scientist",roleSummary:"Read-only marketing scientist for governed audience, purchase, and player-profile analysis.",purpose:"Analyze curated marketing, addressability, purchase, and player-profile data through Player Insights Agent without changing data or managing platform resources.",duties:["Evaluate audience and campaign questions against approved curated data.","Use Data Genie for analysis and Dictionary Genie for governed metric definitions.","Query the Player Insights Agent serving endpoint for evidence-backed responses."],dataBoundaries:["Only resources configured or declared by this Player Insights Agent deployment may be selected.","Table access is limited to the exact audience, marketing, purchase, and player-profile tables in the product data contract.","Metadata search is optional, read-only, and does not grant access to table rows."],exclusions:["No MODIFY, WRITE, CREATE, EDIT, MANAGE, or ownership privileges.","No raw identity, contact, payment, or uncurated behavioral data.","No account, workspace, identity, secret, or permission administration."],keyCapabilities:["Run governed marketing and audience analysis","Use Data Genie and Dictionary Genie","Read only exact curated audience, purchase, and player-profile tables"],variants:[{id:"least-privilege",label:"least-privilege profile",description:"Read-only SQL, curated audience and marketing tables, both Genie spaces, and Player Insights Agent query access.",leastPrivilege:true,grants:marketingScientistGrants},{id:"semantic-discovery",label:"Add permissions template",description:"Adds read-only Vector Search access so Player Insights Agent can find relevant table and column metadata; it does not grant access to table rows.",leastPrivilege:false,grants:[...marketingScientistGrants,semanticIndex]}]}];var SP_PERSONA_TEMPLATES_ENV="PLAYER_INSIGHTS_PERSONA_TEMPLATES";var TEXT_MAX=280;var LIST_MAX=12;var GRANT_INTENT_MAX=24;var VARIANT_MAX=4;var EXAMPLE_PROFILE_ACTIONS=new Set(["READ","USE","VIEW","EXECUTE"]);var SummaryListSchema=external_exports.array(external_exports.string().trim().min(1).max(TEXT_MAX)).max(LIST_MAX);var SpGrantResourceTypeSchema=external_exports.enum(SP_GRANT_RESOURCE_TYPES);var SpGrantActionSchema=external_exports.enum(SP_GRANT_ACTIONS);var SpPersonaResourceSelectorSchema=external_exports.object({match:external_exports.enum(["single","all"]).default("single"),sources:external_exports.array(external_exports.enum(["configured","declared"])).min(1).max(2).optional(),labels:external_exports.array(external_exports.string().trim().min(1).max(120)).min(1).max(12).optional(),ids:external_exports.array(external_exports.string().trim().min(1).max(255)).min(1).max(24).optional(),idSuffixes:external_exports.array(external_exports.string().trim().min(1).max(255).regex(/^[A-Za-z0-9_][A-Za-z0-9_.-]*$/)).min(1).max(24).optional(),labelSegments:external_exports.array(external_exports.string().trim().min(2).max(80).regex(/^[A-Za-z0-9_-]+$/)).min(1).max(12).optional(),choiceLabel:external_exports.string().trim().min(1).max(120)}).strict().superRefine((selector,context)=>{if(selector.match==="all"&&!selector.labels&&!selector.ids&&!selector.idSuffixes&&!selector.labelSegments){context.addIssue({code:"custom",message:"An all-resources selector must use an exact or bounded resource constraint."})}});var SpPersonaGrantIntentSchema=external_exports.object({resourceType:SpGrantResourceTypeSchema,action:SpGrantActionSchema,privilege:external_exports.string().trim().min(1).max(64),optional:external_exports.boolean().optional(),selector:SpPersonaResourceSelectorSchema}).strict().superRefine((intent,context)=>{const option=SP_GRANT_MATRIX[intent.resourceType].options.find(candidate2=>candidate2.action===intent.action);if(!option){context.addIssue({code:"custom",path:["action"],message:`${intent.action} is not valid for ${SP_GRANT_MATRIX[intent.resourceType].label}.`})}else if(intent.privilege!==option.privilege){context.addIssue({code:"custom",path:["privilege"],message:`${intent.action} maps to ${option.privilege}.`})}});var SpPersonaTemplateVariantSchema=external_exports.object({id:external_exports.string().trim().min(1).max(64).regex(/^[a-z0-9][a-z0-9-]*$/),label:external_exports.string().trim().min(1).max(80),description:external_exports.string().trim().min(1).max(TEXT_MAX),leastPrivilege:external_exports.boolean(),grants:external_exports.array(SpPersonaGrantIntentSchema).min(1).max(GRANT_INTENT_MAX)}).strict().superRefine((variant,context)=>{variant.grants.forEach((grant,index)=>{if(!EXAMPLE_PROFILE_ACTIONS.has(grant.action)){context.addIssue({code:"custom",path:["grants",index,"action"],message:"Example profiles may request only read, use, view, or execute access."})}})});var SpPersonaTemplateSchema=external_exports.object({id:external_exports.string().trim().min(1).max(64).regex(/^[a-z0-9][a-z0-9-]*$/),displayName:external_exports.string().trim().min(1).max(120),roleSummary:external_exports.string().trim().min(1).max(TEXT_MAX),purpose:external_exports.string().trim().min(1).max(TEXT_MAX),duties:SummaryListSchema.min(1),dataBoundaries:SummaryListSchema.min(1),exclusions:SummaryListSchema.min(1),keyCapabilities:SummaryListSchema.min(1).max(6),variants:external_exports.array(SpPersonaTemplateVariantSchema).min(1).max(VARIANT_MAX)}).strict().superRefine((template,context)=>{if(template.variants.filter(variant=>variant.leastPrivilege).length!==1){context.addIssue({code:"custom",path:["variants"],message:"Each profile must have exactly one least-privilege variant."})}if(new Set(template.variants.map(variant=>variant.id)).size!==template.variants.length){context.addIssue({code:"custom",path:["variants"],message:"Variant ids must be unique."})}});var SpPersonaTemplatesSchema=external_exports.array(SpPersonaTemplateSchema).max(12).refine(templates=>new Set(templates.map(template=>template.id)).size===templates.length,{message:"Profile ids must be unique."});var DefaultTemplates=SpPersonaTemplatesSchema.parse(DEFAULT_SP_PERSONA_TEMPLATES);var SpPersonaTemplateOverrideSchema=external_exports.union([SpPersonaTemplatesSchema,external_exports.object({mode:external_exports.enum(["replace","extend"]),templates:SpPersonaTemplatesSchema}).strict()]);function parseSpPersonaTemplates(raw){if(!raw?.trim())return{templates:DefaultTemplates,warning:null};try{const parsed=SpPersonaTemplateOverrideSchema.safeParse(JSON.parse(raw));if(parsed.success){if(Array.isArray(parsed.data))return{templates:parsed.data,warning:null};if(parsed.data.mode==="replace")return{templates:parsed.data.templates,warning:null};const defaultIds=new Set(DefaultTemplates.map(template=>template.id));if(parsed.data.templates.some(template=>defaultIds.has(template.id)))throw new Error("Template id collision.");const extended=SpPersonaTemplatesSchema.safeParse([...DefaultTemplates,...parsed.data.templates]);if(extended.success)return{templates:extended.data,warning:null}}}catch{}return{templates:[],warning:"Example profiles are unavailable because this deployment configured an invalid template contract."}}function configuredSpPersonaTemplates(env=process.env){return parseSpPersonaTemplates(env[SP_PERSONA_TEMPLATES_ENV])}async function adminPayload(appkit,readDeploymentOwner){const templateConfig=configuredSpPersonaTemplates();const[personas,rawDefinitions,statuses,assignments,rosterRead,grantResourceDiscovery,deploymentOwner]=await Promise.all([listSpPersonas(appkit),listSpPersonaDefinitions(appkit),listSpPersonaStatuses(appkit),listSpAssignments(appkit),readRoster(appkit.lakebase).catch(()=>({rows:[]})),discoverSpGrantResources(appkit).then(resources=>({status:"ready",...boundedSpGrantResources(resources),detail:""})).catch(error=>({status:"error",resources:[],detail:`Configured resources could not be read: ${error.message}`})),readDeploymentOwner().then(normalizeAdminEmail).catch(()=>"")]);const personaByDefinition=new Map(personas.flatMap(persona=>persona.definitionId?[[persona.definitionId,persona]]:[]));const statusByDefinition=new Map(statuses.map(status=>[status.definitionId,status]));const personaDefinitions=rawDefinitions.map(definition=>({...definition,status:statusForSpPersonaDefinition(definition,personaByDefinition.get(definition.id)??null,statusByDefinition.get(definition.id)??null)}));const assignedByEmail=new Map(assignments.map(row=>[row.email,row.personaId]));const roster=rosterRead.rows.map(row=>({email:row.email,role:row.role,isDeploymentOwner:Boolean(deploymentOwner)&&row.email===deploymentOwner,personaId:row.email===deploymentOwner?null:assignedByEmail.get(row.email)??null}));for(const assignment of assignments){if(assignment.email===deploymentOwner)continue;if(roster.some(row=>row.email===assignment.email))continue;roster.push({email:assignment.email,role:"",isDeploymentOwner:false,personaId:assignment.personaId})}roster.sort((left,right)=>left.email.localeCompare(right.email));return{minting:describeSpTokenMinting(),personas,personaDefinitions,personaTemplates:templateConfig.templates,personaTemplateWarning:templateConfig.warning,grantResourceDiscovery,accountConsoleUrl:accountConsoleUrlForWorkspace(process.env.DATABRICKS_HOST),organizations:parseOrganizationMappings(process.env.PLAYER_INSIGHTS_ORGANIZATIONS),assignments,roster}}function setupSpIdentityRoutes(appkit,deps={}){const readDeploymentOwner=deps.readDeploymentOwner??(()=>deploymentOwnerEmail(appkit.lakebase));appkit.server.extend(app=>{app.get("/api/admin/sp-identity",async(_req,res)=>{try{res.json(await adminPayload(appkit,readDeploymentOwner))}catch(error){res.status(503).json({error:"sp_identity_unreadable",detail:`Service-principal personas could not be read: ${error.message}`})}});app.post("/api/admin/sp-identity/personas",async(req,res)=>{const parsed=SpPersonaWriteSchema.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_sp_persona",detail:parsed.error.message});return}const actor=userEmail(req);try{const persona=await insertSpPersona(appkit,parsed.data,actor);await recordAdminAction(appkit.lakebase,{actor,action:"sp-persona-created",subject:persona.id,detail:`Created service-principal persona ${persona.displayName}.`});res.status(201).json(persona)}catch(error){res.status(503).json({error:"sp_identity_store_unavailable",detail:`The persona was not saved: ${error.message}`})}});app.post("/api/admin/sp-identity/persona-definitions",async(req,res)=>{const parsed=SpPersonaDefinitionWriteSchema.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_sp_persona_definition",detail:parsed.error.message});return}const actor=userEmail(req);try{const definition=await insertSpPersonaDefinition(appkit,parsed.data,actor);await recordAdminAction(appkit.lakebase,{actor,action:"sp-persona-definition-created",subject:definition.id,detail:`Generated credential-free service-principal configuration ${definition.displayName}.`});res.status(201).json(definition)}catch(error){res.status(503).json({error:"sp_identity_store_unavailable",detail:`The persona configuration was not saved: ${error.message}`})}});app.patch("/api/admin/sp-identity/persona-definitions/:id",async(req,res)=>{const parsed=SpPersonaDefinitionPatchSchema.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_sp_persona_definition",detail:parsed.error.message});return}const id=external_exports.string().trim().min(1).max(80).safeParse(req.params.id);if(!id.success){res.status(400).json({error:"invalid_sp_persona_definition",detail:"Missing persona id."});return}const actor=userEmail(req);try{const definition=await updateSpPersonaDefinition(appkit,id.data,parsed.data,actor);if(!definition){res.status(404).json({error:"sp_persona_definition_missing",detail:"That persona is not defined."});return}await recordAdminAction(appkit.lakebase,{actor,action:"sp-persona-definition-updated",subject:definition.id,detail:`Updated service-principal configuration ${definition.displayName}.`});res.json(definition)}catch(error){res.status(503).json({error:"sp_identity_store_unavailable",detail:`The persona configuration was not saved: ${error.message}`})}});app.put("/api/admin/sp-identity/persona-definitions/:id/connection",async(req,res)=>{const parsed=SpPersonaConnectionWriteSchema.safeParse(req.body);const id=external_exports.string().trim().min(1).max(80).safeParse(req.params.id);if(!parsed.success||!id.success){res.status(400).json({error:"invalid_sp_persona_connection",detail:"Enter an Application ID, secret scope, and secret key reference."});return}const actor=userEmail(req);try{const definition=await readSpPersonaDefinition(appkit,id.data);if(!definition){res.status(404).json({error:"sp_persona_definition_missing",detail:"That persona is not defined."});return}const persona=await upsertSpPersonaForDefinition(appkit,definition,parsed.data,actor);forgetSpTokens();await recordAdminAction(appkit.lakebase,{actor,action:"sp-persona-connection-updated",subject:definition.id,detail:`Updated the credential reference for ${definition.displayName}; connection and sync require a new check.`});res.json({persona,payload:await adminPayload(appkit,readDeploymentOwner)})}catch{res.status(503).json({error:"sp_identity_store_unavailable",detail:"The service-principal connection reference was not saved. No secret value was accepted."})}});app.post("/api/admin/sp-identity/persona-definitions/:id/status-check",async(req,res)=>{const id=external_exports.string().trim().min(1).max(80).safeParse(req.params.id);if(!id.success){res.status(400).json({error:"invalid_sp_persona_status_check",detail:"Missing persona id."});return}const actor=userEmail(req);try{const[definition,persona]=await Promise.all([readSpPersonaDefinition(appkit,id.data),readSpPersonaByDefinitionId(appkit,id.data)]);if(!definition){res.status(404).json({error:"sp_persona_definition_missing",detail:"That persona is not defined."});return}if(!persona){res.status(409).json({error:"sp_persona_not_connected",detail:"Connect a service principal to this definition before checking status."});return}const status=await checkSpPersonaDefinitionStatus(definition,persona);await writeSpPersonaStatus(appkit,status);await recordAdminAction(appkit.lakebase,{actor,action:"sp-persona-status-checked",subject:definition.id,detail:status.connectionOk?`Checked the connection and ${status.checks.length} configured permissions for ${definition.displayName}.`:`Checked the connection for ${definition.displayName}; token minting did not succeed.`});res.json({payload:await adminPayload(appkit,readDeploymentOwner)})}catch{res.status(503).json({error:"sp_persona_status_unavailable",detail:"The bounded connection and permission check could not be completed. Retry Check status."})}});app.delete("/api/admin/sp-identity/persona-definitions/:id",async(req,res)=>{const id=external_exports.string().trim().min(1).max(80).safeParse(req.params.id);if(!id.success){res.status(400).json({error:"invalid_sp_persona_definition",detail:"Missing persona id."});return}const actor=userEmail(req);try{const removed=await deleteSpPersonaDefinition(appkit,id.data);if(!removed){res.status(404).json({error:"sp_persona_definition_missing",detail:"That persona is not defined."});return}await recordAdminAction(appkit.lakebase,{actor,action:"sp-persona-definition-removed",subject:id.data,detail:"Removed a credential-free service-principal configuration."});res.status(204).end()}catch(error){res.status(503).json({error:"sp_identity_store_unavailable",detail:`The persona configuration was not removed: ${error.message}`})}});app.patch("/api/admin/sp-identity/personas/:id",async(req,res)=>{const parsed=SpPersonaPatchSchema.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_sp_persona",detail:parsed.error.message});return}const id=external_exports.string().trim().min(1).max(80).safeParse(req.params.id);if(!id.success){res.status(400).json({error:"invalid_sp_persona",detail:"Missing persona id."});return}const actor=userEmail(req);try{const persona=await updateSpPersona(appkit,id.data,parsed.data,actor);if(!persona){res.status(404).json({error:"sp_persona_missing",detail:"That persona is not defined."});return}if(persona.definitionId&&(parsed.data.clientId!==void 0||parsed.data.secretScope!==void 0||parsed.data.secretKey!==void 0)){await deleteSpPersonaStatus(appkit,persona.definitionId);forgetSpTokens()}await recordAdminAction(appkit.lakebase,{actor,action:"sp-persona-updated",subject:persona.id,detail:`Updated service-principal persona ${persona.displayName}.`});res.json(persona)}catch(error){res.status(503).json({error:"sp_identity_store_unavailable",detail:`The persona was not saved: ${error.message}`})}});app.delete("/api/admin/sp-identity/personas/:id",async(req,res)=>{const id=external_exports.string().trim().min(1).max(80).safeParse(req.params.id);if(!id.success){res.status(400).json({error:"invalid_sp_persona",detail:"Missing persona id."});return}const actor=userEmail(req);try{const removed=await deleteSpPersona(appkit,id.data);if(!removed){res.status(404).json({error:"sp_persona_missing",detail:"That persona is not defined."});return}await recordAdminAction(appkit.lakebase,{actor,action:"sp-persona-removed",subject:id.data,detail:"Removed a service-principal persona and its assignments."});res.status(204).end()}catch(error){res.status(503).json({error:"sp_identity_store_unavailable",detail:`The persona was not removed: ${error.message}`})}});app.put("/api/admin/sp-identity/assignments",async(req,res)=>{const parsed=SpAssignmentWriteSchema.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_sp_assignment",detail:parsed.error.message});return}const emailFault=invalidAdminEmail(parsed.data.email);if(emailFault){res.status(400).json({error:"invalid_sp_assignment",detail:emailFault});return}const actor=userEmail(req);try{const email=normalizeAdminEmail(parsed.data.email);const deploymentOwner=await readDeploymentOwner().then(normalizeAdminEmail).catch(()=>"");if(deploymentOwner&&email===deploymentOwner){res.status(409).json({error:"immutable_deployment_owner_persona",detail:"The first app deployer keeps the read-only Owner marker."});return}const assignment=await writeSpAssignment(appkit,parsed.data.email,parsed.data.personaId,actor);if(parsed.data.personaId&&!assignment){res.status(404).json({error:"sp_persona_missing",detail:"That persona is not defined."});return}await recordAdminAction(appkit.lakebase,{actor,action:parsed.data.personaId?"sp-persona-assigned":"sp-persona-unassigned",subject:parsed.data.email,detail:parsed.data.personaId?"Assigned a service-principal persona to this person.":"This person again runs as themselves over OAuth."});res.json({assignment,payload:await adminPayload(appkit,readDeploymentOwner)})}catch(error){res.status(503).json({error:"sp_identity_store_unavailable",detail:`The assignment was not saved: ${error.message}`})}})})}export{setupSpIdentityRoutes};
