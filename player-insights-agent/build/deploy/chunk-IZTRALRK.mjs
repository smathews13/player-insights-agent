
import{qualifyDataContractTables}from"./chunk-XRAITHOQ.mjs";var PAYLOAD_TABLE_SIGNATURE=new Set(["databricks_request_id","request","response","served_entity_id"]);var UNDECLARABLE_SCHEMAS=new Set(["information_schema"]);function isInferencePayloadTable(columns){if(columns===null)return null;const names=new Set(columns);for(const column of PAYLOAD_TABLE_SIGNATURE){if(!names.has(column))return false}return true}function globToRegExp(pattern){const escaped=pattern.replace(/[.+^${}()|[\]\\]/g,"\\$&").replace(/\*/g,".*").replace(/\?/g,".");return new RegExp(`^${escaped}$`,"i")}function denylistMatch(fullName,shortName,patterns){for(const raw of patterns){const pattern=raw.trim();if(!pattern)continue;const match=globToRegExp(pattern);if(match.test(fullName)||match.test(shortName))return pattern}return null}function exclusionReason(table,denylist=[]){if(UNDECLARABLE_SCHEMAS.has(table.schemaName)){return`schema ${table.schemaName} is not declarable`}const pattern=denylistMatch(table.fullName,table.shortName,denylist);if(pattern)return`catalog_denylist pattern ${pattern}`;if(isInferencePayloadTable(table.columns)===true){return"inference payload table"}return null}function listedTableFromBody(row){if(!row||typeof row!=="object")return null;const record=row;const fullName=typeof record.full_name==="string"?record.full_name.trim():"";const name=typeof record.name==="string"?record.name.trim():"";const resolved=fullName||(name.includes(".")?name:"");if(!resolved||resolved.split(".").length!==3)return null;const parts=resolved.split(".");const columns=Array.isArray(record.columns)?record.columns.map(column=>{if(!column||typeof column!=="object")return"";const value=column.name;return typeof value==="string"?value.trim():""}).filter(Boolean):null;return{fullName:resolved,schemaName:parts[1]??"",shortName:parts[2]??name,columns}}function tablesFromListing(rows,denylist=[]){const names=[];for(const table of rows){if(exclusionReason(table,denylist))continue;if(!names.includes(table.fullName))names.push(table.fullName)}return names.sort()}function isDataContractFallback(tables,catalog,schema){const contract=qualifyDataContractTables(catalog,schema);if(contract.length===0)return false;if(tables.length!==contract.length)return false;const listed=[...tables].map(table=>table.trim()).filter(Boolean).sort();return listed.every((table,index)=>table===contract[index])}function unionTableNames(...lists){return[...new Set(lists.flat().map(table=>table.trim()).filter(Boolean))].sort()}var TABLES_PATH="/api/2.1/unity-catalog/tables";async function listDeclarableTablesInSchema(input){const catalog=input.catalog.trim();const schema=input.schema.trim();if(!catalog||!schema||!input.host||!input.token)return[];if(UNDECLARABLE_SCHEMAS.has(schema))return[];const call=input.fetchImpl??fetch;const timeoutMs=input.timeoutMs??15e3;const now=input.now??Date.now;const deadline=now()+timeoutMs;const found=[];let pageToken="";for(let pages=0;pages<20;pages+=1){const remainingMs=deadline-now();if(remainingMs<=0)return tablesFromListing(found,input.denylist);const query=[`catalog_name=${encodeURIComponent(catalog)}`,`schema_name=${encodeURIComponent(schema)}`,"omit_columns=false","max_results=100",pageToken?`page_token=${encodeURIComponent(pageToken)}`:""].filter(Boolean).join("&");try{const response=await call(`${input.host}${TABLES_PATH}?${query}`,{method:"GET",headers:{authorization:`Bearer ${input.token}`},signal:AbortSignal.timeout(Math.max(1,remainingMs))});if(!response.ok)return tablesFromListing(found,input.denylist);const body=await response.json().catch(()=>({}));const rows=Array.isArray(body.tables)?body.tables:[];for(const row of rows){const table=listedTableFromBody(row);if(table)found.push(table)}pageToken=typeof body.next_page_token==="string"?body.next_page_token.trim():"";if(!pageToken)break}catch{return tablesFromListing(found,input.denylist)}}return tablesFromListing(found,input.denylist)}export{isDataContractFallback,unionTableNames,listDeclarableTablesInSchema};
