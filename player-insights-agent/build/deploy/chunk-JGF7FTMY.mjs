
import{ExpiringLruCache}from"./chunk-66G4LGYE.mjs";import{normalizeWorkspaceHost}from"./chunk-3KZTQDF5.mjs";import{sqlQueryTags}from"./chunk-W7NKQP7B.mjs";import{APP_SCHEMA}from"./chunk-OMC625AH.mjs";import crypto from"node:crypto";var ADDED_ADMINS_TABLE=`${APP_SCHEMA}.admin_emails`;var ADMIN_AUDIT_TABLE=`${APP_SCHEMA}.admin_audit`;var ADMIN_GRANTS_TABLE=`${APP_SCHEMA}.admin_access_grants`;var ADMIN_ROLES_DDL=[`CREATE TABLE IF NOT EXISTS ${ADDED_ADMINS_TABLE} (
     email TEXT PRIMARY KEY,
     added_by TEXT NOT NULL,
     added_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   )`,`CREATE TABLE IF NOT EXISTS ${ADMIN_AUDIT_TABLE} (
     id TEXT PRIMARY KEY,
     actor TEXT NOT NULL,
     action TEXT NOT NULL,
     subject TEXT NOT NULL,
     detail TEXT NOT NULL,
     recorded_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   )`,`CREATE INDEX IF NOT EXISTS admin_audit_actor_time
     ON ${APP_SCHEMA}.admin_audit (actor, recorded_at DESC)`,`CREATE TABLE IF NOT EXISTS ${ADMIN_GRANTS_TABLE} (
     email TEXT NOT NULL,
     target TEXT NOT NULL,
     object TEXT NOT NULL,
     privilege TEXT NOT NULL,
     provenance TEXT NOT NULL,
     actor TEXT NOT NULL,
     recorded_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
     PRIMARY KEY (email, object, privilege)
   )`];function normalizeAdminEmail(raw){return raw.trim().toLowerCase()}function columnText(value){if(typeof value==="string")return value;if(typeof value==="number"||typeof value==="boolean"||typeof value==="bigint")return String(value);return""}var ROLES=["super_admin","admin","consumer"];var ROLE_WORD={super_admin:"Super admin",admin:"Admin",consumer:"Consumer"};var ROLE_RANK={consumer:0,admin:1,super_admin:2};function isRole(value){return typeof value==="string"&&ROLES.includes(value)}function highestRole(left,right){return ROLE_RANK[left]>=ROLE_RANK[right]?left:right}function opensAdminSurfaces(role){return ROLE_RANK[role]>=ROLE_RANK.admin}function opensUserRoster(role){return role==="super_admin"}function canMutateConnections(role){return role==="admin"||role==="owner"||role==="super_admin"}function canCheckHealthResources(role){return canMutateConnections(role)}var ROLE_COLUMN="role";var ADD_ROLE_COLUMN_STATEMENT=`ALTER TABLE ${ADDED_ADMINS_TABLE} ADD COLUMN IF NOT EXISTS ${ROLE_COLUMN} TEXT NOT NULL DEFAULT 'admin'`;var UNDEFINED_COLUMN="42703";function missingRoleColumn(error){const code=error.code;if(code===UNDEFINED_COLUMN)return true;const message=error?.message??"";return message.includes(ROLE_COLUMN)&&/does not exist|undefined column/i.test(message)}var REQUEST_ROSTER=Symbol("request-roster");var rosterGeneration=new WeakMap;function generationFor(store){return rosterGeneration.get(store)??0}function invalidateRosterCache(store){rosterGeneration.set(store,generationFor(store)+1)}async function readRoster(store){try{const withRole=await store.query(`SELECT email, ${ROLE_COLUMN}, added_by, added_at FROM ${ADDED_ADMINS_TABLE} ORDER BY added_at ASC`);return{rows:withRole.rows.map(row=>storedRole(row,columnText(row[ROLE_COLUMN]))),roleColumnPresent:true}}catch(error){if(!missingRoleColumn(error))throw error;const withoutRole=await store.query(`SELECT email, added_by, added_at FROM ${ADDED_ADMINS_TABLE} ORDER BY added_at ASC`);return{rows:withoutRole.rows.map(row=>storedRole(row,"admin")),roleColumnPresent:false}}}function readRosterForRequest(store,req){const request=req;const generation=generationFor(store);const cached2=request[REQUEST_ROSTER];if(cached2&&cached2.store===store&&cached2.generation===generation)return cached2.reading;const reading=readRoster(store);request[REQUEST_ROSTER]={store,generation,reading};return reading}function storedRole(row,rawRole){const candidate=rawRole.trim().toLowerCase();return{email:normalizeAdminEmail(columnText(row.email)),role:isRole(candidate)?candidate:"admin",setBy:columnText(row.added_by),setAt:row.added_at instanceof Date?row.added_at.toISOString():columnText(row.added_at)}}async function writeRole(store,input){const email=normalizeAdminEmail(input.email);const actor=normalizeAdminEmail(input.actor);if(!input.roleColumnPresent){await store.query(`INSERT INTO ${ADDED_ADMINS_TABLE} (email, added_by) VALUES ($1, $2)
       ON CONFLICT (email) DO UPDATE SET added_by = EXCLUDED.added_by, added_at = NOW()`,[email,actor]);invalidateRosterCache(store);return}await store.query(`INSERT INTO ${ADDED_ADMINS_TABLE} (email, ${ROLE_COLUMN}, added_by) VALUES ($1, $2, $3)
     ON CONFLICT (email) DO UPDATE
       SET ${ROLE_COLUMN} = EXCLUDED.${ROLE_COLUMN},
           added_by = EXCLUDED.added_by,
           added_at = NOW()`,[email,input.role,actor]);invalidateRosterCache(store)}async function deleteRosterRow(store,email){const result=await store.query(`DELETE FROM ${ADDED_ADMINS_TABLE} WHERE email = $1 RETURNING email`,[normalizeAdminEmail(email)]);const deleted=result.rows.length>0;if(deleted)invalidateRosterCache(store);return deleted}function seedFloorFor(seed,email){const candidate=normalizeAdminEmail(email);if(!candidate)return"consumer";if(seed.superAdmins.includes(candidate))return"super_admin";if(seed.admins.includes(candidate))return"admin";return"consumer"}function effectiveRole(input){const candidate=normalizeAdminEmail(input.email);const floor=seedFloorFor(input.seed,candidate);const row=input.stored.find(entry=>entry.email===candidate);return row?highestRole(floor,row.role):floor}function everyKnownUser(input){const seen=new Set;const out=[];for(const email of[...input.seed.superAdmins,...input.seed.admins]){if(seen.has(email))continue;seen.add(email);out.push({email,role:effectiveRole({...input,email})})}for(const row of input.stored){if(seen.has(row.email))continue;seen.add(row.email);out.push({email:row.email,role:effectiveRole({...input,email:row.email})})}return out}function countSuperAdmins(input){return everyKnownUser(input).filter(user=>user.role==="super_admin").length}function recoveryStatement(){return`INSERT INTO ${ADDED_ADMINS_TABLE} (email, ${ROLE_COLUMN}, added_by) VALUES ('<address>', 'super_admin', '<who ran this>') ON CONFLICT (email) DO UPDATE SET ${ROLE_COLUMN} = 'super_admin'`}function roleChangeRefusal(input){if(!isRole(input.role))return"unknown-role";const target=normalizeAdminEmail(input.email);const desired=input.role;const current=effectiveRole({seed:input.seed,stored:input.stored,email:target});const createsConsumer=input.allowMissingConsumer===true&&desired==="consumer"&&seedFloorFor(input.seed,target)==="consumer"&&!input.stored.some(entry=>entry.email===target);if(desired===current&&!createsConsumer)return"already-holds";if(current==="super_admin"&&seedFloorFor(input.seed,target)==="super_admin"){return"immutable-super-admin"}if(ROLE_RANK[desired]<ROLE_RANK[seedFloorFor(input.seed,target)])return"seed-floor";if(!input.roleColumnPresent&&desired!=="admin")return"no-role-column";if(leavesNoSuperAdmin({...input,target,desired}))return"last-super-admin";return""}function removalRefusal(input){const target=normalizeAdminEmail(input.email);if(!input.stored.some(entry=>entry.email===target))return"not-found";if(effectiveRole({seed:input.seed,stored:input.stored,email:target})==="super_admin"&&seedFloorFor(input.seed,target)==="super_admin"){return"immutable-super-admin"}if(seedFloorFor(input.seed,target)!=="consumer")return"seed-floor";if(leavesNoSuperAdmin({...input,target,desired:"consumer"}))return"last-super-admin";return""}function leavesNoSuperAdmin(input){const after=input.stored.filter(entry=>entry.email!==input.target).concat(input.desired==="consumer"?[]:[{email:input.target,role:input.desired,setBy:"",setAt:""}]);return countSuperAdmins({seed:input.seed,stored:after})===0}var REFUSAL_DETAIL={"immutable-super-admin":"That Super admin role is set in this deployment's configuration and cannot be changed or removed here.","seed-floor":"That role is set in this deployment's configuration and cannot be lowered here. It can be raised.","last-super-admin":"That is the only super admin. Appoint another one first.","not-found":"That address is not on the roster.","already-holds":"That address already holds that role.","no-role-column":`This deployment's roster cannot record that role yet. Run: ${ADD_ROLE_COLUMN_STATEMENT}`,"unknown-role":`Send one of ${ROLES.join(", ")}.`};function rosterPayload(input){const you=normalizeAdminEmail(input.reader);const deploymentOwner=normalizeAdminEmail(input.deploymentOwner??"");const superAdminCount=countSuperAdmins({seed:input.seed,stored:input.stored});const adminCount=everyKnownUser({seed:input.seed,stored:input.stored}).filter(user=>user.role!=="consumer").length;const entries=everyKnownUser({seed:input.seed,stored:input.stored}).map(user=>{const row=input.stored.find(entry=>entry.email===user.email);const floor=seedFloorFor(input.seed,user.email);return{email:user.email,isDeploymentOwner:Boolean(deploymentOwner)&&user.email===deploymentOwner,role:user.role,seedFloor:floor,setBy:row?.setBy??"",setAt:row?.setAt??"",isYou:user.email===you,assignable:ROLES.filter(candidate=>!roleChangeRefusal({email:user.email,role:candidate,seed:input.seed,stored:input.stored,roleColumnPresent:input.roleColumnPresent})),canRemove:!removalRefusal({email:user.email,seed:input.seed,stored:input.stored})}}).sort((left,right)=>ROLE_RANK[right.role]-ROLE_RANK[left.role]||left.email.localeCompare(right.email));return{entries,storedRosterReadable:input.storedRosterReadable,roleColumnPresent:input.roleColumnPresent,pendingSchemaStatement:input.roleColumnPresent?"":ADD_ROLE_COLUMN_STATEMENT,superAdminCount,recoveryStatement:superAdminCount===0&&adminCount===0?recoveryStatement():""}}function roleChangeSentence(input){return`${input.actor} changed ${input.email} from ${ROLE_WORD[input.from].toLowerCase()} to ${ROLE_WORD[input.to].toLowerCase()} in this deployment.`}var NO_EXPORTER_READING={state:"unmeasured",tables:[],error:"",schema:""};var NO_APP_SERVING={app:"",compute:"",message:""};var NO_APP_FACTS={url:"",answered:false,description:"",compute:null,tags:[],deployedAt:"",deployedBy:"",source:{path:"",workspaceUrl:"",gitRef:""},serving:NO_APP_SERVING,otelExporter:"",otelExport:NO_EXPORTER_READING};var DATA_GENIE_TABLES=["gold_player_180d_summary","gold_title_daily_summary","silver_gameplay_activity","silver_player_profiles","silver_purchases"];var DICTIONARY_GENIE_TABLES=["data_dictionary"];var DATA_CONTRACT_TABLES=[...DATA_GENIE_TABLES,...DICTIONARY_GENIE_TABLES];function qualifyDataContractTables(catalog,schema,tables=DATA_CONTRACT_TABLES){const nsCatalog=catalog.trim();const nsSchema=schema.trim();if(!nsCatalog||!nsSchema)return[];return[...new Set(tables.map(table=>table.split(".").length===3?table:`${nsCatalog}.${nsSchema}.${table}`))].sort()}var TOKEN_LAYER="the forwarded user token";function claims(token){const segments=token.split(".");if(segments.length!==3)return null;try{const payload=segments[1].replace(/-/g,"+").replace(/_/g,"/");return JSON.parse(Buffer.from(payload,"base64").toString("utf8"))}catch{return null}}function presentedTokenAge(token,now=new Date){if(!token)return{kind:"unreadable",why:"this request forwarded no user token"};const payload=claims(token);if(!payload){return{kind:"unreadable",why:"the token is not in a form whose claims can be read"}}const exp=payload.exp;if(typeof exp!=="number"||!Number.isFinite(exp)){return{kind:"unreadable",why:"the token states no expiry"}}const expiresAt=new Date(exp*1e3).toISOString();const seconds=Math.round((exp*1e3-now.getTime())/1e3);return seconds<=0?{kind:"expired",expiresAt,secondsAgo:Math.abs(seconds)}:{kind:"live",expiresAt,secondsLeft:seconds}}function readableSpan(seconds){if(seconds<90)return`${seconds} second${seconds===1?"":"s"}`;const minutes=Math.round(seconds/60);if(minutes<90)return`${minutes} minute${minutes===1?"":"s"}`;const hours=Math.round(minutes/60);return`${hours} hour${hours===1?"":"s"}`}var REFUSED="Databricks refused the sign-in this app forwarded for you (HTTP 401) before it looked at any permission, so nothing was run and nothing about your own access was established. This is not a permission you are missing, and no grant made to you would change it.";var NEW_SIGN_IN={kind:"ui",statement:"Open this app again in a private browsing window, and sign in there.\nChrome and Edge call it Incognito or InPrivate. Safari and Firefox call it a Private Window."};var SESSION_IS_NOT_THE_WORKSPACE="Signing out of Databricks does not clear this app’s sign-in: it is held by the proxy in front of the app, not by the workspace.";function tokenRejection(input){const said=input.apiMessage?.trim()?` Databricks said: ${input.apiMessage.trim()}`:" Databricks gave no reason with it.";if(input.age.kind==="expired"){const ago=readableSpan(input.age.secondsAgo);return{layer:TOKEN_LAYER,cause:"forwarded-token-expired",evidence:`The presented token states that it expired at \`${input.age.expiresAt}\`, which is ${ago} before this check ran, and Databricks answered HTTP 401.${said}`,explanation:`${REFUSED} The sign-in it forwarded had run out ${ago} ago, which is the ordinary way for this to happen and the one version of it that clears itself.`,remedy:{kind:"ui",statement:"Reload this page.",guidance:""}}}if(input.age.kind==="live"){const left=readableSpan(input.age.secondsLeft);return{layer:TOKEN_LAYER,cause:"forwarded-token-refused-while-live",evidence:`The presented token states that it does not expire until \`${input.age.expiresAt}\`, which is ${left} after this check ran, so it had not run out when Databricks answered HTTP 401.${said}`,explanation:`${REFUSED} The sign-in it forwarded had not run out: it is good for another ${left}. Reloading this page hands over the same one and gets the same answer, so that is not the thing to try.`,remedy:{kind:NEW_SIGN_IN.kind,statement:NEW_SIGN_IN.statement,guidance:SESSION_IS_NOT_THE_WORKSPACE}}}return{layer:TOKEN_LAYER,cause:"forwarded-token-refused",evidence:`Databricks answered HTTP 401.${said} The presented token’s own expiry could not be read here (${input.age.why}), so whether it had run out was not established either way.`,explanation:`${REFUSED} Whether the sign-in had simply run out cannot be read from here, so this does not guess. The step below is the one that works whether it had or not.`,remedy:{kind:NEW_SIGN_IN.kind,statement:NEW_SIGN_IN.statement,guidance:SESSION_IS_NOT_THE_WORKSPACE}}}var AGE_NOT_SUPPLIED={kind:"unreadable",why:"this check was not given the token to read"};var USER_TOKEN_HEADER="x-forwarded-access-token";function forwardedUserToken(req){const raw=req.header(USER_TOKEN_HEADER);return typeof raw==="string"&&raw.trim()?raw.trim():null}function diagnoseUserToken(req,isDevelopmentIdentity){if(forwardedUserToken(req))return{kind:"present"};if(isDevelopmentIdentity){return{kind:"absent-local",summary:"This session is not running behind Databricks Apps, so there is no signed-in user and nothing to forward a token for. Nothing is wrong with the deployment.",layer:"local development"}}return{kind:"absent-in-apps",summary:"Databricks Apps did not forward a user token with this request. The app is not acting on the user’s behalf at all. This is a platform-side state, not a failure of any check. Either user authorization is not enabled for this workspace, or the app has not been stopped and started since `user_api_scopes` last changed. A redeploy alone does not apply a scope change.",layer:"app configuration"}}function looksLikeMissingScope(message){const text=message.toLowerCase();return text.includes("does not have required scopes")||text.includes("insufficient_scope")||text.includes("scope")&&(text.includes("oauth")||text.includes("token"))}var GENIE_SCOPE="dashboards.genie";var ALL_APIS_SCOPE="all-apis";function scopesFromToken(token){const segments=token.split(".");if(segments.length!==3)return null;let claims2;try{const payload=segments[1].replace(/-/g,"+").replace(/_/g,"/");claims2=JSON.parse(Buffer.from(payload,"base64").toString("utf8"))}catch{return null}const raw=claims2?.scope??claims2?.scopes;if(typeof raw==="string"){const scopes=raw.split(/\s+/).filter(Boolean);return scopes.length?scopes:null}if(Array.isArray(raw)){const scopes=raw.filter(entry=>typeof entry==="string"&&entry.length>0);return scopes.length?scopes:null}return null}function tokenGrantsGenie(token){const scopes=scopesFromToken(token);if(!scopes)return null;return scopes.includes(GENIE_SCOPE)||scopes.includes(ALL_APIS_SCOPE)}function quote(part){return"`"+part.replace(/`/g,"``")+"`"}function quotedName(fullName){return fullName.split(".").map(quote).join(".")}function tableGrant(table,principal){const parts=table.split(".");const target=quote(principal);const statements=[];if(parts.length>=3){statements.push(`GRANT USE CATALOG ON CATALOG ${quote(parts[0])} TO ${target};`);statements.push(`GRANT USE SCHEMA ON SCHEMA ${quote(parts[0])}.${quote(parts[1])} TO ${target};`)}statements.push(`GRANT SELECT ON TABLE ${quotedName(table)} TO ${target};`);return{kind:"sql",statement:statements.join("\n"),guidance:""}}function aclField(principal){return principal.includes("@")?"user_name":"service_principal_name"}function warehouseGrant(warehouseId,principal){return{kind:"cli",statement:`databricks permissions update warehouses ${warehouseId} --json '{"access_control_list":[{"${aclField(principal)}":"${principal}","permission_level":"CAN_USE"}]}'`,guidance:""}}var SQL_ACCESS_ENTITLEMENT="databricks-sql-access";var WORKSPACE_ACCESS_ENTITLEMENT="workspace-access";var UNKNOWN_USER_ID="<numeric-user-id>";function entitlementGrant(userId){const target=userId&&userId.trim()?userId.trim():UNKNOWN_USER_ID;return{kind:"cli",statement:`databricks api patch /api/2.0/preview/scim/v2/Users/${target} --json '{"schemas":["urn:ietf:params:scim:api:messages:2.0:PatchOp"],"Operations":[{"op":"add","path":"entitlements","value":[{"value":"${SQL_ACCESS_ENTITLEMENT}"},{"value":"${WORKSPACE_ACCESS_ENTITLEMENT}"}]}]}'`,guidance:"The id is the numeric SCIM id for the account, not an email address."}}function genieSpaceGrant(spaceId,principal){return{kind:"cli",statement:`databricks permissions update genie ${spaceId} --json '{"access_control_list":[{"${aclField(principal)}":"${principal}","permission_level":"CAN_RUN"}]}'`,guidance:""}}var BLOCKED_STATUS={"no-user-token":409,"no-sql-scope":409,"token-rejected":401,"warehouse-denied":403,"no-sql-entitlement":403,"warehouse-missing":503,"dependency-down":503,"not-configured":503};function statusForOutcome(outcome){return outcome.blocked?BLOCKED_STATUS[outcome.blocked.kind]:403}function probeStatement(table){return`SELECT 1 FROM ${table} WHERE 1=0`}var PERMISSION_MARKERS=["insufficient_permissions","permission_denied","sqlstate: 42501","is not accessible","not authorized","does not have"];var ABSENCE_MARKERS=["table_or_view_not_found","cannot be found","no such table"];var CATALOG_REFUSED=/catalog\s+'([^']+)'\s+is not accessible/i;var SCHEMA_REFUSED=/schema\s+'([^']+)'\s+is not accessible/i;function matches(text,markers){return markers.some(marker=>text.includes(marker))}function classifyDenial(message,table){const text=message.toLowerCase();if(matches(text,PERMISSION_MARKERS)){const catalog=CATALOG_REFUSED.exec(message);if(catalog){return{kind:"no-grant",object:catalog[1],objectKind:"catalog",permission:"USE CATALOG"}}const schema=SCHEMA_REFUSED.exec(message);if(schema){return{kind:"no-grant",object:schema[1],objectKind:"schema",permission:"USE SCHEMA"}}return{kind:"no-grant",object:table,objectKind:"table",permission:"SELECT"}}if(matches(text,ABSENCE_MARKERS))return{kind:"hidden-or-absent"};return{kind:"unrecognised"}}function classify(message,table,principal){const denial=classifyDenial(message,table);if(denial.kind==="unrecognised"){return{table,status:"error",detail:`The check did not complete, so your access to ${table} is unknown rather than refused. This is not a permission result and should not be read as one.`,apiMessage:message}}if(denial.kind==="hidden-or-absent"){return{table,status:"denied",detail:`${table} did not resolve for you. Unity Catalog hides objects it cannot traverse, so a missing grant and a table that is not there report identically. This is either no SELECT, no USE SCHEMA on the schema, no USE CATALOG on the catalog, or a table that genuinely does not exist. The grants below cover the first three; if they run and this still fails, the table is the one that is missing.`,missing:{object:table,permission:"SELECT",objectKind:"table"},remedy:tableGrant(table,principal),reason:"hidden-or-absent",apiMessage:message}}const scope=denial.objectKind==="table"?`${denial.permission} on ${denial.object}`:`${denial.permission} on ${denial.objectKind} ${denial.object}, which ${table} is inside`;return{table,status:"denied",detail:`You do not hold ${scope}. The API said so in those terms rather than hiding the object, so this is a grant that is missing and not a table that is absent.`,missing:{object:denial.object,permission:denial.permission,objectKind:denial.objectKind},remedy:tableGrant(table,principal),reason:"no-grant",apiMessage:message}}var UNKNOWN_PRINCIPAL="<your-username>";function missingScopeBlock(apiMessage){return{kind:"no-sql-scope",summary:"Your token reached Databricks and was refused for lacking the `sql` scope, so no statement was run and nothing about your own permissions was established. This is not a permission you are missing. It is a scope the app is missing, and no grant made to you will change it.",layer:"app configuration",remedy:{kind:"cli",statement:"# 1. `sql` must be in user_api_scopes (resources/player_insights_app.app.yml).\n# 2. A scope is applied when the app STARTS. A redeploy leaves it inert:\ndatabricks apps stop <app-name>\ndatabricks apps start <app-name>",guidance:""},apiMessage}}var VERIFICATION_BUDGET_MS=12e4;async function verifyTableAccess(tables,run,principal=UNKNOWN_PRINCIPAL,options={}){const budgetMs=options.budgetMs??VERIFICATION_BUDGET_MS;const concurrency=Math.max(1,Math.min(8,Math.trunc(options.concurrency??1)));const now=options.now??Date.now;const startedAt=now();const verdicts=[];for(let offset=0;offset<tables.length;offset+=concurrency){if(now()-startedAt>=budgetMs){for(const table of tables.slice(offset)){verdicts.push({table,status:"error",detail:`Not checked: the access check reached its ${Math.round(budgetMs/1e3)} s budget after ${verdicts.length} table(s). Nothing is known about your access to ${table} either way.`})}break}const batch=tables.slice(offset,offset+concurrency);const results=await Promise.all(batch.map(async table=>{try{return await run(table)}catch(error){return{ok:false,message:error.message}}}));for(let index=0;index<batch.length;index+=1){const table=batch[index];const result=results[index];if(result.ok){verdicts.push({table,status:"ok",detail:`SELECT on ${table} succeeded under your own token.`});continue}if(looksLikeMissingScope(result.message)){return{verdicts,...tally(verdicts),blocked:missingScopeBlock(result.message)}}verdicts.push(classify(result.message,table,principal))}}return{verdicts,...tally(verdicts)}}var SCIM_USERS_PATH="/api/2.0/preview/scim/v2/Users";function scimUserFilter(email){return{filter:`userName eq ${email}`}}function readScimEntitlements(body){const resources=body?.Resources;if(!Array.isArray(resources)||resources.length===0){return{kind:"unavailable",why:"the SCIM search returned no user for that address, so this token either cannot read that account or the address is not the one Databricks knows them by"}}const user=resources[0];const entitlements=Array.isArray(user.entitlements)?user.entitlements.map(entry=>typeof entry==="string"?entry:entry?.value??null).filter(value=>typeof value==="string"&&value.length>0):[];return{kind:"read",entitlements,userId:typeof user.id==="string"&&user.id?user.id:null}}function entitlementLookupVia(get){return async email=>{if(!email||!email.includes("@")){return{kind:"unavailable",why:"the signed-in identity is not an email address, so there is nothing to look up"}}try{return readScimEntitlements(await get(SCIM_USERS_PATH,scimUserFilter(email)))}catch(error){return{kind:"unavailable",why:`the SCIM call failed: ${error.message}`}}}}async function refineWarehouseDenial(warehouseId,principal,apiMessage,lookup){if(!lookup)return warehouseDeniedBlock(warehouseId,principal,apiMessage);let reading;try{reading=await lookup(principal)}catch(error){reading={kind:"unavailable",why:`the lookup threw: ${error.message}`}}if(reading.kind==="unavailable"){return warehouseDeniedBlock(warehouseId,principal,apiMessage,{kind:"unknown",why:reading.why})}if(reading.entitlements.includes(SQL_ACCESS_ENTITLEMENT)){return warehouseDeniedBlock(warehouseId,principal,apiMessage,{kind:"held"})}return entitlementDeniedBlock(warehouseId,principal,reading.userId,apiMessage)}async function verifyWarehouseAccess(warehouseId,probe,principal=UNKNOWN_PRINCIPAL,entitlements,age=AGE_NOT_SUPPLIED){let result;try{result=await probe()}catch(error){result={ok:false,message:error.message}}if(result.ok)return null;if(looksLikeMissingScope(result.message))return missingScopeBlock(result.message);const byStatus=classifyWarehouseStatus(result.status,warehouseId,principal,result.message,age);if(byStatus){return byStatus.kind==="warehouse-denied"?refineWarehouseDenial(warehouseId,principal,result.message,entitlements):byStatus}if(matches(result.message.toLowerCase(),PERMISSION_MARKERS)){return warehouseDeniedBlock(warehouseId,principal,result.message)}return{kind:"dependency-down",summary:`SQL warehouse ${warehouseId} did not answer \`SELECT 1\`, and did not refuse it for a permission either. It is most likely stopped, starting, or unhealthy. Nothing about your permissions was established; try again once it is running.`,layer:"SQL warehouse availability",apiMessage:result.message}}function warehouseDeniedBlock(warehouseId,principal,apiMessage,entitlement){const base=`You do not hold CAN_USE on SQL warehouse ${warehouseId}, so no statement could be run as you at all. No table was checked, and none of them should be assumed either way. This is one missing grant on one workspace object, not a verdict on your Unity Catalog access.`;const qualifier=entitlement?.kind==="held"?` Your account does carry the \`${SQL_ACCESS_ENTITLEMENT}\` entitlement, which is checked because a missing one is refused identically, so the warehouse’s own permissions really are the thing to look at.`:entitlement?.kind==="unknown"?` A missing \`${SQL_ACCESS_ENTITLEMENT}\` entitlement produces this exact refusal and could not be checked here (${entitlement.why}), so it is not ruled out. Read it before anybody edits the warehouse ACL:
  databricks api get "/api/2.0/preview/scim/v2/Users?filter=userName+eq+<email>"`:"";return{kind:"warehouse-denied",summary:`${base}${qualifier}`,layer:"SQL warehouse permissions",missing:{object:warehouseId,permission:"CAN_USE",objectKind:"sql-warehouse"},remedy:warehouseGrant(warehouseId,principal),apiMessage}}function entitlementDeniedBlock(warehouseId,principal,userId,apiMessage){return{kind:"no-sql-entitlement",summary:`Your account does not carry the \`${SQL_ACCESS_ENTITLEMENT}\` entitlement, so no SQL statement can run as you anywhere in this workspace. This was read from SCIM, not inferred from the refusal. An entitlement is an assignment on your identity, not a permission on warehouse ${warehouseId}, and no CAN_USE added to that warehouse would change it. A workspace admin can grant it with the command below. No table was checked.`,layer:"workspace entitlements",missing:{object:principal,permission:SQL_ACCESS_ENTITLEMENT,objectKind:"workspace-entitlement"},remedy:entitlementGrant(userId),apiMessage}}function classifyWarehouseStatus(status,warehouseId,principal,apiMessage,age=AGE_NOT_SUPPLIED){if(status===void 0)return null;if(status===403)return warehouseDeniedBlock(warehouseId,principal,apiMessage);if(status===401){const rejected=tokenRejection({age,apiMessage});return{kind:"token-rejected",summary:rejected.explanation,layer:rejected.layer,...rejected.remedy?{remedy:rejected.remedy}:{},apiMessage}}if(status===404){return{kind:"warehouse-missing",summary:`Databricks has no SQL warehouse \`${warehouseId}\` to answer for (HTTP 404). Either that id does not exist in this workspace, or it is not visible to you at all. The API reports both the same way and this cannot tell them apart. Either way no statement was run, and nothing about your permissions was established.`,layer:"SQL warehouse configuration",remedy:{kind:"cli",statement:`databricks warehouses get ${warehouseId}`,guidance:"Run by a workspace admin: a warehouse that comes back is one you cannot see, and one still absent is a deployment to correct."},apiMessage}}return null}function describeImpact(outcome){if(outcome.blocked)return[];if(outcome.denied===0)return[];return["Genie is all-or-nothing per space: a space fails as a whole if a single table it curates is unreadable, so a question it would have answered either fails outright or falls back to direct SQL over the tables that do resolve, in the same voice as a complete one."]}function genieSpaceLabel(parts){const title=(parts.title??"").trim();const id=(parts.id??"").trim();const role=(parts.role??"").trim();if(title&&id)return`${title} · ${id}`;if(role&&title)return`${role} · ${title}`;if(role&&id)return`${role} · ${id}`;return title||id||role||"Genie space"}function configValue(entries,key){const entry=entries.find(item=>item.key===key);return entry?.value}function asStringList(value){if(Array.isArray(value)){return value.map(item=>String(item).trim()).filter(Boolean)}if(typeof value==="string"){return value.split(",").map(item=>item.trim()).filter(Boolean)}return[]}function asString(value){if(typeof value==="string")return value.trim();if(typeof value==="number"||typeof value==="boolean"||typeof value==="bigint"){return String(value)}return""}function asRecord(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}function accessDependenciesFrom(sources){const configuration=sources.configuration??[];const env=sources.env??{};const fromConfig=(key,envVar)=>{const configured=configValue(configuration,key);if(configured!==void 0&&configured!==null&&configured!==""){return configured}return env[envVar]};const manifest=asStringList(fromConfig("declared_manifest","PLAYER_INSIGHTS_DECLARED_MANIFEST"));const listed=asStringList(fromConfig("tables","PLAYER_INSIGHTS_TABLES"));const tables=manifest.length>0?manifest:listed.length>0?listed:qualifyDataContractTables(asString(fromConfig("catalog","PLAYER_INSIGHTS_CATALOG")),asString(fromConfig("schema","PLAYER_INSIGHTS_SCHEMA")));const dataId=asString(fromConfig("data_genie_space_id","PLAYER_INSIGHTS_DATA_GENIE_ID"));const dictionaryId=asString(fromConfig("dictionary_genie_space_id","PLAYER_INSIGHTS_DICTIONARY_GENIE_ID"));const dataTitle=asString(fromConfig("data_genie_space_title","PLAYER_INSIGHTS_DATA_GENIE_TITLE"));const dictionaryTitle=asString(fromConfig("dictionary_genie_space_title","PLAYER_INSIGHTS_DICTIONARY_GENIE_TITLE"));const genieSpaces=[];if(dataId){genieSpaces.push({id:dataId,role:"Data Genie space",label:genieSpaceLabel({id:dataId,title:dataTitle,role:"Data Genie space"})})}if(dictionaryId){genieSpaces.push({id:dictionaryId,role:"Dictionary Genie space",label:genieSpaceLabel({id:dictionaryId,title:dictionaryTitle,role:"Dictionary Genie space"})})}return{tables,genieSpaces}}function missingGenieScopeLimit(evidence){return{what:"Whether the Genie spaces are shared with you.",why:`Reading a Genie space needs the \`${GENIE_SCOPE}\` scope on the forwarded token, and this token does not carry it (${evidence}). That is a scope the app is missing rather than a permission you are missing, and no grant made to you would change it. Add \`${GENIE_SCOPE}\` to \`user_api_scopes\`, then STOP and START the app: a scope is applied when the app starts, and a redeploy leaves it inert.`}}function classifyGenieProbe(result,space,principal,age=AGE_NOT_SUPPLIED){const liveTitle=result.ok&&typeof result.title==="string"?result.title.trim():"";const label=liveTitle?genieSpaceLabel({id:space.id,title:liveTitle,role:space.role}):space.label;const base={space:space.id,label};if(result.ok){if(result.space){return{...base,status:"ok",detail:`${label} resolved under your own token, so you hold at least CAN RUN on it. This is about the space being shared with you, not about the tables it curates; those are the table rows above.`}}return{...base,status:"error",detail:`Databricks answered for ${label} without naming the space, so nothing about your access to it was established. An empty answer is not a yes, and this is deliberately not reported as one.`,apiMessage:"The response carried no space_id."}}if(result.status===403){return{...base,status:"denied",detail:`You do not hold CAN RUN on ${label}. Databricks refused the space itself (HTTP 403), so this is one grant on one workspace object and says nothing about your Unity Catalog access.`,missing:{object:space.id,permission:"CAN_RUN",objectKind:"genie-space"},remedy:genieSpaceGrant(space.id,principal),reason:"no-grant",apiMessage:result.message}}if(result.status===404){return{...base,status:"denied",detail:`${label} did not resolve for you (HTTP 404). Databricks reports a space that is not shared with you and a space id that does not exist the same way, so this is either a missing grant or a space that is gone. The grant below covers the first; if it runs and this still fails, the id the agent is configured with is the problem.`,missing:{object:space.id,permission:"CAN_RUN",objectKind:"genie-space"},remedy:genieSpaceGrant(space.id,principal),reason:"hidden-or-absent",apiMessage:result.message}}if(result.status===401){const rejected=tokenRejection({age,apiMessage:result.message});return{...base,status:"error",detail:`${rejected.explanation} Nothing about your access to ${label} was established.`,...rejected.remedy?{remedy:rejected.remedy}:{},apiMessage:result.message}}return{...base,status:"error",detail:`The check against ${label} did not complete, so your access to it is unknown rather than refused. This is not a permission result and should not be read as one.`,apiMessage:result.message}}async function verifyGenieAccess(spaces,probe,principal=UNKNOWN_PRINCIPAL,scopeState=null,age=AGE_NOT_SUPPLIED){if(spaces.length===0){return{verdicts:[],notChecked:{what:"Whether the Genie spaces are shared with you.",why:"The agent’s configuration named no Genie spaces, so there was nothing to ask about on your behalf. This is a configuration state and says nothing about your permissions."}}}if(scopeState===false){return{verdicts:[],notChecked:missingGenieScopeLimit("its own scope claim does not list it")}}const results=await Promise.all(spaces.map(async space=>{try{return await probe(space.id)}catch(error){return{ok:false,message:error.message}}}));const refusedForScope=results.find(result=>!result.ok&&looksLikeMissingScope(result.message));if(refusedForScope){return{verdicts:[],notChecked:missingGenieScopeLimit(`Databricks refused the call and said so: ${refusedForScope.message}`)}}return{verdicts:results.map((result,index)=>classifyGenieProbe(result,spaces[index],principal,age))}}function limitsOfThisCheck(servingChecked,genie,tablesChecked=0){const servingSaw=servingChecked.length>0?"Also checked as the agent serving principal (what preflight reported for that identity, not a claim about who executes later asks): "+servingChecked.map(check=>`${check.label} (${check.status})`).join("; ")+".":void 0;const genieLimit=genie?.notChecked?{...genie.notChecked,insteadAs:servingSaw}:genie&&genie.verdicts.length>0?{what:"Whether the answers you get would be limited to what you can see in Genie.",why:"Your own access to the spaces was checked and is reported above. That establishes which named spaces answered under your token; it does not prove every figure on screen came from one of them. Who runs a later ask is the deployment's execution identity (Connections), not this check.",insteadAs:servingSaw}:{what:"Whether the Genie spaces are shared with you.",why:`Reading a Genie space needs the \`${GENIE_SCOPE}\` scope on the forwarded token. This run did not get as far as asking, so nothing about your access to the spaces was established either way.`,insteadAs:servingSaw};const limits=[genieLimit,...tablesChecked===0?[{what:"Whether you can read the tables behind an answer.",why:"The app could not learn which tables this release may read (no PLAYER_INSIGHTS_DECLARED_MANIFEST / PLAYER_INSIGHTS_TABLES, and no catalog+schema to qualify the committed data contract), so no SELECT was run on your behalf. A pass above means you can run a statement, not that you could read the data."}]:[],{what:"Whether a row filter or a column mask narrows what you would see.",why:"Neither reports itself. A filtered query succeeds and returns fewer rows, so a green above means the grant exists, not that you would see every row behind a figure. There is no error to check for and nothing here can detect it."}];return limits}async function verifyAccess(input,probes){const age=input.presentedToken??AGE_NOT_SUPPLIED;const genie=probes.genieSpace?await verifyGenieAccess(input.genieSpaces??[],probes.genieSpace,input.principal,input.genieScope??null,age):void 0;const notChecked=limitsOfThisCheck(input.servingChecked??[],genie,input.tables.length);const blocked=await verifyWarehouseAccess(input.warehouseId,()=>probes.warehouse(),input.principal,probes.entitlements,age);if(blocked){return{verdicts:[],ok:0,denied:0,errored:0,blocked,impact:[],notChecked,...genie?{genie:genie.verdicts}:{}}}const outcome=await verifyTableAccess(input.tables,probes.table,input.principal);return{...outcome,warehouseVerified:true,impact:describeImpact(outcome),notChecked,...genie?{genie:genie.verdicts}:{}}}function tally(verdicts){return{ok:verdicts.filter(v=>v.status==="ok").length,denied:verdicts.filter(v=>v.status==="denied").length,errored:verdicts.filter(v=>v.status==="error").length}}function isVerified(outcome){return!outcome.blocked&&(outcome.warehouseVerified===true||outcome.verdicts.length>0)&&outcome.verdicts.every(verdict=>verdict.status==="ok")&&!(outcome.genie??[]).some(verdict=>verdict.status==="denied")}function verificationSummary(outcome){const tables=outcome.ok===1?"1 table":`${outcome.ok} tables`;const head=outcome.ok===0?"Verified you can run a statement on the SQL warehouse under your own token. No table was checked, so nothing about your access to the data behind an answer was established":`Verified you hold CAN_USE on the SQL warehouse and SELECT on ${tables} under your own token`;return`${head}. ${genieClause(outcome.genie)} Row-level filters and column masks were not checked and are not covered by this.`}function genieClause(genie){if(!genie||genie.length===0){return"Genie space access was not checked as you and is not covered by this."}const passed=genie.filter(verdict=>verdict.status==="ok").length;const unknown=genie.filter(verdict=>verdict.status==="error");const spaces=genie.length===1?"1 Genie space":`${genie.length} Genie spaces`;const head=`CAN RUN confirmed on ${passed} of ${spaces} under the same token.`;if(unknown.length===0)return head;return`${head} ${unknown.length} did not answer, so ${unknown.length===1?"it is":"they are"} unknown rather than granted: ${unknown.map(verdict=>verdict.label).join("; ")}.`}var STATEMENT_TIMEOUT_MS=45e3;function warehouseProbeFor(options){const run=statementExecutorFor(options);return()=>run("SELECT 1")}function statementRunnerFor(options){const run=statementExecutorFor(options);return table=>run(probeStatement(table))}var GENIE_PROBE_TIMEOUT_MS=15e3;function genieSpaceProbeFor(options){const call=options.fetchImpl??fetch;const timeoutMs=options.timeoutMs??GENIE_PROBE_TIMEOUT_MS;return async spaceId=>{let response;try{response=await call(`${options.host}/api/2.0/genie/spaces/${encodeURIComponent(spaceId)}`,{method:"GET",headers:{authorization:`Bearer ${options.token}`},signal:AbortSignal.timeout(timeoutMs)})}catch(error){const timedOut=error?.name==="TimeoutError"||error?.name==="AbortError";return{ok:false,message:timedOut?`Genie did not answer within ${timeoutMs} ms, so this check did not complete.`:`Genie could not be reached: ${error.message}`}}const body=await response.json().catch(()=>({}));if(!response.ok){return{ok:false,status:response.status,message:asString(body?.message)||`Databricks answered HTTP ${response.status} with no message body.`}}return{ok:true,space:typeof body?.space_id==="string"?body.space_id:null,title:typeof body?.title==="string"?body.title:null}}}function statementExecutorFor(options){const call=options.fetchImpl??fetch;const timeoutMs=options.timeoutMs??STATEMENT_TIMEOUT_MS;const waitTimeoutSeconds=Math.max(5,Math.min(50,Math.trunc(options.waitTimeoutSeconds??30)));return async statement=>{let response;try{response=await call(`${options.host}/api/2.0/sql/statements`,{method:"POST",headers:{authorization:`Bearer ${options.token}`,"content-type":"application/json"},body:JSON.stringify({warehouse_id:options.warehouseId,statement,query_tags:sqlQueryTags({surface:"connections",tool:"access_verification",operation:"preflight"}),wait_timeout:`${waitTimeoutSeconds}s`,on_wait_timeout:"CANCEL"}),signal:AbortSignal.timeout(timeoutMs)})}catch(error){const timedOut=error?.name==="TimeoutError"||error?.name==="AbortError";return{ok:false,message:timedOut?`The SQL warehouse did not answer within ${timeoutMs} ms, so this check did not complete.`:`The SQL warehouse could not be reached: ${error.message}`}}const body=await response.json().catch(()=>({}));if(!response.ok){return{ok:false,status:response.status,message:asString(body.message)||`Databricks answered HTTP ${response.status} with no message body.`}}const status=asRecord(body.status);const state=asString(status.state);if(state==="SUCCEEDED")return{ok:true};const failure=asString(asRecord(status.error).message);return{ok:false,message:failure||`The statement ended in state ${state||"UNKNOWN"}.`}}}var SPAN_PERCENTILE_FLOOR=20;var DAY_MS=864e5;function opsCurrentMonthRange(now=Date.now()){const today=new Date(now).toISOString().slice(0,10);const monthStart=`${today.slice(0,7)}-01`;const lastComplete=new Date(now-DAY_MS).toISOString().slice(0,10);return{from:monthStart,to:lastComplete<monthStart?monthStart:lastComplete}}function opsDayRange(from,to,now){const day=at=>new Date(at).toISOString().slice(0,10);const start=Date.parse(from);const end=Date.parse(to);const lastComplete=now-DAY_MS;if(Number.isFinite(start)&&Number.isFinite(end)&&end>=start){return{from:day(start),to:day(Math.min(end,lastComplete))}}return{from:day(lastComplete-6*DAY_MS),to:day(lastComplete)}}var TELEMETRY_SCHEMA_ENV="PLAYER_INSIGHTS_TELEMETRY_SCHEMA";var LOGS_TABLE="otel_logs";var EXPORTER_TABLES=["otel_spans","otel_metrics"];function telemetrySchema(raw=process.env[TELEMETRY_SCHEMA_ENV]){const candidate=(raw??"").trim().replace(/^`|`$/g,"");if(!candidate)return"";const parts=candidate.split(".").filter(part=>part.length>0);if(parts.length!==2){console.warn(`[ops] ${TELEMETRY_SCHEMA_ENV} is ${JSON.stringify(candidate)}, which is not a catalog and schema. App telemetry is being reported as not configured rather than guessed at.`);return""}return parts.join(".")}function record(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}function telemetryDestinationFromApp(body){const app=record(body);const raw=app.telemetry_export_destinations??app.telemetryExportDestinations;const destinations=Array.isArray(raw)?raw:[];if(destinations.length===0)return{state:"disabled",schema:"",reason:""};for(const destination of destinations){const row=record(destination);const unityCatalog=record(row.unity_catalog??row.unityCatalog);const logs=typeof unityCatalog.logs_table==="string"?unityCatalog.logs_table:typeof unityCatalog.logsTable==="string"?unityCatalog.logsTable:"";const match=/^([^.]+\.[^.]+)\.otel_logs$/.exec(logs.trim());if(match)return{state:"configured",schema:match[1],reason:""}}return{state:"unreadable",schema:"",reason:"The Apps record has telemetry destinations, but none names a valid Unity Catalog logs table."}}var workspaceTelemetryDestinationReader=async appName=>{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");return new WorkspaceClient({}).apiClient.request({path:`/api/2.0/apps/${encodeURIComponent(appName)}`,method:"GET",headers:new Headers({Accept:"application/json"}),raw:false})};async function readTelemetryDestination(input={}){const fromEnvironment=telemetrySchema(input.raw);if(fromEnvironment)return{state:"configured",schema:fromEnvironment,reason:""};const appName=(input.appName??process.env.DATABRICKS_APP_NAME??"").trim();if(!appName){return{state:"unreadable",schema:"",reason:"The app name is unavailable, so the live telemetry configuration could not be checked."}}try{return telemetryDestinationFromApp(await(input.read??workspaceTelemetryDestinationReader)(appName))}catch(error){return{state:"unreadable",schema:"",reason:`The Apps record could not be read, so telemetry configuration is unknown: ${error.message}`}}}function logsTable(schema){return schema?`${schema}.${LOGS_TABLE}`:""}function grantFor(table,principal,permission="SELECT"){const remedy=tableGrant(table,principal);return{object:table,privilege:permission,statement:remedy.statement}}function offMeasurement(insightsHref){return{telemetry:"not-enabled",variable:TELEMETRY_SCHEMA_ENV,table:"",grant:null,insightsHref,requestsPerHour:[],lastServedAt:"",recordingSince:"",signInsPerDay:[],errors:{count:0,recent:[]},reason:`App telemetry is not switched on for this deployment, so nothing is recording what the app served. It is configuration rather than code: set ${TELEMETRY_SCHEMA_ENV} to a catalog and schema and redeploy, and the platform begins writing from that deploy onward.`}}function uncheckedMeasurement(insightsHref,note){const schema=telemetrySchema();if(!schema){return{...offMeasurement(insightsHref),telemetry:"unreadable",reason:`Telemetry configuration could not be checked because ${note}`}}const table=logsTable(schema);return{...offMeasurement(insightsHref),telemetry:"unreadable",table,reason:`App telemetry is switched on and writing to ${table}, and ${note} So nothing about what this app served was established here, which is unchecked rather than empty.`}}function noHistoryReason(){return"No app requests have been recorded yet."}function stateFromFailure(message,table){const denial=classifyDenial(message,table);if(denial.kind==="no-grant"){return{state:"no-grant",permission:denial.permission,object:denial.object}}return{state:"unreadable",permission:"",object:table}}function attribute(key){return`variant_get(attributes, '$["${key}"]', 'string')`}var AUTH_EVENT="app.auth";var SIGN_IN_REASON="user_login";var APP_LOG_SOURCE="APP";function buildTelemetryStatement(table){return`WITH scoped AS (
  SELECT time, severity_text, body, attributes
  FROM ${table}
), served AS (
  SELECT time, severity_text, body
  FROM scoped
  WHERE ${attribute("app.log_source")} = '${APP_LOG_SOURCE}'
)
SELECT 'request-hour' AS kind,
       date_format(date_trunc('HOUR', time), 'yyyy-MM-dd HH:00') AS bucket,
       CAST(COUNT(*) AS STRING) AS value,
       '' AS detail
FROM served
GROUP BY 1, 2
UNION ALL
SELECT 'last-served', '', CAST(MAX(time) AS STRING), '' FROM served
UNION ALL
SELECT 'sign-in-day',
       date_format(date_trunc('DAY', time), 'yyyy-MM-dd'),
       CAST(COUNT(*) AS STRING),
       ''
FROM scoped
WHERE ${attribute("event.name")} = '${AUTH_EVENT}'
  AND ${attribute(`${AUTH_EVENT}.reason`)} = '${SIGN_IN_REASON}'
GROUP BY 1, 2
UNION ALL
SELECT 'error-count', '', CAST(COUNT(*) AS STRING), ''
FROM served
WHERE upper(severity_text) = 'ERROR'
UNION ALL
SELECT 'error-line', CAST(time AS STRING), '', substring(body, 1, 400)
FROM served
WHERE upper(severity_text) = 'ERROR'
UNION ALL
SELECT 'first-recorded', '', CAST(MIN(time) AS STRING), '' FROM ${table}
ORDER BY 1, 2`}var RECENT_ERROR_LIMIT=5;function readTelemetryRows(dataArray){const requestsPerHour=[];const signInsPerDay=[];const recent=[];let lastServedAt="";let recordingSince="";let count=0;if(Array.isArray(dataArray)){for(const raw of dataArray){if(!Array.isArray(raw)||raw.length<4)continue;const[kind,bucket,value,detail]=raw;if(kind==="request-hour"&&bucket){requestsPerHour.push({hour:bucket,count:Number(value??0)})}else if(kind==="sign-in-day"&&bucket){signInsPerDay.push({day:bucket,count:Number(value??0)})}else if(kind==="last-served"&&value){lastServedAt=value}else if(kind==="first-recorded"&&value){recordingSince=value}else if(kind==="error-count"){count=Number(value??0)}else if(kind==="error-line"&&bucket){recent.push({at:bucket,body:detail??""})}}}recent.sort((left,right)=>right.at.localeCompare(left.at));return{requestsPerHour,signInsPerDay,lastServedAt,recordingSince,errors:{count,recent:recent.slice(0,RECENT_ERROR_LIMIT)}}}function hasHistory(figures){return figures.requestsPerHour.length>0||figures.signInsPerDay.length>0||Boolean(figures.lastServedAt)}function buildExporterStatement(schema){const branches=EXPORTER_TABLES.map(name=>`SELECT '${name}' AS name,
       CAST(COUNT(*) AS STRING) AS rows,
       CAST(MIN(time) AS STRING) AS first_at,
       CAST(MAX(time) AS STRING) AS last_at
FROM ${schema}.${name}`);return`${branches.join("\nUNION ALL\n")}
ORDER BY 1`}function rowText(value){return typeof value==="string"?value.trim():""}function readExporterRows(dataArray,schema){const tables=[];if(Array.isArray(dataArray)){for(const raw of dataArray){if(!Array.isArray(raw)||raw.length<4)continue;const[name,rows,firstAt,lastAt]=raw;const table=rowText(name);if(!table)continue;tables.push({table,rows:Number(rows??0)||0,firstAt:rowText(firstAt),lastAt:rowText(lastAt)})}}const written=tables.reduce((total,entry)=>total+entry.rows,0);return{state:tables.length===0?"unreadable":written>0?"exporting":"silent",tables,error:tables.length===0?"The warehouse answered the count with no rows at all, so nothing was established.":"",schema}}function exporterFailure(message,schema){return{state:"unreadable",tables:[],error:message.trim()||"the count did not complete",schema}}var WAREHOUSE_ENV="DATABRICKS_SQL_WAREHOUSE_ID";var EXPORTER_CACHE_MS=5*60*1e3;var workspaceExporterReader=async()=>{const schema=telemetrySchema();if(!schema)return{...NO_EXPORTER_READING};const warehouse=(process.env[WAREHOUSE_ENV]??"").trim();if(!warehouse){return exporterFailure(`No ${WAREHOUSE_ENV} is set, so there is nothing to run the count on.`,schema)}try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});const body=await client.apiClient.request({path:"/api/2.0/sql/statements",method:"POST",headers:new Headers({"content-type":"application/json"}),payload:{warehouse_id:warehouse,statement:buildExporterStatement(schema),query_tags:sqlQueryTags({surface:"telemetry",tool:"ops_telemetry",operation:"exporter_read"}),wait_timeout:"30s",on_wait_timeout:"CANCEL",format:"JSON_ARRAY",disposition:"INLINE"},raw:false});const state=rowText(body?.status?.state);if(state!=="SUCCEEDED"){return exporterFailure(rowText(body?.status?.error?.message)||`the count ended in ${state||"an unknown state"}`,schema)}return readExporterRows(body?.result?.data_array??[],schema)}catch(error){return exporterFailure(error?.message??"the count did not complete",schema)}};var cached=null;async function readExporter(input={}){const now=input.now??Date.now();const ttl=input.cacheMs??EXPORTER_CACHE_MS;if(cached&&now-cached.at<ttl)return cached.reading;const reading=await(input.read??workspaceExporterReader)();cached={at:now,reading};return reading}var APPS_PATH="/api/2.0/apps";var APP_NAME_ENV="DATABRICKS_APP_NAME";var OTEL_ENDPOINT_ENV="OTEL_EXPORTER_OTLP_ENDPOINT";var COMPUTE_ENVELOPES={MEDIUM:{vcpus:2,memoryGb:6,dbuPerHour:.5}};function textOf(value){return typeof value==="string"?value.trim():""}function objectOf(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}var WORKSPACE_STATUS_PATH="/api/2.0/workspace/get-status";var WORKSPACE_PREFIX="/Workspace/";function workspaceIdFromAppUrl(url){return/^https?:\/\/[^./]*?-(\d{6,})\.[^/]*databricksapps\.com/i.exec(textOf(url))?.[1]??""}function withWorkspace(url,workspaceId){const org=textOf(workspaceId);return org?`${url}?o=${encodeURIComponent(org)}`:url}function browseFolderUrl(input){const base=normalizeWorkspaceHost(input.host);const id=textOf(input.folderId);if(!base||!id)return"";return withWorkspace(`${base}/browse/folders/${encodeURIComponent(id)}`,input.workspaceId)}function appPageUrl(input){const base=normalizeWorkspaceHost(input.host);const name=textOf(input.appName);if(!base||!name)return"";return withWorkspace(`${base}/apps/${encodeURIComponent(name)}`,input.workspaceId)}function sourceFolderPath(body){const deployment=objectOf(body.active_deployment);if(Object.keys(objectOf(deployment.git_source)).length>0)return"";const path=textOf(deployment.source_code_path);return path.startsWith(WORKSPACE_PREFIX)?path:""}function appTags(raw){if(Array.isArray(raw)){return raw.map(entry=>typeof entry==="string"?entry.trim():textOf(objectOf(entry).value)||textOf(objectOf(entry).key)).filter(Boolean)}const map=objectOf(raw);return Object.values(map).map(textOf).filter(Boolean)}function appServing(body){const app=objectOf(body.app_status);const compute=objectOf(body.compute_status);return{app:textOf(app.state),compute:textOf(compute.state),message:textOf(app.message)||textOf(compute.message)}}function appCompute(raw){const size=textOf(raw);if(!size)return null;return{size,envelope:COMPUTE_ENVELOPES[size.toUpperCase()]??null}}function appFacts(input){const otelExporter=textOf(input.otelExporter);const otelExport=input.otelExport??NO_EXPORTER_READING;if(input.read.kind!=="ok")return{...NO_APP_FACTS,otelExporter,otelExport};const body=input.read.body;const appUrl=textOf(body.url);const deployment=objectOf(body.active_deployment);const gitSource=objectOf(deployment.git_source);const gitBacked=Object.keys(gitSource).length>0;const sourcePath=gitBacked?textOf(gitSource.source_code_path):textOf(deployment.source_code_path);const appName=textOf(body.name);const workspaceHost=normalizeWorkspaceHost(input.workspaceHost);const gitRef=textOf(gitSource.branch)||textOf(gitSource.tag)||textOf(gitSource.commit);const workspaceId=textOf(input.workspaceId)||workspaceIdFromAppUrl(appUrl);const folderUrl=gitBacked?"":browseFolderUrl({host:workspaceHost,folderId:input.sourceFolderId??"",workspaceId});return{url:appUrl,answered:true,description:textOf(body.description),compute:appCompute(body.compute_size),tags:appTags(body.tags),deployedAt:textOf(deployment.create_time)||textOf(body.update_time),deployedBy:textOf(deployment.creator)||textOf(body.updater),source:{path:sourcePath,workspaceUrl:folderUrl||(sourcePath?appPageUrl({host:workspaceHost,appName,workspaceId}):""),gitRef},serving:appServing(body),otelExporter,otelExport}}var workspaceAppReader=async name=>{try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});const body=await client.apiClient.request({path:`${APPS_PATH}/${encodeURIComponent(name)}`,method:"GET",headers:new Headers({Accept:"application/json"}),raw:false});return{kind:"ok",body:body??{}}}catch(error){const shape=error??{};const status=Number(shape.statusCode??shape.status??0);const message=textOf(shape.message)||"the call did not complete";if(Number.isFinite(status)&&status>=400)return{kind:"refused",status,message};return{kind:"no-response",message}}};var FOLDER_ID_CACHE_MAX_ENTRIES=256;var FOLDER_ID_CACHE_TTL_MS=60*6e4;var knownFolderIds=new ExpiringLruCache(FOLDER_ID_CACHE_MAX_ENTRIES,FOLDER_ID_CACHE_TTL_MS);var workspaceFolderIdResolver=async path=>{const wanted=path.trim();if(!wanted)return"";const cached2=knownFolderIds.get(wanted);if(cached2!==void 0)return cached2;let id="";try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});const body=await client.apiClient.request({path:WORKSPACE_STATUS_PATH,method:"GET",query:{path:wanted},headers:new Headers({Accept:"application/json"}),raw:false});const found=(body??{}).object_id??(body??{}).resource_id;id=typeof found==="number"?String(found):textOf(found)}catch(error){console.warn(`[settings] The workspace could not resolve the folder id for ${wanted} (${error.message}), so the App source row points at the app's own page.`)}knownFolderIds.set(wanted,id);return id};async function readAppFacts(input={}){const name=(input.name??process.env[APP_NAME_ENV]??"").trim();const workspaceHost=input.workspaceHost??process.env.DATABRICKS_HOST??"";const otelExporter=(input.otelExporter??process.env[OTEL_ENDPOINT_ENV]??"").trim();let otelExport=NO_EXPORTER_READING;try{otelExport=await readExporter({read:input.readExport})}catch(error){console.warn("[settings] The exporter tables could not be counted:",error.message)}if(!name)return{...NO_APP_FACTS,otelExporter,otelExport};let read;try{read=await(input.read??workspaceAppReader)(name)}catch(error){read={kind:"no-response",message:error.message}}if(read.kind!=="ok"){console.warn(`[settings] The workspace could not be asked about the app ${name}:`,read.message)}const folderPath=read.kind==="ok"?sourceFolderPath(read.body):"";let sourceFolderId="";if(folderPath){try{sourceFolderId=await(input.resolveFolderId??workspaceFolderIdResolver)(folderPath)}catch(error){console.warn(`[settings] The folder id for ${folderPath} could not be read:`,error.message)}}return appFacts({read,workspaceHost,otelExporter,otelExport,sourceFolderId})}var USER_METADATA_TTL_MS=6e4;var USER_METADATA_CACHE_MAX_ENTRIES=512;var APP_CONTEXT_TTL_MS=5*6e4;var APP_CONTEXT_CACHE_MAX_ENTRIES=64;var APP_ATTACHED_RESOURCE_MAX_RECORDS=64;var SCIM_USERS_PATH2="/api/2.0/preview/scim/v2/Users";var userCache=new ExpiringLruCache(USER_METADATA_CACHE_MAX_ENTRIES,USER_METADATA_TTL_MS);var appCache=new ExpiringLruCache(APP_CONTEXT_CACHE_MAX_ENTRIES,APP_CONTEXT_TTL_MS);function recordOf(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}function textOf2(value,maxLength=512){if(typeof value!=="string")return"";let safe="";for(const character of value){const codePoint=character.codePointAt(0)??0;if(codePoint>=32&&codePoint!==127)safe+=character;if(safe.length>=maxLength)break}return safe.trim().slice(0,maxLength)}function identifierOf(value){if(typeof value==="string")return textOf2(value);return typeof value==="number"&&Number.isSafeInteger(value)?String(value):""}function readAt(now){return new Date(now).toISOString()}function keyPart(value){return value.trim().toLocaleLowerCase()}function scimResources(body){const resources=recordOf(body).Resources;return Array.isArray(resources)?resources.map(recordOf):[]}var KNOWN_APP_RESOURCE_TYPES=["postgres","serving_endpoint","sql_warehouse"];var SENSITIVE_RESOURCE_FIELD=/authorization|credential|password|secret|token/i;function resourceTypeOf(resource){const known=KNOWN_APP_RESOURCE_TYPES.find(type=>Object.prototype.hasOwnProperty.call(resource,type));if(known)return known;const future=Object.keys(resource).filter(key=>key!=="name"&&!SENSITIVE_RESOURCE_FIELD.test(key)).sort()[0];return textOf2(future,128)||"unknown"}function attachedResource(value,index){const resource=recordOf(value);const resourceType=resourceTypeOf(resource);const suppliedKey=textOf2(resource.name,256);if(resourceType==="unknown"&&!suppliedKey)return null;const resourceKey=suppliedKey||resourceType||`resource-${index+1}`;const details=recordOf(resource[resourceType]);const permission=textOf2(details.permission,128);let displayIdentifier=resourceKey;let title="";if(resourceType==="postgres"){const database=textOf2(details.database);const branch=textOf2(details.branch);displayIdentifier=database||branch||resourceKey;title=database&&branch&&branch!==database?`${database} · branch ${branch}`:displayIdentifier}else if(resourceType==="serving_endpoint"){displayIdentifier=textOf2(details.name)||resourceKey;title=displayIdentifier}else if(resourceType==="sql_warehouse"){displayIdentifier=identifierOf(details.id)||resourceKey;title=displayIdentifier}else{displayIdentifier=resourceKey;title=resourceKey}return{resourceKey,resourceType,displayIdentifier,permission,...title&&title!==displayIdentifier?{title}:{}}}function attachedResourcesOf(value){if(!Array.isArray(value))return[];return value.slice(0,APP_ATTACHED_RESOURCE_MAX_RECORDS).map(attachedResource).filter(resource=>resource!==null)}async function readUser(email,host,reader,now){const key=`${keyPart(host)}\0${keyPart(email)}`;const cached2=userCache.get(key,now);if(cached2)return cached2;const empty={displayName:"",objectId:"",state:"not_reported",readAt:readAt(now)};if(!email||!host){userCache.set(key,empty,now);return empty}let result=empty;try{const body=await reader(SCIM_USERS_PATH2,{filter:`userName eq ${email}`});const matching=scimResources(body).find(candidate=>keyPart(textOf2(candidate.userName))===keyPart(email));if(matching){result={displayName:textOf2(matching.displayName),objectId:textOf2(matching.id),state:"verified",readAt:readAt(now)}}}catch{}userCache.set(key,result,now);return result}async function readAppContext(appName,host,reader,now){const key=`${keyPart(host)}\0${keyPart(appName)}`;const cached2=appCache.get(key,now);if(cached2)return cached2;let result={workspaceId:"",servicePrincipal:{displayName:"",applicationId:"",objectId:"",authenticationType:"",attachedResources:[],state:"not_reported"}};if(appName&&host){try{const app=recordOf(await reader(`${APPS_PATH}/${encodeURIComponent(appName)}`));const applicationId=textOf2(app.service_principal_client_id);const displayName=textOf2(app.service_principal_name);const objectId=identifierOf(app.service_principal_id);const attachedResources=attachedResourcesOf(app.resources);result={workspaceId:workspaceIdFromAppUrl(textOf2(app.url)),servicePrincipal:{displayName,applicationId,objectId,authenticationType:applicationId?"OAuth machine-to-machine":"",attachedResources,state:displayName||applicationId||objectId?"verified":"not_reported"}}}catch{}}appCache.set(key,result,now);return result}var workspaceClient;var workspaceControlPlaneReader=async(path,query)=>{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");if(!workspaceClient){workspaceClient=new WorkspaceClient({httpTimeoutSeconds:5,retryTimeoutSeconds:0})}return workspaceClient.apiClient.request({path,method:"GET",...query?{query}:{},headers:new Headers({Accept:"application/json"}),raw:false})};async function readControlPlaneIdentityMetadata(input,deps={}){const appName=(input.appName??process.env[APP_NAME_ENV]??"").trim();const workspaceHost=normalizeWorkspaceHost(input.workspaceHost??process.env.DATABRICKS_HOST);const now=deps.now??Date.now();const reader=deps.read??workspaceControlPlaneReader;const[user,app]=await Promise.all([readUser(input.email.trim(),workspaceHost,reader,now),readAppContext(appName,workspaceHost,reader,now)]);return{user,app:{displayName:"Player Insights Agent",resourceName:appName,workspaceHost,workspaceId:app.workspaceId},servicePrincipal:app.servicePrincipal}}var GROUP_ROLE_MAPPINGS_TABLE=`${APP_SCHEMA}.group_role_mappings`;var GROUP_ROLE_MAPPINGS_DDL=`CREATE TABLE IF NOT EXISTS ${GROUP_ROLE_MAPPINGS_TABLE} (
  group_name TEXT PRIMARY KEY,
  role TEXT NOT NULL CHECK (role IN ('admin', 'consumer')),
  added_by TEXT NOT NULL,
  added_at TIMESTAMPTZ NOT NULL DEFAULT now()
)`;async function readGroupRoleMappings(store){const result=await store.query(`SELECT group_name, role, added_by, added_at FROM ${GROUP_ROLE_MAPPINGS_TABLE} ORDER BY added_at ASC`);return result.rows.flatMap(row=>{const groupName=columnText(row.group_name).trim();const role=columnText(row.role).trim().toLocaleLowerCase();if(!groupName||role!=="admin"&&role!=="consumer")return[];return[{groupName,role,setBy:normalizeAdminEmail(columnText(row.added_by)),setAt:row.added_at instanceof Date?row.added_at.toISOString():columnText(row.added_at)}]})}async function writeGroupRoleMapping(store,input){await store.query(`INSERT INTO ${GROUP_ROLE_MAPPINGS_TABLE} (group_name, role, added_by)
     VALUES ($1, $2, $3)
     ON CONFLICT (group_name) DO UPDATE
       SET role = EXCLUDED.role,
           added_by = EXCLUDED.added_by,
           added_at = NOW()`,[input.groupName.trim(),input.role,normalizeAdminEmail(input.actor)])}async function deleteGroupRoleMapping(store,groupName){await store.query(`DELETE FROM ${GROUP_ROLE_MAPPINGS_TABLE} WHERE lower(group_name) = lower($1)`,[groupName.trim()])}function record2(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}function normalized(value){return typeof value==="string"?value.trim().toLocaleLowerCase():""}function scimFilterLiteral(value){return`"${value.replace(/\\/g,"\\\\").replace(/"/g,'\\"')}"`}function groupRoleLookupForStore(store,reader=workspaceControlPlaneReader){return async email=>{const caller=normalized(email);if(!caller)return null;try{const mappings=await readGroupRoleMappings(store);if(mappings.length===0)return null;const body=await reader(SCIM_USERS_PATH2,{filter:`userName eq ${scimFilterLiteral(caller)}`});const resources=record2(body).Resources;const user=Array.isArray(resources)?resources.map(record2).find(candidate=>normalized(candidate.userName)===caller):null;const groups=Array.isArray(user?.groups)?new Set(user.groups.map(record2).map(group=>normalized(group.display)).filter(Boolean)):new Set;if(mappings.some(mapping=>mapping.role==="admin"&&groups.has(normalized(mapping.groupName)))){return"admin"}return mappings.some(mapping=>mapping.role==="consumer"&&groups.has(normalized(mapping.groupName)))?"consumer":null}catch{return null}}}var SEED_ADMIN_EMAILS_ENV="PLAYER_INSIGHTS_ADMIN_EMAILS";var SEED_SUPER_ADMIN_PREFIX="super:";function parseSeedAdmins(raw){const emails=[];const superEmails=[];const rejected=[];for(const token of(raw??"").split(/[,;\s]+/)){const raw0=normalizeAdminEmail(token);if(!raw0)continue;const isSuper=raw0.startsWith(SEED_SUPER_ADMIN_PREFIX);const candidate=isSuper?raw0.slice(SEED_SUPER_ADMIN_PREFIX.length).trim():raw0;if(!candidate.includes("@")){rejected.push(raw0);continue}if(!emails.includes(candidate))emails.push(candidate);if(isSuper&&!superEmails.includes(candidate))superEmails.push(candidate)}return{emails,superEmails,rejected}}var seedAdmins=[];var seedSuperAdmins=[];function seedAdminEmails(){return seedAdmins}function seedSuperAdminEmails(){return seedSuperAdmins}function seedRoles(){return{superAdmins:seedSuperAdmins,admins:seedAdmins}}function announceSeedAdmins(raw=process.env[SEED_ADMIN_EMAILS_ENV]){const{emails,superEmails,rejected}=parseSeedAdmins(raw);seedAdmins=emails;seedSuperAdmins=superEmails;if(rejected.length>0){console.error(`[admin] ${SEED_ADMIN_EMAILS_ENV} contains ${rejected.length} entr${rejected.length===1?"y":"ies"} with no "@" in ${JSON.stringify(rejected)}, so they are NOT addresses and have been ignored. Nothing is exposed by this. If one of them was meant to be an administrator, they are not one.`)}if(emails.length===0){console.warn(`[admin] NO SEED ADMINISTRATORS. ${SEED_ADMIN_EMAILS_ENV} is unset or empty, so nobody is an administrator except whoever an existing admin has added in Lakebase, and on a fresh deployment that is nobody. Monitoring, Ops, Benchmark Lab and the Settings gear will refuse every caller with 403. An empty list means nobody rather than everybody, deliberately. Set the bundle variable to give this deployment an administrator.`);return}console.log(`[admin:test] installed ${emails.length} in-memory administrator${emails.length===1?"":"s"}, ${superEmails.length} of them super administrator${superEmails.length===1?"":"s"}. Production does not use this path; it bootstraps an empty Lakebase roster once.`);if(superEmails.length===0){console.log(`[admin] NO SEED SUPER ADMINISTRATOR. Nobody can appoint or remove administrators from inside the app unless the stored roster already names a super admin. This deployment therefore has the two roles it always had. To name one, prefix an entry in ${SEED_ADMIN_EMAILS_ENV} with "${SEED_SUPER_ADMIN_PREFIX}".`)}}async function bootstrapSeedRoles(store,raw=process.env[SEED_ADMIN_EMAILS_ENV]){const parsed=parseSeedAdmins(raw);seedAdmins=[];seedSuperAdmins=[];if(parsed.rejected.length>0){console.error(`[admin] ${SEED_ADMIN_EMAILS_ENV} contains ${parsed.rejected.length} invalid entr${parsed.rejected.length===1?"y":"ies"} and they were ignored.`)}let current;try{current=await readRoster(store)}catch(error){const rawCode=error.code;const code=typeof rawCode==="string"||typeof rawCode==="number"?String(rawCode):"unknown";console.error(`[admin] ROLE BOOTSTRAP SKIPPED: the Lakebase roster could not be read (code ${code}: ${error.message}). Player Insights Agent will keep serving with no stored roles available; Connections reports the storage problem. No configured role was written or retained, because an unreadable roster is not evidence that it is empty.`);return"unavailable"}if(current.rows.length>0){console.log(`[admin] Lakebase already contains ${current.rows.length} role row${current.rows.length===1?"":"s"}; ${SEED_ADMIN_EMAILS_ENV} is ignored. A code deploy cannot add, remove, promote, or demote anybody.`);return"existing-roster"}const roles=parsed.emails.map(email=>({email,role:parsed.superEmails.includes(email)?"super_admin":"admin"}));if(roles.length===0){console.warn(`[admin] The Lakebase roster is empty and ${SEED_ADMIN_EMAILS_ENV} names nobody. No role was bootstrapped; every caller remains a consumer until an operator explicitly creates a role row.`);return"empty"}const params=[];const values=roles.map(({email,role},index)=>{params.push(email,role,email);const offset=index*3;return`($${offset+1}, $${offset+2}, $${offset+3})`}).join(", ");const inserted=await store.query(`INSERT INTO ${ADDED_ADMINS_TABLE} (email, ${ROLE_COLUMN}, added_by)
     SELECT seed.email, seed.role, seed.added_by
       FROM (VALUES ${values}) AS seed(email, role, added_by)
      WHERE NOT EXISTS (SELECT 1 FROM ${ADDED_ADMINS_TABLE})
     ON CONFLICT (email) DO NOTHING
     RETURNING email, ${ROLE_COLUMN}`,params);if(inserted.rows.length===0){console.log(`[admin] The roster stopped being empty before bootstrap committed; ${SEED_ADMIN_EMAILS_ENV} was ignored.`);return"existing-roster"}console.log(`[admin] Bootstrapped ${inserted.rows.length} role row${inserted.rows.length===1?"":"s"} into Lakebase. Future boots ignore deployed role config because the database is now authoritative.`);invalidateRosterCache(store);return"bootstrapped"}async function readAddedAdmins(store){const result=await store.query(`SELECT email, added_by, added_at FROM ${ADDED_ADMINS_TABLE} ORDER BY added_at ASC`);return result.rows.map(row=>({email:normalizeAdminEmail(columnText(row.email)),addedBy:columnText(row.added_by),addedAt:row.added_at instanceof Date?row.added_at.toISOString():columnText(row.added_at)}))}async function resolveRoleFrom(email,read,readGroupRole){const caller=normalizeAdminEmail(email);const seed=seedRoles();const floor=seedFloorFor(seed,caller);if(floor==="super_admin"){return{role:"super_admin",addedAdminsReadable:true,seedAdminCount:seed.admins.length}}try{const{rows}=await read();const explicitRole=caller?effectiveRole({seed,stored:rows,email:caller}):"consumer";const groupRole=caller?await readGroupRole(caller):null;const role=groupRole?highestRole(explicitRole,groupRole):explicitRole;return{role,addedAdminsReadable:true,seedAdminCount:seed.admins.length}}catch(error){console.warn(`[admin] The stored roster could not be read (${error.message}), so this request has no stored roles to check against and resolves at the seed floor. Seed administrators are unaffected. An unreadable roster denies rather than admits.`);return{role:floor,addedAdminsReadable:false,seedAdminCount:seed.admins.length}}}async function resolveRole(store,email){return resolveRoleFrom(email,()=>readRoster(store),groupRoleLookupForStore(store))}async function resolveRoleForRequest(store,req,readEmail){return resolveRoleFrom(readEmail(req),()=>readRosterForRequest(store,req),groupRoleLookupForStore(store))}async function rolePayload(store,email){const{role,addedAdminsReadable,seedAdminCount}=await resolveRole(store,email);return{role,addedAdminsReadable,seedAdminCount}}var ADMIN_ROUTE_PREFIXES=["/api/monitoring","/api/ops","/api/admins","/api/benchmarks","/api/users","/api/egress/admin","/api/settings/values","/api/settings/connections","/api/settings/apply","/api/settings/resource-tags","/api/admin"];var SUPER_ADMIN_ROUTE_PREFIXES=["/api/users"];function matchesPrefix(path,prefixes){const lowered=path.toLowerCase();return prefixes.some(prefix=>lowered===prefix||lowered.startsWith(`${prefix}/`))}function isAdminRoute(path){return matchesPrefix(path,ADMIN_ROUTE_PREFIXES)}function isSuperAdminRoute(path){return matchesPrefix(path,SUPER_ADMIN_ROUTE_PREFIXES)}var ADMIN_REQUIRED_BODY={error:"admin_role_required",detail:"This deployment restricts this to its administrators, and you are not one."};var SUPER_ADMIN_REQUIRED_BODY={error:"super_admin_role_required",detail:"This deployment restricts changing roles to its super administrator."};function requireAdmin(store,readEmail){return function refuseNonAdmins(req,res,next){if(!isAdminRoute(req.path)){next();return}let caller;try{caller=readEmail(req)}catch{res.status(403).json(ADMIN_REQUIRED_BODY);return}resolveRoleForRequest(store,req,()=>caller).then(resolution=>{if(canMutateConnections(resolution.role)){next();return}console.warn(`[admin] REFUSED ${req.method} ${req.path}: the caller is not an administrator of this deployment. Expected whenever a consumer follows a link to an admin surface; the page they land on says so.`);res.status(403).json(ADMIN_REQUIRED_BODY)}).catch(error=>{console.error(`[admin] REFUSED ${req.method} ${req.path}: the role could not be established (${error.message}). Denying rather than admitting, because an unresolved role is not evidence of one.`);res.status(403).json(ADMIN_REQUIRED_BODY)})}}function requireSuperAdmin(store,readEmail){return function refuseNonSuperAdmins(req,res,next){if(!isSuperAdminRoute(req.path)){next();return}let caller;try{caller=readEmail(req)}catch{res.status(403).json(SUPER_ADMIN_REQUIRED_BODY);return}resolveRoleForRequest(store,req,()=>caller).then(resolution=>{if(opensUserRoster(resolution.role)){next();return}console.warn(`[admin] REFUSED ${req.method} ${req.path}: the caller does not hold the super administrator role of this deployment. Expected whenever an administrator reaches the roster; the panel is not drawn for them.`);res.status(403).json(SUPER_ADMIN_REQUIRED_BODY)}).catch(error=>{console.error(`[admin] REFUSED ${req.method} ${req.path}: the role could not be established (${error.message}). Denying rather than admitting, because an unresolved role is not evidence of one.`);res.status(403).json(SUPER_ADMIN_REQUIRED_BODY)})}}async function recordAdminAction(store,entry){try{await store.query(`INSERT INTO ${ADMIN_AUDIT_TABLE} (id, actor, action, subject, detail) VALUES ($1, $2, $3, $4, $5)`,[crypto.randomUUID(),normalizeAdminEmail(entry.actor),entry.action,entry.subject,entry.detail]);return true}catch(error){console.error(`[admin] AUDIT ROW NOT WRITTEN for ${entry.action} by ${entry.actor} on ${entry.subject}: ${error.message}. The action itself went ahead. This line is the record.`);return false}}function adminListPayload(input){const you=normalizeAdminEmail(input.reader);const seedRows=input.seed.map(email=>({email,origin:"seed",addedBy:"",addedAt:"",isYou:email===you,removable:false}));const addedRows=input.added.filter(entry=>!input.seed.includes(entry.email)).map(entry=>({email:entry.email,origin:"added",addedBy:entry.addedBy,addedAt:entry.addedAt,isYou:entry.email===you,removable:true}));return{entries:[...seedRows,...addedRows],addedAdminsReadable:input.addedAdminsReadable,seedAdminCount:input.seed.length}}function invalidAdminEmail(raw){const candidate=normalizeAdminEmail(raw);if(!candidate)return"Enter an email address.";if(candidate.length>320)return"That is longer than an email address can be.";if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(candidate))return"That does not look like an email address.";return""}function removalRefusal2(input){const target=normalizeAdminEmail(input.email);if(input.seed.includes(target))return"seed-row";if(!input.added.some(entry=>entry.email===target))return"not-found";if(input.seed.length===0&&input.added.length===1)return"last-admin";return""}var REMOVAL_REFUSAL_DETAIL={"seed-row":"That administrator was set at deployment and cannot be removed here. Edit the bundle variable to change it.","last-admin":"That is the last administrator, and this deployment has no seed administrators to fall back on. Removing them would leave nobody able to open Monitoring, Ops or these settings, and nobody able to appoint anybody. Add another administrator first.","not-found":"That address is not on the list."};async function addAdmin(store,input){const email=normalizeAdminEmail(input.email);const result=await store.query(`INSERT INTO ${ADDED_ADMINS_TABLE} (email, added_by) VALUES ($1, $2)
     ON CONFLICT (email) DO NOTHING
     RETURNING email`,[email,normalizeAdminEmail(input.addedBy)]);const inserted=result.rows.length>0;if(inserted)invalidateRosterCache(store);return inserted}async function removeAdmin(store,email){const result=await store.query(`DELETE FROM ${ADDED_ADMINS_TABLE} WHERE email = $1 RETURNING email`,[normalizeAdminEmail(email)]);const deleted=result.rows.length>0;if(deleted)invalidateRosterCache(store);return deleted}export{SPAN_PERCENTILE_FLOOR,opsCurrentMonthRange,opsDayRange,DATA_GENIE_TABLES,qualifyDataContractTables,presentedTokenAge,forwardedUserToken,diagnoseUserToken,looksLikeMissingScope,scopesFromToken,tokenGrantsGenie,statusForOutcome,classifyDenial,UNKNOWN_PRINCIPAL,verifyTableAccess,entitlementLookupVia,accessDependenciesFrom,limitsOfThisCheck,verifyAccess,isVerified,verificationSummary,warehouseProbeFor,statementRunnerFor,genieSpaceProbeFor,telemetrySchema,readTelemetryDestination,logsTable,grantFor,offMeasurement,uncheckedMeasurement,noHistoryReason,stateFromFailure,buildTelemetryStatement,readTelemetryRows,hasHistory,APP_NAME_ENV,appPageUrl,readAppFacts,normalizeAdminEmail,columnText,GROUP_ROLE_MAPPINGS_TABLE,GROUP_ROLE_MAPPINGS_DDL,readGroupRoleMappings,writeGroupRoleMapping,deleteGroupRoleMapping,ADDED_ADMINS_TABLE,ADMIN_AUDIT_TABLE,ADMIN_GRANTS_TABLE,ADMIN_ROLES_DDL,ROLE_WORD,isRole,opensAdminSurfaces,canMutateConnections,canCheckHealthResources,ROLE_COLUMN,readRoster,readRosterForRequest,writeRole,deleteRosterRow,effectiveRole,everyKnownUser,roleChangeRefusal,removalRefusal,REFUSAL_DETAIL,rosterPayload,roleChangeSentence,workspaceControlPlaneReader,readControlPlaneIdentityMetadata,SEED_ADMIN_EMAILS_ENV,SEED_SUPER_ADMIN_PREFIX,parseSeedAdmins,seedAdminEmails,seedSuperAdminEmails,seedRoles,announceSeedAdmins,bootstrapSeedRoles,readAddedAdmins,resolveRole,resolveRoleForRequest,rolePayload,ADMIN_ROUTE_PREFIXES,SUPER_ADMIN_ROUTE_PREFIXES,isAdminRoute,isSuperAdminRoute,ADMIN_REQUIRED_BODY,SUPER_ADMIN_REQUIRED_BODY,requireAdmin,requireSuperAdmin,recordAdminAction,adminListPayload,invalidAdminEmail,removalRefusal2,REMOVAL_REFUSAL_DETAIL,addAdmin,removeAdmin};
