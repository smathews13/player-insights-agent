
import{isPlayerInsightsAgentQueryTag}from"./chunk-W7NKQP7B.mjs";var BILLING_TAG={key:"system_billing",value:"player-insights-agent"};var RETIRED_BILLING_TAG_KEY="astrolabe";function billingTagPair(){return`${BILLING_TAG.key}=${BILLING_TAG.value}`}var ACTIVE_QUERY_STATUSES=["QUEUED","STARTED","COMPILING","COMPILED","RUNNING"];var ACTIVE_STATUS_SET=new Set(ACTIVE_QUERY_STATUSES);var DEFAULT_SWEEP_DELAY_MS=500;var QUERY_HISTORY_PAGE_SIZE=100;var MAX_CANCELLATION_HISTORY_PAGES=8;var CANCELLATION_LOOKBACK_MS=7*24*60*60*1e3;var CANCELLATION_DEADLINE_MS=1e4;function stringValue(value){return typeof value==="string"&&value.length>0?value:void 0}function parseQueryTags(value){const tags=new Map;if(Array.isArray(value)){for(const item of value){if(!item||typeof item!=="object")continue;const record=item;const key=stringValue(record.key);const tagValue=stringValue(record.value);if(!key||!tagValue)continue;const existing=tags.get(key);if(existing!==void 0&&existing!==tagValue)return new Map;tags.set(key,tagValue)}return tags}if(!value||typeof value!=="object")return tags;for(const[key,candidate]of Object.entries(value)){const tagValue=stringValue(candidate);if(tagValue!==void 0)tags.set(key,tagValue)}return tags}function sameEmail(left,right){return left?.trim().toLocaleLowerCase("en-US")===right.trim().toLocaleLowerCase("en-US")}function matchesScope(row,scope){const tags=parseQueryTags(row.query_tags);if(!isPlayerInsightsAgentQueryTag(tags.get("application")))return false;if(scope.mode==="admin")return true;const executionUser=stringValue(row.executed_as_user_name)??stringValue(row.user_name);if(!sameEmail(executionUser,scope.signedInEmail))return false;return scope.runId!==void 0&&tags.get("run_id")===scope.runId||scope.correlationId!==void 0&&tags.get("correlation_id")===scope.correlationId}function returnedActiveStatus(row,requested){const status=stringValue(row.status);if(status===void 0)return requested;return ACTIVE_STATUS_SET.has(status)?status:void 0}function errorStatus(error){if(!error||typeof error!=="object")return void 0;const record=error;const direct=record.statusCode??record.status;if(typeof direct==="number"&&Number.isFinite(direct))return direct;const response=record.response;if(response&&typeof response==="object"){const nested=response.status;if(typeof nested==="number"&&Number.isFinite(nested))return nested}return void 0}function errorWording(error){if(typeof error==="string")return error;if(!error||typeof error!=="object")return"";const record=error;const pieces=[record.message,record.error_code,record.errorCode];const response=record.response;if(response&&typeof response==="object"){const nested=response;pieces.push(nested.message,nested.error_code,nested.errorCode)}return pieces.filter(piece=>typeof piece==="string").join(" ")}function classifyCancellationError(error){const providerStatus=errorStatus(error);const wording=errorWording(error);const alreadyFinished=providerStatus===409||/\bnot(?: currently)? running\b|\bnot in (?:an? )?(?:running|active) state\b|\bno longer (?:running|active)\b|\balready (?:finished|completed|canceled|cancelled)\b/i.test(wording);if(alreadyFinished)return{outcome:"already_finished_or_raced",providerStatus};if(providerStatus===401||providerStatus===403)return{outcome:"refused",providerStatus};return{outcome:"failed",providerStatus}}async function defaultSleep(milliseconds){await new Promise(resolve=>setTimeout(resolve,milliseconds))}var CancellationDeadlineError=class extends Error{constructor(){super("SQL cancellation lookup deadline reached.");this.name="CancellationDeadlineError"}};function abortReason(signal){return signal.reason instanceof Error?signal.reason:new Error("SQL cancellation lookup aborted.")}function abortable(work,signal){if(signal.aborted)return Promise.reject(abortReason(signal));return new Promise((resolve,reject)=>{const onAbort=()=>{signal.removeEventListener("abort",onAbort);reject(abortReason(signal))};signal.addEventListener("abort",onAbort,{once:true});work.then(value=>{signal.removeEventListener("abort",onAbort);resolve(value)},error=>{signal.removeEventListener("abort",onAbort);reject(error instanceof Error?error:new Error(String(error)))})})}async function scanPass(input){const candidates=new Map;if(input.coverage.pagesRead>=input.coverage.maxPages){input.coverage.complete=false;input.coverage.reason="page-cap";return candidates}let pageToken;const usedTokens=new Set;while(input.coverage.pagesRead<input.coverage.maxPages){let response;try{response=await abortable(input.transport.listQueries({warehouseId:input.warehouseId,status:"RUNNING",statuses:ACTIVE_QUERY_STATUSES,startTimeMs:input.startTimeMs,endTimeMs:input.endTimeMs,pageToken,maxResults:QUERY_HISTORY_PAGE_SIZE,signal:input.signal}),input.signal)}catch{input.coverage.complete=false;input.coverage.reason=input.signal.aborted?input.signal.reason instanceof CancellationDeadlineError?"deadline":"caller-abort":"transport-error";break}input.coverage.pagesRead+=1;const rows=Array.isArray(response.res)?response.res:[];input.coverage.rowsRead+=rows.length;for(const row of rows){const queryId=stringValue(row.query_id);const activeStatus=returnedActiveStatus(row,"RUNNING");if(!queryId||!activeStatus)continue;if(row.warehouse_id!==void 0&&row.warehouse_id!==input.warehouseId)continue;if(matchesScope(row,input.scope))candidates.set(queryId,activeStatus)}const nextPageToken=stringValue(response.next_page_token);if(!nextPageToken){if(response.has_next_page){input.coverage.complete=false;input.coverage.reason="missing-page-token"}break}if(usedTokens.has(nextPageToken)){input.coverage.complete=false;input.coverage.reason="repeated-page-token";break}usedTokens.add(nextPageToken);pageToken=nextPageToken;if(input.coverage.pagesRead>=input.coverage.maxPages){input.coverage.complete=false;input.coverage.reason="page-cap";break}}return candidates}async function cancelAstrolabeWarehouseQueries(input){const warehouseId=input.warehouseId.trim();if(!warehouseId)throw new Error("A configured SQL warehouse ID is required.");const sleep=input.sleep??defaultSleep;const sweepDelayMs=Math.max(0,input.sweepDelayMs??DEFAULT_SWEEP_DELAY_MS);const now=input.now?.()??Date.now();const maxPages=Math.max(1,Math.min(MAX_CANCELLATION_HISTORY_PAGES,Math.floor(input.maxPages??MAX_CANCELLATION_HISTORY_PAGES)));const deadlineMs=Math.max(1,Math.min(CANCELLATION_DEADLINE_MS,input.deadlineMs??CANCELLATION_DEADLINE_MS));const controller=new AbortController;const parentAbort=()=>controller.abort(input.signal?.reason);if(input.signal?.aborted)parentAbort();else input.signal?.addEventListener("abort",parentAbort,{once:true});const timer=setTimeout(()=>controller.abort(new CancellationDeadlineError),deadlineMs);timer.unref?.();const coverage={complete:true,queriedRange:{from:new Date(now-CANCELLATION_LOOKBACK_MS).toISOString(),to:new Date(now).toISOString()},rowsRead:0,pagesRead:0,passesRead:0,maxPages,reason:"complete"};const matched=new Set;const attempted=new Set;const details=[];try{for(let pass=0;pass<2&&coverage.complete&&!controller.signal.aborted;pass+=1){const candidates=await scanPass({warehouseId,scope:input.scope,transport:input.transport,startTimeMs:now-CANCELLATION_LOOKBACK_MS,endTimeMs:now,signal:controller.signal,coverage});coverage.passesRead+=1;for(const queryId of candidates.keys())matched.add(queryId);for(const[queryId,queryStatus]of candidates){if(attempted.has(queryId))continue;attempted.add(queryId);try{await abortable(input.transport.cancelStatement(queryId,controller.signal),controller.signal);details.push({query_id:queryId,query_status:queryStatus,outcome:"cancel_requested"})}catch(error){if(controller.signal.aborted){coverage.complete=false;coverage.reason=controller.signal.reason instanceof CancellationDeadlineError?"deadline":"caller-abort";break}const classified=classifyCancellationError(error);details.push({query_id:queryId,query_status:queryStatus,outcome:classified.outcome,...classified.providerStatus===void 0?{}:{provider_status:classified.providerStatus}})}}if(pass===0&&coverage.complete){try{await abortable(sleep(sweepDelayMs),controller.signal)}catch{coverage.complete=false;coverage.reason=controller.signal.reason instanceof CancellationDeadlineError?"deadline":"caller-abort"}}}}finally{clearTimeout(timer);input.signal?.removeEventListener("abort",parentAbort)}if(controller.signal.aborted&&coverage.complete){coverage.complete=false;coverage.reason=controller.signal.reason instanceof CancellationDeadlineError?"deadline":"caller-abort"}const count=outcome=>details.filter(detail=>detail.outcome===outcome).length;return{matched:matched.size,cancel_requested:count("cancel_requested"),already_finished_or_raced:count("already_finished_or_raced"),refused:count("refused"),failed:count("failed"),details,coverage}}function queryHistoryPage(value){if(!value||typeof value!=="object")return{};const record=value;return{res:Array.isArray(record.res)?record.res:[],...typeof record.next_page_token==="string"?{next_page_token:record.next_page_token}:{},...typeof record.has_next_page==="boolean"?{has_next_page:record.has_next_page}:{}}}function createDatabricksWarehouseCancellationTransport(client){return{async listQueries({warehouseId,status,statuses,startTimeMs,endTimeMs,pageToken,maxResults,signal}){const end=endTimeMs??Date.now();const response=await client.request({path:"/api/2.0/sql/history/queries",method:"GET",headers:new Headers({Accept:"application/json"}),raw:false,query:{filter_by:{warehouse_ids:[warehouseId],statuses:[...statuses??[status]],query_start_time_range:{start_time_ms:startTimeMs??end-CANCELLATION_LOOKBACK_MS,end_time_ms:end}},include_metrics:false,max_results:maxResults,...pageToken?{page_token:pageToken}:{}},...signal?{signal}:{}});return queryHistoryPage(response)},async cancelStatement(statementId,signal){await client.request({path:`/api/2.0/sql/statements/${encodeURIComponent(statementId)}/cancel`,method:"POST",headers:new Headers,raw:false,...signal?{signal}:{}})}}}async function createWorkspaceWarehouseCancellationTransport(input={}){const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=input.token?new WorkspaceClient({host:input.host,token:input.token,authType:"pat"}):new WorkspaceClient({});return createDatabricksWarehouseCancellationTransport({request:options=>client.apiClient.request(options)})}var QUERY_HISTORY_PAGE_SIZE2=999;var MAX_QUERY_HISTORY_PAGES=40;var MAX_QUERY_HISTORY_RANGE_MS=30*24*60*60*1e3;var MAX_QUERY_HISTORY_TOTAL_RANGE_MS=366*24*60*60*1e3;var QUERY_HISTORY_DEADLINE_MS=2e4;var EMPTY_WAREHOUSE_QUERY_ATTRIBUTION={complete:false,astrolabeQueries:0,totalQueries:0,astrolabeExecutionMs:0,totalExecutionMs:0,askRuns:[],genieSpaces:[],users:[],coverage:{state:"unavailable",requestedRange:null,queriedRange:null,rowsRead:0,pagesRead:0,chunksRead:0,reasons:["invalid-range"]}};function finiteMilliseconds(value){const parsed=typeof value==="number"?value:typeof value==="string"?Number(value):Number.NaN;return Number.isFinite(parsed)&&parsed>=0?parsed:null}function executionMilliseconds(row){if(row.metrics&&typeof row.metrics==="object"){const executionTime=finiteMilliseconds(row.metrics.execution_time_ms);if(executionTime!==null)return executionTime}return finiteMilliseconds(row.duration)}function rowId(row){return typeof row.query_id==="string"?row.query_id.trim():""}function interactiveRunId(row,runIds){const tags=parseQueryTags(row.query_tags);if(!isPlayerInsightsAgentQueryTag(tags.get("application"))||tags.get("surface")!=="ask")return"";for(const key of["run_id","correlation_id"]){const candidate=tags.get(key)??"";const runId=runIds.get(candidate);if(runId)return runId}return""}function isAppOperation(row){const tags=parseQueryTags(row.query_tags);return isPlayerInsightsAgentQueryTag(tags.get("application"))}function genieSpaceId(row){if(!row.query_source||typeof row.query_source!=="object")return"";const value=row.query_source.genie_space_id;return typeof value==="string"?value.trim():""}function attributableUser(row){const executedBy=typeof row.user_name==="string"?row.user_name.trim().toLowerCase():"";const executedAs=typeof row.executed_as_user_name==="string"?row.executed_as_user_name.trim().toLowerCase():"";if(!executedBy.includes("@")||executedBy!==executedAs)return"";return executedBy}var QueryHistoryDeadlineError=class extends Error{constructor(){super("Query History read deadline reached.");this.name="QueryHistoryDeadlineError"}};function isoTimestamp(value){if(!Number.isFinite(value)||Math.abs(value)>864e13)return null;return new Date(value).toISOString()}function addReason(coverage,reason){if(!coverage.reasons.includes(reason))coverage.reasons.push(reason)}function abortReason2(signal){return signal.reason instanceof Error?signal.reason:new Error("Query History read aborted.")}function abortable2(work,signal){if(signal.aborted)return Promise.reject(abortReason2(signal));return new Promise((resolve,reject)=>{const onAbort=()=>{signal.removeEventListener("abort",onAbort);reject(abortReason2(signal))};signal.addEventListener("abort",onAbort,{once:true});work.then(value=>{signal.removeEventListener("abort",onAbort);resolve(value)},error=>{signal.removeEventListener("abort",onAbort);reject(error instanceof Error?error:new Error(String(error)))})})}async function readWarehouseQueryAttribution(input){const warehouseId=input.warehouseId.trim();const requestedFrom=isoTimestamp(input.startTimeMs);const requestedTo=isoTimestamp(input.endTimeMs);if(!warehouseId||!requestedFrom||!requestedTo||input.endTimeMs<input.startTimeMs){return{...EMPTY_WAREHOUSE_QUERY_ATTRIBUTION,coverage:EMPTY_WAREHOUSE_QUERY_ATTRIBUTION.coverage}}const coverage={state:"partial",requestedRange:{from:requestedFrom,to:requestedTo},queriedRange:null,rowsRead:0,pagesRead:0,chunksRead:0,reasons:[]};const now=input.now?.()??Date.now();const boundedEnd=Math.min(input.endTimeMs,now);const boundedStart=Math.max(input.startTimeMs,boundedEnd-MAX_QUERY_HISTORY_TOTAL_RANGE_MS+1);if(boundedEnd!==input.endTimeMs||boundedStart!==input.startTimeMs)addReason(coverage,"range-clamped");const boundedFrom=isoTimestamp(boundedStart);const boundedTo=isoTimestamp(boundedEnd);if(!boundedFrom||!boundedTo||boundedEnd<boundedStart){coverage.state="unavailable";addReason(coverage,"invalid-range");return{...EMPTY_WAREHOUSE_QUERY_ATTRIBUTION,coverage}}const maximumPages=Math.max(1,Math.min(MAX_QUERY_HISTORY_PAGES,Math.floor(input.maxPages??MAX_QUERY_HISTORY_PAGES)));const deadlineMs=Math.max(1,Math.min(QUERY_HISTORY_DEADLINE_MS,input.deadlineMs??QUERY_HISTORY_DEADLINE_MS));const controller=new AbortController;const parentAbort=()=>controller.abort(input.signal?.reason);if(input.signal?.aborted)parentAbort();else input.signal?.addEventListener("abort",parentAbort,{once:true});const timer=setTimeout(()=>controller.abort(new QueryHistoryDeadlineError),deadlineMs);timer.unref?.();const seenQueryIds=new Set;let astrolabeQueries=0;let astrolabeExecutionMs=0;let totalExecutionMs=0;const genieSpaces=new Map;const askRuns=new Map;const users=new Map;const runIds=new Map;if(input.interactiveRuns?.some(run=>run.evidenceComplete===false)){addReason(coverage,"interactive-run-coverage")}for(const run of input.interactiveRuns??[]){for(const id of[run.runId,run.requestId??"",run.correlationId]){if(id)runIds.set(id,run.runId)}}let totalQueries=0;const aggregate=row=>{if(row.warehouse_id!==void 0&&row.warehouse_id!==warehouseId){addReason(coverage,"unexpected-warehouse");return}const id=rowId(row);if(!id){addReason(coverage,"invalid-row");return}if(seenQueryIds.has(id))return;seenQueryIds.add(id);totalQueries+=1;const spaceId=genieSpaceId(row);const interactiveRun=!spaceId?interactiveRunId(row,runIds):"";const astrolabe=!spaceId&&isAppOperation(row);if(astrolabe)astrolabeQueries+=1;if(spaceId){const current=genieSpaces.get(spaceId)??{queries:0,executionMs:0};current.queries+=1;genieSpaces.set(spaceId,current)}const duration=executionMilliseconds(row);if(duration===null){addReason(coverage,"missing-execution-time");return}totalExecutionMs+=duration;if(astrolabe)astrolabeExecutionMs+=duration;if(interactiveRun)askRuns.set(interactiveRun,(askRuns.get(interactiveRun)??0)+duration);if(spaceId)genieSpaces.get(spaceId).executionMs+=duration;const user=attributableUser(row);if(user){const current=users.get(user)??{astrolabeExecutionMs:0,askExecutionMs:0,genieSpaces:new Map};if(astrolabe)current.astrolabeExecutionMs+=duration;if(interactiveRun)current.askExecutionMs+=duration;if(spaceId)current.genieSpaces.set(spaceId,(current.genieSpaces.get(spaceId)??0)+duration);users.set(user,current)}};let stop=controller.signal.aborted;try{for(let windowStart=boundedStart;windowStart<=boundedEnd&&!stop;){if(coverage.pagesRead>=maximumPages){addReason(coverage,"page-cap");break}const windowEnd=Math.min(boundedEnd,windowStart+MAX_QUERY_HISTORY_RANGE_MS-1);let pageToken;const usedTokens=new Set;coverage.chunksRead+=1;coverage.queriedRange={from:coverage.queriedRange?.from??new Date(windowStart).toISOString(),to:new Date(windowEnd).toISOString()};for(;;){if(controller.signal.aborted){stop=true;break}if(coverage.pagesRead>=maximumPages){addReason(coverage,"page-cap");stop=true;break}let response;try{response=await abortable2(input.transport.listQueries({warehouseId,startTimeMs:windowStart,endTimeMs:windowEnd,pageToken,maxResults:QUERY_HISTORY_PAGE_SIZE2,signal:controller.signal}),controller.signal)}catch{if(controller.signal.aborted){addReason(coverage,controller.signal.reason instanceof QueryHistoryDeadlineError?"deadline":"caller-abort")}else{addReason(coverage,"transport-error")}stop=true;break}coverage.pagesRead+=1;const pageRows=Array.isArray(response.res)?response.res:[];coverage.rowsRead+=pageRows.length;for(const row of pageRows)aggregate(row);const next=typeof response.next_page_token==="string"?response.next_page_token.trim():"";if(!next){if(response.has_next_page){addReason(coverage,"missing-page-token");stop=true}break}if(usedTokens.has(next)){addReason(coverage,"repeated-page-token");stop=true;break}usedTokens.add(next);pageToken=next;if(coverage.pagesRead>=maximumPages){addReason(coverage,"page-cap");stop=true;break}}if(windowEnd>=boundedEnd||stop)break;windowStart=windowEnd+1}}finally{clearTimeout(timer);input.signal?.removeEventListener("abort",parentAbort)}if(controller.signal.aborted&&coverage.reasons.length===0){addReason(coverage,controller.signal.reason instanceof QueryHistoryDeadlineError?"deadline":"caller-abort")}coverage.state=coverage.reasons.length===0?"complete":"partial";return{complete:coverage.state==="complete",astrolabeQueries,totalQueries,astrolabeExecutionMs,totalExecutionMs,askRuns:[...askRuns].sort(([left],[right])=>left.localeCompare(right)).map(([runId,executionMs])=>({runId,executionMs})),genieSpaces:[...genieSpaces].map(([spaceId,values])=>({spaceId,...values})),users:[...users].sort(([left],[right])=>left.localeCompare(right)).map(([email,values])=>({email,astrolabeExecutionMs:values.astrolabeExecutionMs,askExecutionMs:values.askExecutionMs,genieSpaces:[...values.genieSpaces].sort(([left],[right])=>left.localeCompare(right)).map(([spaceId,executionMs])=>({spaceId,executionMs}))})),coverage}}function queryHistoryPage2(value){if(!value||typeof value!=="object")return{};const record=value;return{res:Array.isArray(record.res)?record.res:[],...typeof record.next_page_token==="string"?{next_page_token:record.next_page_token}:{},...typeof record.has_next_page==="boolean"?{has_next_page:record.has_next_page}:{}}}function createDatabricksQueryHistoryTransport(client){return{async listQueries({warehouseId,startTimeMs,endTimeMs,pageToken,maxResults,signal}){const response=await client.request({path:"/api/2.0/sql/history/queries",method:"GET",headers:new Headers({Accept:"application/json"}),raw:false,query:{filter_by:{warehouse_ids:[warehouseId],query_start_time_range:{start_time_ms:startTimeMs,end_time_ms:endTimeMs}},include_metrics:true,max_results:maxResults,...pageToken?{page_token:pageToken}:{}},signal});return queryHistoryPage2(response)}}}async function createWorkspaceQueryHistoryTransport(input){const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({host:input.host,token:input.token,authType:"pat"});return createDatabricksQueryHistoryTransport({request:options=>client.apiClient.request(options)})}var COST_COMPONENTS=["serving-endpoint","sql-warehouse","genie","vector-search","app-compute"];function readResourceActivityRows(rows){const allowed=new Set(["genie:data","genie:dictionary","vector-search"]);const parsed=[];for(const row of rows){const tileId=typeof row.tile_id==="string"?row.tile_id:"";if(!allowed.has(tileId))continue;const calls=Number(row.astrolabe_calls);const observedCalls=Number(row.observed_calls);if(!Number.isFinite(calls)||calls<0||!Number.isFinite(observedCalls)||observedCalls<0)continue;parsed.push({tileId,calls,observedCalls})}return parsed}var WORKSPACE_ESTIMATE_SUFFIX=":workspace";function workspaceEstimateRow(component){return`${component}${WORKSPACE_ESTIMATE_SUFFIX}`}var MATCHERS={"serving-endpoint":{product:"MODEL_SERVING",column:"u.usage_metadata.endpoint_name",parameter:"endpointName",type:"STRING"},"sql-warehouse":{product:"SQL",column:"u.usage_metadata.warehouse_id",parameter:"warehouseId",type:"STRING"},genie:{product:"GENIE",column:null,parameter:"workspaceId",type:"STRING"},"vector-search":{product:"VECTOR_SEARCH",column:"u.usage_metadata.endpoint_name",parameter:"vectorEndpoint",type:"STRING"},"app-compute":{product:"APPS",column:"u.usage_metadata.app_name",parameter:"appName",type:"STRING"}};var RANGE_ROW="__range";var BILLING_TAG_KEY=BILLING_TAG.key;var BILLING_TAG_VALUE=BILLING_TAG.value;var LIST_PRICE_SOURCE="system.billing.list_prices";var GENIE_LLM_UNAVAILABLE="Genie LLM dollars unavailable by space: billing exposes surface and channel, not a Genie space ID.";var GENIE_SQL_NOT_COMPLETE="Only generated SQL is estimated from Query History execution time; Genie model cost is excluded.";var TILED_PRODUCTS=new Set(["MODEL_SERVING","SQL","VECTOR_SEARCH","APPS"]);var PRODUCT_REASONS={MODEL_SERVING:"Measured only when an exact tracked endpoint name matches; tag coverage is reported separately.",SQL:"Warehouse billing rows are allocated only by complete Player Insights Agent Query History execution-time share.",VECTOR_SEARCH:"Exact endpoint billing is allocated with configured-index activity when the endpoint is shared.",APPS:"Measured by exact app name. App tag presence is a separate organizational signal.",GENIE:GENIE_LLM_UNAVAILABLE,LAKEBASE:"Lakebase can be tagged. No documented billing join exists in this model.",MLFLOW:"MLflow experiments can be tagged. They have no Cost tile."};var EMPTY_PRICING={source:"list_prices",match:"none",currency:"",pricedQuantity:0,unpricedQuantity:0,pricedRows:0,unpricedRows:0,unpricedSkus:[],duplicateMatches:0,correctionRows:0,priceEffectiveAt:""};function canAsk(component,ids){if(!ids.workspaceId)return false;if(component==="genie")return false;if(component==="vector-search"){return Boolean(vectorIndexName(ids.vectorIndex)&&ids.vectorEndpoint&&ids.vectorEndpointIndexCount===1)}return Boolean(ids[MATCHERS[component].parameter])}function resourceIdFor(component,ids){switch(component){case"serving-endpoint":return ids.endpointName;case"sql-warehouse":return ids.warehouseId;case"app-compute":return ids.appName;case"vector-search":return vectorIndexName(ids.vectorIndex)||ids.vectorEndpoint;case"genie":return""}}function resourceKindFor(component,ids){switch(component){case"serving-endpoint":return"serving-endpoint";case"sql-warehouse":return"sql-warehouse";case"app-compute":return"app";case"vector-search":return vectorIndexName(ids.vectorIndex)?"vector-index":ids.vectorEndpoint?"vector-endpoint":"";case"genie":return""}}function vectorIndexName(raw){const name=raw.trim();const parts=name.split(".").filter(piece=>piece.length>0);return parts.length===3?name:""}function buildCostStatement(ids,range){const covered=COST_COMPONENTS.filter(component=>canAsk(component,ids));const estimated=[];if(covered.length===0&&estimated.length===0)return null;const parameters=[];const branches=[];const bound=new Set;const bind=(marker,value,type)=>{if(bound.has(marker))return;parameters.push({name:marker,value,type});bound.add(marker)};bind("from_day",range.from,"DATE");bind("to_day",range.to,"DATE");if(range.fromTimestamp)bind("from_instant",range.fromTimestamp,"TIMESTAMP");if(ids.workspaceId)bind("workspaceId",ids.workspaceId,"STRING");for(const component of covered){const matcher=MATCHERS[component];const marker=String(matcher.parameter);bind(marker,ids[matcher.parameter],matcher.type);const predicate=matcher.column===null?`u.workspace_id = :${marker}`:`${matcher.column} = :${marker}`;branches.push(`      WHEN u.billing_origin_product = '${matcher.product}' AND ${predicate} THEN '${component}'`)}for(const component of estimated){const matcher=MATCHERS[component];bind("workspaceId",ids.workspaceId,"STRING");branches.push(`      WHEN u.billing_origin_product = '${matcher.product}' AND u.workspace_id = :workspaceId AND u.custom_tags['${BILLING_TAG.key}'] = '${BILLING_TAG.value}' THEN '${workspaceEstimateRow(component)}'`)}const resourcePredicates=[];if(ids.warehouseId){resourcePredicates.push(`(u.billing_origin_product = 'SQL' AND u.usage_metadata.warehouse_id = :warehouseId)`)}if(ids.endpointName&&canAsk("serving-endpoint",ids)){resourcePredicates.push(`(u.billing_origin_product = 'MODEL_SERVING' AND u.usage_metadata.endpoint_name = :endpointName)`)}if(ids.appName){resourcePredicates.push(`(u.billing_origin_product = 'APPS' AND u.usage_metadata.app_name = :appName)`)}if(canAsk("vector-search",ids)){resourcePredicates.push(`(u.billing_origin_product = 'VECTOR_SEARCH' AND u.usage_metadata.endpoint_name = :vectorEndpoint)`)}const leakPredicate=resourcePredicates.length>0?resourcePredicates.join("\n     OR "):"FALSE";const requestedComponents=covered.map(component=>`('${component}')`).join(",\n    ");const deploymentStart=ids.appName?`SELECT COALESCE(MIN(u.usage_date), :from_day) AS source_from
  FROM system.billing.usage u
  WHERE u.workspace_id = :workspaceId
    AND u.billing_origin_product = 'APPS'
    AND u.usage_metadata.app_name = :appName
    AND u.usage_date <= :to_day`:"SELECT :from_day AS source_from";const statement=`WITH deployment_start AS (
  ${deploymentStart}
),
requested_components(component) AS (
  VALUES
    ${requestedComponents}
),
tagged AS (
  SELECT
    u.usage_date,
    u.usage_quantity,
    u.sku_name,
    u.cloud,
    u.usage_unit,
    u.usage_start_time,
    u.usage_end_time,
    u.workspace_id,
    u.billing_origin_product,
    COALESCE(
      CAST(u.record_id AS STRING),
      CONCAT_WS('|', CAST(u.workspace_id AS STRING), u.sku_name, CAST(u.usage_start_time AS STRING), CAST(u.usage_end_time AS STRING))
    ) AS record_id,
    COALESCE(u.record_type, 'ORIGINAL') AS record_type,
    COALESCE(u.custom_tags['${BILLING_TAG.key}'] = '${BILLING_TAG.value}', FALSE) AS tag_matches,
    CASE
${branches.join("\n")}
      ELSE NULL
    END AS component
  FROM system.billing.usage u
  WHERE u.usage_date >= GREATEST(:from_day, (SELECT source_from FROM deployment_start))
    AND u.usage_date <= :to_day
    ${range.fromTimestamp?"AND u.usage_start_time >= :from_instant":""}
    AND u.workspace_id = :workspaceId
    AND u.billing_origin_product <> 'JOBS'
    AND (
      u.custom_tags['${BILLING_TAG.key}'] = '${BILLING_TAG.value}'
      OR ${leakPredicate}
    )
),
price_hits AS (
  SELECT
    t.*,
    p.pricing.default AS unit_price,
    p.currency_code,
    p.price_start_time,
    COUNT(p.sku_name) OVER (PARTITION BY t.record_id) AS price_match_count
  FROM tagged t
  LEFT JOIN system.billing.list_prices p
    ON t.sku_name = p.sku_name
   AND t.cloud = p.cloud
   AND t.usage_unit = p.usage_unit
   AND t.usage_end_time >= p.price_start_time
   AND (p.price_end_time IS NULL OR t.usage_end_time < p.price_end_time)
),
deduped AS (
  SELECT
    record_id,
    usage_date,
    usage_quantity,
    usage_unit,
    usage_start_time,
    usage_end_time,
    sku_name,
    billing_origin_product,
    component,
    record_type,
    tag_matches,
    MAX(price_match_count) AS price_match_count,
    MAX(unit_price) AS unit_price,
    MAX(currency_code) AS currency_code,
    MAX(CAST(price_start_time AS STRING)) AS price_start_time
  FROM price_hits
  GROUP BY record_id, usage_date, usage_quantity, usage_unit, usage_start_time, usage_end_time, sku_name, billing_origin_product, component, record_type, tag_matches
),
priced AS (
  SELECT
    *,
    CASE
      WHEN unit_price IS NOT NULL AND price_match_count = 1 THEN usage_quantity * unit_price
      ELSE CAST(NULL AS DOUBLE)
    END AS spend,
    CASE
      WHEN price_match_count > 1 THEN 'duplicate'
      WHEN unit_price IS NULL THEN 'unpriced'
      ELSE 'priced'
    END AS row_match
  FROM deduped
)
SELECT
  'component' AS row_kind,
  requested.component AS key,
  COALESCE(SUM(priced.spend), 0) AS spend,
  CASE WHEN COUNT(DISTINCT CASE WHEN priced.currency_code IS NOT NULL THEN priced.currency_code END) = 1
       THEN MAX(priced.currency_code) ELSE CAST('' AS STRING) END AS currency,
  COUNT(DISTINCT CASE WHEN priced.currency_code IS NOT NULL THEN priced.currency_code END) AS currency_count,
  COUNT(DISTINCT priced.usage_date) AS billed_days,
  CAST(NULL AS BIGINT) AS job_runs,
  MAX(priced.usage_date) AS last_day,
  COALESCE(SUM(CASE WHEN priced.row_match = 'priced' THEN priced.usage_quantity ELSE 0 END), 0) AS priced_quantity,
  COALESCE(SUM(CASE WHEN priced.row_match <> 'priced' THEN priced.usage_quantity ELSE 0 END), 0) AS unpriced_quantity,
  COUNT(priced.record_id) FILTER (WHERE priced.row_match = 'priced') AS priced_rows,
  COUNT(priced.record_id) FILTER (WHERE priced.row_match <> 'priced') AS unpriced_rows,
  array_join(collect_set(CASE WHEN priced.row_match <> 'priced' THEN priced.sku_name END), ',') AS unpriced_skus,
  CASE
    WHEN COUNT(priced.record_id) = 0 THEN 'none'
    WHEN COUNT(priced.record_id) FILTER (WHERE priced.row_match = 'duplicate') > 0 THEN 'duplicate'
    WHEN COUNT(DISTINCT CASE WHEN priced.currency_code IS NOT NULL THEN priced.currency_code END) > 1 THEN 'mixed-currency'
    WHEN COUNT(priced.record_id) FILTER (WHERE priced.row_match = 'unpriced') > 0
     AND COUNT(priced.record_id) FILTER (WHERE priced.row_match = 'priced') > 0 THEN 'partial'
    WHEN COUNT(priced.record_id) FILTER (WHERE priced.row_match = 'priced') = 0 THEN 'unpriced'
    ELSE 'priced'
  END AS price_match_status,
  COUNT(priced.record_id) FILTER (
    WHERE priced.record_type ILIKE '%CORRECT%' OR priced.usage_quantity < 0
  ) AS correction_rows,
  COUNT(priced.record_id) FILTER (WHERE priced.price_match_count > 1) AS duplicate_matches,
  MAX(priced.price_start_time) AS price_effective_at,
  COUNT(priced.record_id) FILTER (WHERE priced.tag_matches) AS tagged_rows,
  COUNT(priced.record_id) FILTER (WHERE NOT priced.tag_matches) AS untagged_rows,
  COUNT(DISTINCT priced.usage_unit) AS usage_unit_count,
  COALESCE(SUM(CASE WHEN UPPER(TRIM(priced.usage_unit)) = 'DBU' THEN priced.usage_quantity ELSE 0 END), 0) AS dbu_quantity,
  COUNT(priced.record_id) FILTER (WHERE UPPER(TRIM(priced.usage_unit)) = 'DBU') AS dbu_rows,
  COALESCE(SUM(
    CASE WHEN priced.row_match = 'priced'
      THEN GREATEST(0, UNIX_MILLIS(priced.usage_end_time) - UNIX_MILLIS(priced.usage_start_time)) / 1000.0
      ELSE 0 END
  ), 0) AS billed_seconds
  , MIN(priced.usage_date) AS source_from
FROM requested_components requested
LEFT JOIN priced ON priced.component = requested.component
GROUP BY requested.component
UNION ALL
SELECT
  'coverage' AS row_kind,
  billing_origin_product AS key,
  SUM(spend) AS spend,
  CASE WHEN COUNT(DISTINCT CASE WHEN currency_code IS NOT NULL THEN currency_code END) = 1
       THEN MAX(currency_code) ELSE CAST('' AS STRING) END AS currency,
  COUNT(DISTINCT CASE WHEN currency_code IS NOT NULL THEN currency_code END) AS currency_count,
  COUNT(DISTINCT usage_date) AS billed_days,
  CAST(NULL AS BIGINT) AS job_runs,
  MAX(usage_date) AS last_day,
  SUM(CASE WHEN row_match = 'priced' THEN usage_quantity ELSE 0 END) AS priced_quantity,
  SUM(CASE WHEN row_match <> 'priced' THEN usage_quantity ELSE 0 END) AS unpriced_quantity,
  COUNT(*) FILTER (WHERE row_match = 'priced') AS priced_rows,
  COUNT(*) FILTER (WHERE row_match <> 'priced') AS unpriced_rows,
  array_join(collect_set(CASE WHEN row_match <> 'priced' THEN sku_name END), ',') AS unpriced_skus,
  CASE
    WHEN COUNT(*) FILTER (WHERE row_match = 'duplicate') > 0 THEN 'duplicate'
    WHEN COUNT(DISTINCT CASE WHEN currency_code IS NOT NULL THEN currency_code END) > 1 THEN 'mixed-currency'
    WHEN COUNT(*) FILTER (WHERE row_match = 'unpriced') > 0 AND COUNT(*) FILTER (WHERE row_match = 'priced') > 0 THEN 'partial'
    WHEN COUNT(*) FILTER (WHERE row_match = 'priced') = 0 THEN 'unpriced'
    ELSE 'priced'
  END AS price_match_status,
  COUNT(*) FILTER (WHERE record_type ILIKE '%CORRECT%' OR usage_quantity < 0) AS correction_rows,
  COUNT(*) FILTER (WHERE price_match_count > 1) AS duplicate_matches,
  MAX(price_start_time) AS price_effective_at,
  COUNT(*) FILTER (WHERE tag_matches) AS tagged_rows,
  COUNT(*) FILTER (WHERE NOT tag_matches) AS untagged_rows,
  COUNT(DISTINCT usage_unit) AS usage_unit_count,
  SUM(CASE WHEN UPPER(TRIM(usage_unit)) = 'DBU' THEN usage_quantity ELSE 0 END) AS dbu_quantity,
  COUNT(*) FILTER (WHERE UPPER(TRIM(usage_unit)) = 'DBU') AS dbu_rows,
  COALESCE(SUM(
    CASE WHEN row_match = 'priced'
      THEN GREATEST(0, UNIX_MILLIS(usage_end_time) - UNIX_MILLIS(usage_start_time)) / 1000.0
      ELSE 0 END
  ), 0) AS billed_seconds
  , MIN(usage_date) AS source_from
FROM priced
GROUP BY billing_origin_product
UNION ALL
SELECT
  'range' AS row_kind,
  '${RANGE_ROW}' AS key,
  CAST(NULL AS DOUBLE) AS spend,
  CASE WHEN COUNT(DISTINCT CASE WHEN currency_code IS NOT NULL THEN currency_code END) = 1
       THEN MAX(currency_code) ELSE CAST('' AS STRING) END AS currency,
  COUNT(DISTINCT CASE WHEN currency_code IS NOT NULL THEN currency_code END) AS currency_count,
  COUNT(DISTINCT usage_date) AS billed_days,
  CAST(NULL AS BIGINT) AS job_runs,
  MAX(usage_date) AS last_day,
  SUM(CASE WHEN row_match = 'priced' THEN usage_quantity ELSE 0 END) AS priced_quantity,
  SUM(CASE WHEN row_match <> 'priced' THEN usage_quantity ELSE 0 END) AS unpriced_quantity,
  COUNT(*) FILTER (WHERE row_match = 'priced') AS priced_rows,
  COUNT(*) FILTER (WHERE row_match <> 'priced') AS unpriced_rows,
  CAST('' AS STRING) AS unpriced_skus,
  CAST('' AS STRING) AS price_match_status,
  COUNT(*) FILTER (WHERE record_type ILIKE '%CORRECT%' OR usage_quantity < 0) AS correction_rows,
  COUNT(*) FILTER (WHERE price_match_count > 1) AS duplicate_matches,
  MAX(price_start_time) AS price_effective_at,
  COUNT(*) FILTER (WHERE tag_matches) AS tagged_rows,
  COUNT(*) FILTER (WHERE NOT tag_matches) AS untagged_rows,
  COUNT(DISTINCT usage_unit) AS usage_unit_count,
  SUM(CASE WHEN UPPER(TRIM(usage_unit)) = 'DBU' THEN usage_quantity ELSE 0 END) AS dbu_quantity,
  COUNT(*) FILTER (WHERE UPPER(TRIM(usage_unit)) = 'DBU') AS dbu_rows,
  COALESCE(SUM(
    CASE WHEN row_match = 'priced'
      THEN GREATEST(0, UNIX_MILLIS(usage_end_time) - UNIX_MILLIS(usage_start_time)) / 1000.0
      ELSE 0 END
  ), 0) AS billed_seconds
  , COALESCE(MIN(CASE WHEN component = 'app-compute' THEN usage_date END), MIN(usage_date)) AS source_from
FROM priced
UNION ALL
SELECT
  'propagation' AS row_kind,
  u.billing_origin_product AS key,
  CAST(NULL AS DOUBLE) AS spend,
  CAST('' AS STRING) AS currency,
  CAST(0 AS BIGINT) AS currency_count,
  COUNT(DISTINCT u.usage_date) AS billed_days,
  CAST(NULL AS BIGINT) AS job_runs,
  MAX(u.usage_date) AS last_day,
  CAST(0 AS DOUBLE) AS priced_quantity,
  CAST(0 AS DOUBLE) AS unpriced_quantity,
  CAST(0 AS BIGINT) AS priced_rows,
  CAST(0 AS BIGINT) AS unpriced_rows,
  CAST('' AS STRING) AS unpriced_skus,
  CAST('' AS STRING) AS price_match_status,
  CAST(0 AS BIGINT) AS correction_rows,
  CAST(0 AS BIGINT) AS duplicate_matches,
  CAST('' AS STRING) AS price_effective_at,
  COUNT(*) FILTER (WHERE u.custom_tags['${BILLING_TAG.key}'] = '${BILLING_TAG.value}') AS tagged_rows,
  COUNT(*) FILTER (
    WHERE u.custom_tags['${BILLING_TAG.key}'] IS NULL
       OR u.custom_tags['${BILLING_TAG.key}'] <> '${BILLING_TAG.value}'
  ) AS untagged_rows,
  CAST(0 AS BIGINT) AS usage_unit_count,
  CAST(0 AS DOUBLE) AS dbu_quantity,
  CAST(0 AS BIGINT) AS dbu_rows,
  CAST(0 AS DOUBLE) AS billed_seconds,
  MIN(u.usage_date) AS source_from
FROM system.billing.usage u
WHERE u.usage_date >= GREATEST(:from_day, (SELECT source_from FROM deployment_start))
  AND u.usage_date <= :to_day
  ${range.fromTimestamp?"AND u.usage_start_time >= :from_instant":""}
  AND u.workspace_id = :workspaceId
  AND u.billing_origin_product <> 'JOBS'
  AND (${leakPredicate})
GROUP BY u.billing_origin_product`;return{statement,parameters,covered,estimated}}var ROW_KINDS=new Set(["component","coverage","propagation","range"]);function emptyRow(kind,component){return{kind,component,spend:null,currency:"",currencyCount:0,billedDays:0,jobRuns:null,lastDay:"",pricedQuantity:0,unpricedQuantity:0,pricedRows:0,unpricedRows:0,unpricedSkus:[],priceMatchStatus:"none",correctionRows:0,duplicateMatches:0,priceEffectiveAt:"",taggedRows:0,untaggedRows:0,usageUnitCount:0,dbuQuantity:0,dbuRows:0,billedSeconds:0}}function asNumber(value){if(value===null||value===void 0||value==="")return null;const parsed=Number(value);return Number.isFinite(parsed)?parsed:null}function asCount(value){return asNumber(value)??0}function asMatch(value){if(value==="priced"||value==="unpriced"||value==="partial"||value==="duplicate"||value==="mixed-currency"||value==="none"){return value}return"none"}function parseSkus(value){if(!value)return[];return value.split(",").map(sku=>sku.trim()).filter(Boolean)}function readComponentRows(dataArray){if(!Array.isArray(dataArray))return[];const rows=[];for(const raw of dataArray){if(!Array.isArray(raw)||raw.length<6)continue;const cells=raw;const wide=cells.length>=8&&typeof cells[0]==="string"&&ROW_KINDS.has(cells[0]);if(wide){const[kind2,component2,spend2,currency2,currencyCount,billedDays2,jobRuns2,lastDay2,pricedQuantity,unpricedQuantity,pricedRows,unpricedRows,unpricedSkus,priceMatchStatus,correctionRows,duplicateMatches,priceEffectiveAt,taggedRows,untaggedRows,usageUnitCount,dbuQuantity,dbuRows,billedSeconds,firstDay]=cells;if(typeof component2!=="string"||typeof kind2!=="string")continue;rows.push({kind:kind2,component:component2,spend:asNumber(spend2),currency:typeof currency2==="string"?currency2:"",currencyCount:asCount(currencyCount),billedDays:asCount(billedDays2),jobRuns:asNumber(jobRuns2),lastDay:typeof lastDay2==="string"?lastDay2:"",pricedQuantity:asCount(pricedQuantity),unpricedQuantity:asCount(unpricedQuantity),pricedRows:asCount(pricedRows),unpricedRows:asCount(unpricedRows),unpricedSkus:parseSkus(unpricedSkus),priceMatchStatus:asMatch(priceMatchStatus),correctionRows:asCount(correctionRows),duplicateMatches:asCount(duplicateMatches),priceEffectiveAt:typeof priceEffectiveAt==="string"?priceEffectiveAt:"",taggedRows:asCount(taggedRows),untaggedRows:asCount(untaggedRows),usageUnitCount:asCount(usageUnitCount),dbuQuantity:asCount(dbuQuantity),...dbuRows===void 0?{}:{dbuRows:asCount(dbuRows)},...billedSeconds===void 0?{}:{billedSeconds:asCount(billedSeconds)},...firstDay===void 0?{}:{firstDay:typeof firstDay==="string"?firstDay:""}});continue}const[component,spend,currency,billedDays,jobRuns,lastDay]=cells;if(typeof component!=="string")continue;const kind=component===RANGE_ROW?"range":"component";rows.push({...emptyRow(kind,component),spend:asNumber(spend),currency:typeof currency==="string"?currency:"",billedDays:asCount(billedDays),jobRuns:asNumber(jobRuns),lastDay:typeof lastDay==="string"?lastDay:""})}return rows}function splitBillingRows(rows){return{components:rows.filter(row=>(row.kind??"component")==="component"&&row.component!==RANGE_ROW),coverage:rows.filter(row=>row.kind==="coverage"),propagation:rows.filter(row=>row.kind==="propagation"),meta:rows.find(row=>row.kind==="range"||row.component===RANGE_ROW)}}function pricingFromRow(row){if(!row)return{...EMPTY_PRICING};const pricedRows=row.pricedRows??0;const unpricedRows=row.unpricedRows??0;const unpricedQuantity=row.unpricedQuantity??0;const duplicateMatches=row.duplicateMatches??0;const currencyCount=row.currencyCount??(row.currency?1:0);let match=row.priceMatchStatus??"none";if(currencyCount>1)match="mixed-currency";else if(duplicateMatches>0||match==="duplicate")match="duplicate";else if(unpricedRows>0&&pricedRows>0)match="partial";else if(pricedRows===0&&(unpricedRows>0||unpricedQuantity>0))match="unpriced";else if(pricedRows>0)match=match==="none"?"priced":match;else if(row.spend!==null&&Number.isFinite(row.spend)&&match==="none")match="priced";return{source:"list_prices",match,currency:currencyCount>1?"":row.currency,pricedQuantity:row.pricedQuantity??0,unpricedQuantity,pricedRows,unpricedRows,unpricedSkus:row.unpricedSkus??[],duplicateMatches,correctionRows:row.correctionRows??0,priceEffectiveAt:row.priceEffectiveAt??""}}function attributionFor(population,amount){if(population==="Whole warehouse"||population==="Whole workspace")return"shared-upper-bound";if(amount===null)return"unavailable";return"deployment"}function spendAmountFor(row,basis){if(!row)return null;const pricing=pricingFromRow(row);if(pricing.match==="unpriced"||pricing.match==="partial"||pricing.match==="duplicate"||pricing.match==="mixed-currency"){return null}if(row.spend===null||!Number.isFinite(row.spend))return null;if(pricing.match==="priced"||pricing.match==="none"){return basis==="per-day"?row.spend/Math.max(row.billedDays,1):row.spend}return null}function dbuAmountFor(row,basis){if(!row)return null;const hasDbuEvidence=row.dbuRows===void 0?row.usageUnitCount===1:row.dbuRows>0;if(!hasDbuEvidence)return null;const amount=row.dbuQuantity;if(typeof amount!=="number"||!Number.isFinite(amount))return null;return basis==="per-day"?amount/Math.max(row.billedDays,1):amount}function unpricedUnavailable(pricing){if(pricing.match==="duplicate")return"Duplicate list prices; spend withheld";if(pricing.match==="mixed-currency")return"Mixed currencies; spend withheld";if(pricing.match==="partial"){const skus=pricing.unpricedSkus.slice(0,4).join(", ");return skus?`Estimated coverage is unavailable; spend withheld. Unpriced SKUs: ${skus}`:"Estimated coverage is unavailable; spend withheld"}if(pricing.match==="unpriced"){const skus=pricing.unpricedSkus.slice(0,4).join(", ");return skus?`Unpriced SKUs: ${skus}`:"Usage has no matching list price"}return""}function buildHonesty(range,meta,tiles){const currencies=new Set(tiles.map(tile=>tile.pricing?.currency).filter(code=>Boolean(code)));const through=meta?.lastDay||"";return{priceSource:"list_prices",contractRates:"unavailable",dataThrough:through,rangeMayStillFill:Boolean(through&&through<range.to)||!through,currencyConsistent:currencies.size<=1}}function buildCoverage(input){const products=[];const seen=new Set;for(const row of input.coverageRows){if(row.component==="JOBS")continue;seen.add(row.component);const tiled=TILED_PRODUCTS.has(row.component)||row.component==="GENIE";products.push({product:row.component,taggedRows:row.taggedRows||(row.pricedRows??0)+(row.unpricedRows??0),taggedQuantity:(row.pricedQuantity??0)+(row.unpricedQuantity??0),pricedRows:row.pricedRows??0,unpricedRows:row.unpricedRows??0,tiled,reason:PRODUCT_REASONS[row.component]??(tiled?"On the tracked cost grid.":"Tagged usage with no Cost tile.")})}for(const[product,reason]of Object.entries(PRODUCT_REASONS)){if(seen.has(product))continue;if(product==="MLFLOW"||product==="LAKEBASE"){products.push({product,taggedRows:0,taggedQuantity:0,pricedRows:0,unpricedRows:0,tiled:false,reason})}}const through=input.meta?.lastDay||"";const delayed=Boolean(through&&through<input.range.to);const propagation=input.propagationRows.filter(row=>row.component!=="JOBS").map(row=>{if(row.component==="APPS"){const pair=billingTagPair();const assignment=input.appBillingTag==="matched"?`${pair} is assigned to this app.`:input.appBillingTag==="missing"?`${pair} is not assigned to this app.`:`The app's ${pair} assignment could not be read.`;return{product:"APPS",status:"unsupported",detail:`${assignment} App spend is measured separately by exact app name; billing-row tag propagation is not required.`}}if(row.component==="GENIE"){return{product:"GENIE",status:"unsupported",detail:GENIE_LLM_UNAVAILABLE+" "+GENIE_SQL_NOT_COMPLETE}}if((row.taggedRows??0)>0){return{product:row.component,status:"propagated",detail:`${row.taggedRows} tagged billing rows.`}}if((row.untaggedRows??0)>0){return{product:row.component,status:"unpropagated",detail:`${row.untaggedRows} matching usage rows have no ${BILLING_TAG.key}=${BILLING_TAG.value} tag.`}}if(delayed){return{product:row.component,status:"delayed",detail:`Billing data through ${through}. Later days may still be filling.`}}return{product:row.component,status:"unused",detail:"No matching usage rows."}});return{inventoryCount:input.inventoryCount,costModelCount:COST_COMPONENTS.length+1,products,propagation}}var DESCRIPTIONS={"serving-endpoint":{label:"Serving endpoint",quality:"real",population:"This endpoint",basis:"total-in-range",variable:"DATABRICKS_SERVING_ENDPOINT_NAME"},"sql-warehouse":{label:"App and Genie SQL",quality:"estimate",population:"Attributed app and Genie queries",basis:"total-in-range",variable:"DATABRICKS_SQL_WAREHOUSE_ID"},genie:{label:"Genie",quality:"estimate",population:"Whole workspace",basis:"total-in-range",variable:"DATABRICKS_WORKSPACE_ID"},"vector-search":{label:"Vector Search",quality:"rate",population:"Hosting endpoint",basis:"total-in-range",variable:""},"app-compute":{label:"App compute",quality:"real",population:"This app",basis:"total-in-range",variable:"DATABRICKS_APP_NAME"}};function buildTiles(ids,rows,warehouseAttribution=EMPTY_WAREHOUSE_QUERY_ATTRIBUTION,resourceActivity=[],genie,genieReason="",marginal){const byComponent=new Map(rows.filter(row=>(row.kind??"component")==="component").map(row=>[row.component,row]));const tiles=[];for(const component of COST_COMPONENTS){if(component==="genie"){tiles.push(...genieAccountingTiles(ids,genie,genieReason));continue}const tile=componentTile(component,ids,byComponent,warehouseAttribution,resourceActivity);let decorated=tile;if(marginal){if(component==="serving-endpoint"){decorated=withMarginalServingEvidence(tile,byComponent.get(component),marginal.interactive)}else if(component==="app-compute"||component==="vector-search"){decorated=withMarginalUptimeEvidence(tile,byComponent.get(component),marginal.interactive)}}tiles.push(decorated);if(component==="serving-endpoint"){tiles.push(marginal?.foundation??{id:"foundation-model",label:"Foundation model tokens",resourceId:ids.foundationModel,resourceKind:ids.foundationModel?"serving-endpoint":"",quality:"unknown",amount:null,dbus:null,basis:"total-in-range",population:"Interactive Ask tokens",attribution:"unavailable",pricing:EMPTY_PRICING,unavailable:ids.foundationModel?"Foundation-model billing was not read.":"Configured foundation model unavailable.",remedy:"",note:"",evidence:{billingRows:null,astrolabeQueries:null,interactiveRequests:null,coveredRequests:null,tokens:null}})}}return tiles}function validRunDurationMs(run){const started=Date.parse(run.startedAt??"");const completed=Date.parse(run.completedAt);return Number.isFinite(started)&&Number.isFinite(completed)&&completed>=started?completed-started:null}function activeStandingSplit(full,row,interactive,messages){const durations=interactive.runs.map(validRunDurationMs);const covered=durations.filter(value=>value!==null);const complete=interactive.complete&&covered.length===interactive.runs.length;const billedMs=(row?.billedSeconds??0)*1e3;const requestMs=covered.reduce((sum,value)=>sum+value,0);const factor=complete&&billedMs>0?Math.min(1,requestMs/billedMs):null;const active=value=>factor!==null&&typeof value==="number"&&Number.isFinite(value)?value*factor:null;const standing=(value,marginal)=>marginal!==null&&typeof value==="number"&&Number.isFinite(value)?Math.max(0,value-marginal):null;const marginalAmount=row?active(full.amount):null;const marginalDbus=row?active(full.dbus):null;return{marginalAmount,marginalDbus,standingAmount:row?standing(full.amount,marginalAmount):null,standingDbus:row?standing(full.dbus,marginalDbus):null,marginalUnavailable:!row?messages.noRow:factor===null?complete?messages.unpriced:messages.incomplete:"",interactiveRequests:interactive.runs.length,coveredRequests:covered.length}}function withMarginalServingEvidence(full,row,interactive){const split=activeStandingSplit(full,row,interactive,{noRow:"No endpoint billing row.",unpriced:"Marginal serving cost needs priced endpoint billing intervals.",incomplete:"Interactive Ask timing coverage is incomplete; full endpoint uptime is excluded."});if(!row)return{...full,marginalAmount:null,marginalDbus:null,standingAmount:null,standingDbus:null,marginalUnavailable:"No endpoint billing row."};return{...full,label:"Agent serving",population:"Configured endpoint",marginalAmount:split.marginalAmount,marginalDbus:split.marginalDbus,standingAmount:split.standingAmount,standingDbus:split.standingDbus,marginalUnavailable:split.marginalUnavailable,evidence:{...full.evidence??{billingRows:null,astrolabeQueries:null},interactiveRequests:split.interactiveRequests,coveredRequests:split.coveredRequests}}}function withMarginalUptimeEvidence(full,row,interactive){const split=activeStandingSplit(full,row,interactive,{noRow:"No billing row matched this resource.",unpriced:"Marginal cost needs priced billing intervals.",incomplete:"Interactive Ask timing coverage is incomplete; full uptime is excluded."});return{...full,marginalAmount:split.marginalAmount,marginalDbus:split.marginalDbus,standingAmount:split.standingAmount,standingDbus:split.standingDbus,marginalUnavailable:split.marginalUnavailable,evidence:full.evidence?{...full.evidence,interactiveRequests:split.interactiveRequests,coveredRequests:split.coveredRequests}:full.evidence}}function genieAccountingTiles(ids,accounting,reason){if(!accounting){return ids.genieSpaces.map(space=>({id:space.tileId,label:space.label,resourceId:space.id,resourceKind:space.id?"genie-space":"",quality:"unknown",amount:null,dbus:null,basis:"total-in-range",population:space.id?"This Genie space":"This workspace",attribution:"unavailable",pricing:EMPTY_PRICING,unavailable:reason||"Genie billing could not be classified.",remedy:"",note:"",evidence:{billingRows:null,astrolabeQueries:null},genieInstanceAccounting:null}))}const{period}=accounting;if(!period.instances){return genieAccountingTiles(ids,null,"Legacy Genie billing has no per-space attribution evidence.")}const tiles=period.instances.map(instance=>{const pricing={...EMPTY_PRICING,source:"list_prices",match:instance.pricingState,currency:instance.paidUsd===null?"":"USD",pricedQuantity:instance.paidUsd===null?0:instance.chargedEffectiveDbus,unpricedQuantity:instance.paidUsd===null?instance.chargedEffectiveDbus:0};return{id:instance.tileId,label:instance.label,resourceId:instance.spaceId,resourceKind:"genie-space",quality:instance.paidUsd===null?"unknown":instance.pricingState==="partial"||instance.attribution.endsWith("-allocation")?"estimate":"real",amount:instance.paidUsd,dbus:instance.chargedEffectiveDbus,basis:"total-in-range",population:"This Genie space",attribution:instance.paidUsd===null&&instance.chargedEffectiveDbus>0?"unavailable":"deployment",pricing,unavailable:instance.paidUsd===null?"Charged Genie DBUs are measured, but USD is unavailable because pricing is incomplete.":"",remedy:"",note:instance.attribution.endsWith("-allocation")?`Allocated by user-day execution or app-call share · billing through ${period.throughDay||"unavailable"}.`:`Matched by user-day Query History or app-run evidence to this configured space · billing through ${period.throughDay||"unavailable"}.`,evidence:{billingRows:null,astrolabeQueries:null},genieInstanceAccounting:instance}});return tiles}function appComputeAbsence(state){const pair=billingTagPair();if(state==="matched"){return{unavailable:"No Apps billing rows matched this app.",remedy:"",note:`${pair} is on this app; Apps billing is matched by app name.`}}if(state==="missing"){return{unavailable:"No Apps billing rows matched this app.",remedy:"",note:`${pair} is not on this app; Apps billing is still matched by app name.`}}return{unavailable:"No Apps billing rows matched this app.",remedy:"",note:`The app tag ${pair} could not be read; Apps billing is matched by app name.`}}function componentTile(component,ids,byComponent,warehouseAttribution,resourceActivity){const description=DESCRIPTIONS[component];const base={id:component,label:description.label,resourceId:resourceIdFor(component,ids),...component==="vector-search"&&vectorIndexName(ids.vectorIndex)?{secondaryResourceId:ids.vectorEndpoint}:{},resourceKind:resourceKindFor(component,ids),quality:description.quality,basis:description.basis,population:description.population};const withMeta=tile=>{const pricing2=tile.pricing??EMPTY_PRICING;const amount2=tile.amount;const dbus2=tile.dbus;const unpriced=unpricedUnavailable(pricing2);return{...tile,quality:unpriced?"unknown":tile.quality,amount:unpriced?null:amount2,attribution:attributionFor(tile.population,unpriced?dbus2??null:amount2??dbus2??null),pricing:pricing2,unavailable:tile.unavailable||unpriced,note:tile.note}};if(!canAsk(component,ids)){const pricing2=EMPTY_PRICING;if(component==="vector-search"){const activity=resourceActivity.find(item=>item.tileId==="vector-search");const configuredIndex=vectorIndexName(ids.vectorIndex);const identityReason=!configuredIndex?ids.vectorIdentityError||"The active Vector Search index was not carried into this app release.":!ids.vectorEndpoint?ids.vectorIdentityError||"The active index did not identify its hosting endpoint.":ids.vectorEndpointIndexCount===null||ids.vectorEndpointIndexCount===void 0?ids.vectorIdentityError||"The hosting endpoint index count could not be read.":ids.vectorEndpointIndexCount>1?`The hosting endpoint serves ${ids.vectorEndpointIndexCount} indexes; billing exposes endpoint identity only.`:ids.vectorIdentityError||"The active index-to-endpoint relationship was not established.";return withMeta({...base,quality:"unknown",amount:null,pricing:pricing2,note:"",unavailable:`Vector Search cost unavailable: ${identityReason}`,remedy:configuredIndex?"":"Release the app with the active Vector Search index identity.",evidence:{billingRows:null,astrolabeQueries:null,activity:activity?{calls:activity.calls,observedCalls:activity.observedCalls,unit:"queries"}:null}})}return withMeta({...base,quality:"unknown",amount:null,pricing:pricing2,note:"",unavailable:"Resource identifier unavailable",remedy:description.variable?`Set ${description.variable}.`:"",evidence:{billingRows:null,astrolabeQueries:null}})}const row=byComponent.get(component);const pricing=pricingFromRow(row);const amount=spendAmountFor(row,description.basis);const billingRows=row?(row.pricedRows??0)+(row.unpricedRows??0):0;const dbus=component==="vector-search"&&row&&billingRows===0?0:dbuAmountFor(row,description.basis);const measuredActivity=component==="vector-search"?resourceActivity.find(item=>item.tileId==="vector-search"):void 0;const evidence={billingRows,astrolabeQueries:component==="sql-warehouse"?warehouseAttribution.astrolabeQueries:null,...component==="sql-warehouse"?{warehouseQueries:warehouseAttribution.totalQueries,queryHistoryComplete:warehouseAttribution.complete,queryHistoryCoverage:warehouseAttribution.coverage}:{},...component==="vector-search"?{activity:measuredActivity?{calls:measuredActivity.calls,observedCalls:measuredActivity.observedCalls,unit:"queries"}:null}:{}};if(component==="vector-search"){const noUsage=Boolean(row&&billingRows===0);return withMeta({...base,quality:"rate",population:"Hosting endpoint",amount,dbus,pricing,note:[noUsage?"No billable usage in this period":"Billing is endpoint-level; this endpoint hosts only the active index.",ids.vectorIdentityDrift].filter(Boolean).join(" "),unavailable:amount===null&&dbus===null?unpricedUnavailable(pricing)||"No billing rows matched the hosting endpoint":"",remedy:"",evidence})}if(component==="sql-warehouse"){const attributedExecutionMs=warehouseAttribution.astrolabeExecutionMs+warehouseAttribution.genieSpaces.reduce((sum,space)=>sum+space.executionMs,0);if(amount===null||!warehouseAttribution.complete||warehouseAttribution.totalExecutionMs<=0){return withMeta({...base,amount:null,dbus,pricing,note:"",unavailable:amount===null?unpricedUnavailable(pricing)||"No billing rows":"Incomplete Query History",remedy:"",evidence})}return withMeta({...base,amount:amount*attributedExecutionMs/warehouseAttribution.totalExecutionMs,dbus:dbus===null?null:dbus*attributedExecutionMs/warehouseAttribution.totalExecutionMs,pricing,note:"",unavailable:"",remedy:"",evidence})}if(amount===null){if(component==="app-compute"&&pricing.match==="none"){const absence=appComputeAbsence(ids.appBillingTag);return withMeta({...base,amount:null,dbus,pricing,note:absence.note,unavailable:absence.unavailable,remedy:absence.remedy,evidence})}return withMeta({...base,amount:null,dbus,pricing,note:"",unavailable:unpricedUnavailable(pricing)||"No billing rows",remedy:"",evidence})}return withMeta({...base,amount,dbus,pricing,note:"",unavailable:"",remedy:"",evidence})}var UNKNOWN_QUESTION_PARTS=[{id:"genie",label:"Genie spaces",unavailable:"Space tags are organizational only. Genie LLM spend is not attributable in this model; Genie SQL is billed through the associated warehouse and is not the complete Genie cost."},{id:"lakebase",label:"Lakebase Postgres",unavailable:"No documented billing row in this app can be joined to a Lakebase query or run."}];function unknownPart(id,label,unavailable){return{id,label,quality:"unknown",amount:null,unavailable}}function buildQuestionAttribution(runs,tiles,limit,warehouseAttribution=EMPTY_WAREHOUSE_QUERY_ATTRIBUTION){const newest=runs.slice(0,limit);const first=runs[0];const runsInRange=first?.runsInRange??0;const tokenCoveredRuns=first?.tokenCoveredRuns??0;const totalRecordedTokens=first?.totalRecordedTokens??0;const servingTile=tiles.find(tile=>tile.id==="serving-endpoint");const servingSpend=servingTile?.amount;const foundationTile=tiles.find(tile=>tile.id==="foundation-model");const foundationSpend=foundationTile?.amount;const sqlSpend=tiles.find(tile=>tile.id==="sql-warehouse")?.amount;const durationByRun=new Map(runs.map(run=>[run.runId,validRunDurationMs(run)]).filter(entry=>entry[1]!==null));const totalDurationMs=[...durationByRun.values()].reduce((sum,duration)=>sum+duration,0);const timingReported=runs.every(run=>Boolean(run.startedAt));const sqlByRun=new Map((warehouseAttribution.askRuns??[]).map(run=>[run.runId,run.executionMs]));const totalAskSqlMs=[...sqlByRun.values()].reduce((sum,duration)=>sum+duration,0);const uptimeMarginal=id=>{const value=tiles.find(tile=>tile.id===id)?.marginalAmount;return typeof value==="number"&&Number.isFinite(value)?value:null};const vectorMarginal=uptimeMarginal("vector-search");const appComputeMarginal=uptimeMarginal("app-compute");const attributed=newest.map(run=>{const parts=[];if((durationByRun.has(run.runId)||!timingReported&&run.totalTokens!==null)&&typeof servingSpend==="number"&&Number.isFinite(servingSpend)&&(totalDurationMs>0||!timingReported&&totalRecordedTokens>0)){parts.push({id:"serving-endpoint",label:"Model serving",quality:timingReported?"estimate":"per-token",amount:timingReported?servingSpend*(durationByRun.get(run.runId)??0)/totalDurationMs:servingSpend*(run.totalTokens??0)/totalRecordedTokens,unavailable:""})}else{parts.push(unknownPart("serving-endpoint","Model serving",!durationByRun.has(run.runId)?"This run has no complete request interval.":"No marginal serving spend was measured."))}if(run.totalTokens!==null&&run.totalTokens>=0&&typeof foundationSpend==="number"&&Number.isFinite(foundationSpend)&&totalRecordedTokens>0){parts.push({id:"foundation-model",label:"Foundation model tokens",quality:"per-token",amount:foundationSpend*run.totalTokens/totalRecordedTokens,unavailable:""})}else{parts.push(unknownPart("foundation-model","Foundation model tokens",run.totalTokens===null?"This run recorded no token count.":"No foundation-model spend was measured."))}const runSqlMs=sqlByRun.get(run.runId)??0;const legacySql=warehouseAttribution.askRuns===void 0||warehouseAttribution.askRuns.length===0;if(typeof sqlSpend==="number"&&Number.isFinite(sqlSpend)&&(runSqlMs>0&&totalAskSqlMs>0||legacySql&&runsInRange>0)){parts.push({id:"sql-warehouse",label:"Ask SQL",quality:"estimate",amount:legacySql?sqlSpend/runsInRange:sqlSpend*runSqlMs/totalAskSqlMs,unavailable:""})}else{parts.push(unknownPart("sql-warehouse","Ask SQL","No Ask-tagged SQL spend was available for this run."))}for(const uptime of[{id:"vector-search",label:"Vector search",marginal:vectorMarginal,missing:"No marginal Vector Search spend was measured."},{id:"app-compute",label:"App compute",marginal:appComputeMarginal,missing:"No marginal app-compute spend was measured."}]){if(uptime.marginal!==null&&durationByRun.has(run.runId)&&totalDurationMs>0){parts.push({id:uptime.id,label:uptime.label,quality:"estimate",amount:uptime.marginal*(durationByRun.get(run.runId)??0)/totalDurationMs,unavailable:""})}else{parts.push(unknownPart(uptime.id,uptime.label,uptime.marginal===null?uptime.missing:"This run has no complete request interval."))}}parts.push(...UNKNOWN_QUESTION_PARTS.map(part=>({...part,quality:"unknown",amount:null})));return{runId:run.runId,requestId:run.requestId??"",correlationId:run.correlationId,traceId:run.traceId,user:run.user??"",startedAt:run.startedAt??"",completedAt:run.completedAt,durationMs:validRunDurationMs(run),inputTokens:run.inputTokens,outputTokens:run.outputTokens,totalTokens:run.totalTokens,...run.cachedReadTokens===void 0?{}:{cachedReadTokens:run.cachedReadTokens},...run.cacheWriteTokens===void 0?{}:{cacheWriteTokens:run.cacheWriteTokens},parts}});return{runs:attributed,runsInRange,tokenCoveredRuns,totalRecordedTokens,requestCoveredRuns:runs.filter(run=>Boolean(run.requestId&&run.correlationId)).length,traceCoveredRuns:runs.filter(run=>Boolean(run.traceId)).length,timingCoveredRuns:durationByRun.size,complete:Boolean(first?.evidenceComplete??runsInRange===runs.length),limited:runsInRange>attributed.length,reason:runsInRange===0?"No completed runs were recorded.":""}}export{cancelAstrolabeWarehouseQueries,createWorkspaceWarehouseCancellationTransport,BILLING_TAG,RETIRED_BILLING_TAG_KEY,billingTagPair,EMPTY_WAREHOUSE_QUERY_ATTRIBUTION,readWarehouseQueryAttribution,createWorkspaceQueryHistoryTransport,COST_COMPONENTS,readResourceActivityRows,WORKSPACE_ESTIMATE_SUFFIX,workspaceEstimateRow,RANGE_ROW,BILLING_TAG_KEY,BILLING_TAG_VALUE,LIST_PRICE_SOURCE,GENIE_LLM_UNAVAILABLE,GENIE_SQL_NOT_COMPLETE,EMPTY_PRICING,canAsk,resourceIdFor,vectorIndexName,buildCostStatement,readComponentRows,splitBillingRows,pricingFromRow,attributionFor,spendAmountFor,dbuAmountFor,unpricedUnavailable,buildHonesty,buildCoverage,buildTiles,buildQuestionAttribution};
