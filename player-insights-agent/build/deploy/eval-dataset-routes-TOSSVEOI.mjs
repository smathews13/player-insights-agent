
import{alignGuidelinesToHumans,loadCasesForAlignment,promotePromptAlias,promptTemplateFromPromote,readLabState,snapshotWorkingCopy}from"./chunk-AQSE5FLQ.mjs";import{BakeOffHistorySchema,DEFAULT_LIVE_SAMPLE_RATE,EvalDatasetSchema,LastSuiteSchema,MATCHING_POLICY_FACT,MATCHING_POLICY_ID,MATCHING_POLICY_REFERENCE,PromotedAgentSchema,REVIEW_LABEL_SCHEMA,SUITE_KINDS,auditHeldOutEdits,classifyGenieMiss,compareExecutedResults,executedTableFromMatrix,findLatestAnsweredConversation,formatConversationTurns,genieLaneReady,genieSuitePlan,isExcludedGenieMiss,labCaseFromRow,labeledRowCount,labelingIdsFromBody,listLiveScores,loadConversationTurns,missingSqlGateCopy,patchFlywheelState,promoteTargetCaption,readBenchmarkSettings,readBenchmarkSettingsDocument,readEvalDataset,readEvalDatasetEnvelope,readFlywheelState,rememberAccuracy,rememberBakeOff,requestBenchmarkCancel,requestString,reviewAppUrlFromBody,scoreSampledAskTurn,scoredAccuracy,servingInvocationPath,tableView,uniqueQuestionsToAdd,userEmail,writeBenchmarkSettingsPatch,writeEvalDataset,writeLastGenieRun}from"./chunk-AA7BIVQI.mjs";import"./chunk-Q7N2ZVLF.mjs";import{executionToken}from"./chunk-2SBMSOUU.mjs";import"./chunk-32YNEJYL.mjs";import{recordAdminAction}from"./chunk-N2MF6JF6.mjs";import"./chunk-66G4LGYE.mjs";import{normalizeWorkspaceHost}from"./chunk-3KZTQDF5.mjs";import"./chunk-3LJPB2Y3.mjs";import"./chunk-P7U7VA46.mjs";import{sqlQueryTags}from"./chunk-W7NKQP7B.mjs";import"./chunk-KX6GWMVX.mjs";import{external_exports}from"./chunk-DDLERORI.mjs";import"./chunk-OMC625AH.mjs";import"./chunk-A7SHUGSC.mjs";var LABELING_SESSION_PATHS=["/api/2.0/mlflow/labeling-sessions","/api/2.0/mlflow/genai/labeling-sessions"];async function startLabelingSession(client,input){const name=input.name.trim()||`PIA SME review ${new Date().toISOString().slice(0,10)}`;const at=new Date().toISOString();let lastError=new Error("No labeling-session path answered.");for(const path of LABELING_SESSION_PATHS){try{const body=await client.request({method:"POST",path,payload:{name,experiment_id:input.experimentId.trim()||void 0,label_schemas:REVIEW_LABEL_SCHEMA.map(entry=>entry.name),enable_multi_turn_chat:true}});const ids=labelingIdsFromBody(body);const url=reviewAppUrlFromBody(body);if(!url){return{name:ids.name||name,sessionId:ids.sessionId,runId:ids.runId,url:"",status:"blocked",note:"A labeling session was created but Databricks did not return a Review App URL. Nothing was invented. SMEs can still label thumbs and SQL correct on this tab.",at}}return{name:ids.name||name,sessionId:ids.sessionId,runId:ids.runId,url,status:"open",note:"Share this Review App with SMEs. Labels use the same thumbs and SQL-correct fields already stored here.",at}}catch(error){lastError=error}}return{name,sessionId:"",runId:"",url:"",status:"blocked",note:`Review App could not be started: ${lastError instanceof Error?lastError.message:String(lastError)} Apps often cannot create labeling sessions. SMEs can still label thumbs and SQL correct on this tab.`,at}}var GENIE_RESULT_WAIT="50s";function text(value){return typeof value==="string"?value.trim():""}function asRecord(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:null}function columnNames(payload){const manifest=asRecord(payload.manifest);const schema=asRecord(manifest?.schema)??asRecord(payload.schema)??asRecord(asRecord(payload.result)?.schema);const columns=schema?.columns;if(Array.isArray(columns)){return columns.map((entry,index)=>{const column=asRecord(entry);return text(column?.name)||text(column?.display_name)||`col_${index}`})}return[]}function dataArray(payload){const result=asRecord(payload.result);const rows=result?.data_array??payload.data_array;if(!Array.isArray(rows))return[];return rows.filter(Array.isArray)}function sqlIsResultQuery(sql){const head=sql.trim().replace(/^\(+/,"").toLowerCase();return/^(select|with)\b/.test(head)}function tableFromStatementPayload(payload){const root=asRecord(payload);if(!root)return null;const rows=dataArray(root);const names=columnNames(root);const width=names.length||rows[0]?.length||0;if(width<=0&&rows.length===0)return{rowCount:0,columns:[]};const columns=names.length>0?names:Array.from({length:width},(_,index)=>`col_${index}`);return executedTableFromMatrix(columns,rows)}function createSqlExecutor(options){const host=options.host.replace(/\/+$/,"");const warehouseId=options.warehouseId.trim();if(!host||!options.token||!warehouseId)return null;const call=options.fetchImpl??fetch;return async sql=>{if(!sqlIsResultQuery(sql)){return{ok:false,note:"Statement is not a result set. Matching needs a SELECT."}}try{const response=await call(`${host}/api/2.0/sql/statements`,{method:"POST",headers:{authorization:`Bearer ${options.token}`,accept:"application/json","content-type":"application/json"},body:JSON.stringify({warehouse_id:warehouseId,statement:sql,query_tags:sqlQueryTags({surface:"benchmark",tool:"genie_result",operation:"execute"}),wait_timeout:GENIE_RESULT_WAIT,on_wait_timeout:"CANCEL",format:"JSON_ARRAY",disposition:"INLINE"}),signal:AbortSignal.timeout(55e3)});const payload=await response.json().catch(()=>({}))??{};if(!response.ok){const note=text(payload.message)||text(payload.error)||`warehouse returned HTTP ${response.status}`;return{ok:false,note}}const status=asRecord(payload.status);const state=text(status?.state).toUpperCase();if(state&&state!=="SUCCEEDED"){const error=asRecord(status?.error);const note=text(error?.message)||`statement ended in ${state}`;return{ok:false,note}}const table=tableFromStatementPayload(payload);if(!table)return{ok:false,note:"Warehouse returned no result table."};return{ok:true,table}}catch(error){const timedOut=error?.name==="TimeoutError"||error?.name==="AbortError";return{ok:false,note:timedOut?"SQL cancelled or timed out after 50s.":`The SQL warehouse could not be reached: ${error.message}`}}}}function executeMissKind(note){return classifyGenieMiss(note)}var GENIE_ACCURACY_POLL_MS=2e3;var GENIE_ACCURACY_TIMEOUT_MS=9e4;var MissingSqlGateError=class extends Error{missing;selected;constructor(missing,selected){super(missingSqlGateCopy(missing,selected));this.name="MissingSqlGateError";this.missing=missing;this.selected=selected}};function messageOf(error){return error?.message??String(error)}function asRecord2(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:null}function text2(value){return typeof value==="string"?value.trim():""}function numberish(value){if(typeof value==="number"||typeof value==="boolean")return value;if(typeof value==="string")return value;return null}function extractGenieSql(payload){const root=asRecord2(payload);if(!root)return"";const message=asRecord2(root.message)??root;const attachments=message.attachments;if(Array.isArray(attachments)){for(const entry of attachments){const attachment=asRecord2(entry);if(!attachment)continue;const query=asRecord2(attachment.query)??attachment;const sql=text2(query.query)||text2(query.sql)||text2(attachment.query)||text2(attachment.sql);if(sql)return sql}}return text2(message.query)||text2(root.query)||text2(root.sql)}function extractGenieResultTable(payload){const root=asRecord2(payload);if(!root)return null;const message=asRecord2(root.message)??root;const attachments=Array.isArray(message.attachments)?message.attachments:[];for(const entry of attachments){const attachment=asRecord2(entry);if(!attachment)continue;const query=asRecord2(attachment.query)??attachment;const columnsRaw=query.columns??query.schema??attachment.columns;const rowsRaw=query.rows??query.data??attachment.rows??attachment.data;if(!Array.isArray(columnsRaw)||!Array.isArray(rowsRaw))continue;const names=columnsRaw.map((columnEntry,index)=>{if(typeof columnEntry==="string")return columnEntry;const column=asRecord2(columnEntry);return text2(column?.name)||text2(column?.display_name)||`col_${index}`});const values=names.map(()=>[]);for(const row of rowsRaw){if(Array.isArray(row)){names.forEach((_,index)=>values[index].push(numberish(row[index])))}else{const record=asRecord2(row);names.forEach((name,index)=>values[index].push(record?numberish(record[name]):null))}}return{rowCount:rowsRaw.length,columns:names.map((name,index)=>({name,values:values[index]}))}}return null}function messageStatus(payload){const root=asRecord2(payload);const message=asRecord2(root?.message)??root;return text2(message?.status).toUpperCase()}function conversationIds(payload){const root=asRecord2(payload)??{};const conversation=asRecord2(root.conversation);const message=asRecord2(root.message);return{conversationId:text2(conversation?.id)||text2(root.conversation_id),messageId:text2(message?.id)||text2(root.message_id)}}async function jsonRequest(call,url,token,method,body){const response=await call(url,{method,headers:{authorization:`Bearer ${token}`,accept:"application/json",...method==="POST"?{"content-type":"application/json"}:{}},...method==="POST"?{body:JSON.stringify(body??{})}:{},signal:AbortSignal.timeout(GENIE_ACCURACY_TIMEOUT_MS)});const payload=await response.json().catch(()=>({}))??{};if(!response.ok){const detail=text2(payload.message)||text2(payload.error)||`workspace returned HTTP ${response.status}`;throw new Error(detail)}return payload}function fromPayload(payload,fallbackNote){const ids=conversationIds(payload);return{sql:extractGenieSql(payload),note:fallbackNote,rows:extractGenieResultTable(payload),conversationId:ids.conversationId||void 0,messageId:ids.messageId||void 0}}function createGenieAsker(options){const call=options.fetchImpl??fetch;const sleep=options.sleep??(ms=>new Promise(resolve=>setTimeout(resolve,ms)));const now=options.now??Date.now;const base=options.host.replace(/\/+$/,"");return{async ask({spaceId,question}){if(!base||!options.token){throw new Error("This app cannot reach the workspace to ask the Genie space.")}const start=await jsonRequest(call,`${base}/api/2.0/genie/spaces/${encodeURIComponent(spaceId)}/start-conversation`,options.token,"POST",{content:question});const immediate=fromPayload(start,"Genie returned SQL.");if(immediate.sql&&["COMPLETED","EXECUTED"].includes(messageStatus(start))){return immediate}const ids=conversationIds(start);if(!ids.conversationId||!ids.messageId){if(immediate.sql)return immediate;throw new Error("Genie did not return a conversation to poll.")}const deadline=now()+GENIE_ACCURACY_TIMEOUT_MS;while(now()<deadline){const polled=await jsonRequest(call,`${base}/api/2.0/genie/spaces/${encodeURIComponent(spaceId)}/conversations/${encodeURIComponent(ids.conversationId)}/messages/${encodeURIComponent(ids.messageId)}`,options.token,"GET");const status=messageStatus(polled);const asked=fromPayload(polled,"Genie returned SQL.");if(["COMPLETED","EXECUTED"].includes(status)&&asked.sql){return asked}if(["FAILED","CANCELLED","QUERY_RESULT_EXPIRED"].includes(status)){throw new Error(`Genie finished with status ${status}.`)}await sleep(GENIE_ACCURACY_POLL_MS)}throw new Error("Genie did not finish before the wait ran out.")}}}async function resolveTable(sql,already,executor){if(already&&already.columns.length>0)return{table:already,execute:null};if(!executor)return{table:already??null,execute:null};if(!sql.trim())return{table:null,execute:null};const execute=await executor(sql);return{table:execute.ok?execute.table:null,execute}}function excludedCase(row,note,durationMs,extras={}){const missKind=extras.missKind??classifyGenieMiss(note);return{id:row.id,question:row.question,outcome:"excluded",predictedSql:extras.predictedSql??"",groundTruthSql:row.groundTruthSql,note,durationMs,missKind,excluded:true,conversationId:extras.conversationId??"",comparisonReason:extras.comparisonReason??"",predictedTable:extras.predictedTable,groundTable:extras.groundTable}}function scoredCase(input){return{id:input.row.id,question:input.row.question,outcome:input.outcome,predictedSql:input.predictedSql,groundTruthSql:input.row.groundTruthSql,note:input.note,durationMs:input.durationMs,missKind:input.missKind,excluded:input.excluded,conversationId:input.conversationId??"",comparisonReason:input.comparisonReason??"",predictedTable:tableView(input.predictedTable),groundTable:tableView(input.groundTable)}}function newRunId(now){return`run_${now.toString(36)}`}async function runGenieAccuracy(input){const now=input.now??Date.now;const startedAtMs=now();const startedAt=new Date(startedAtMs).toISOString();const suiteKind=input.suiteKind??"complete";const plan=genieSuitePlan(input.rows.map(labCaseFromRow),input.caseIds);const byId=new Map(input.rows.map(row=>[row.id,row]));if(suiteKind==="complete"&&!plan.canRunComplete){throw new MissingSqlGateError(plan.missingSql.length,plan.selected.length)}const cases=[];for(const labCase of plan.selected){const row=byId.get(labCase.id);if(!row)continue;if(!genieLaneReady(labCase)){if(labCase.question.trim()){cases.push(excludedCase(row,"Missing ground-truth SQL. Out of the accuracy denominator.",0))}continue}const caseStarted=now();try{const asked=await input.asker.ask({spaceId:input.spaceId,question:row.question});const durationMs=Math.max(0,now()-caseStarted);const predicted=await resolveTable(asked.sql,asked.rows,input.executor);const ground=await resolveTable(row.groundTruthSql,null,input.executor);if(predicted.execute&&!predicted.execute.ok){const missKind=executeMissKind(predicted.execute.note);const excluded2=isExcludedGenieMiss(missKind);cases.push(scoredCase({row,outcome:excluded2?"excluded":"fail",predictedSql:asked.sql,note:predicted.execute.note,durationMs,missKind,excluded:excluded2,conversationId:asked.conversationId,predictedTable:null,groundTable:ground.table}));continue}if(ground.execute&&!ground.execute.ok){const missKind=executeMissKind(ground.execute.note);cases.push(excludedCase(row,`Ground-truth SQL did not produce a result table: ${ground.execute.note}`,durationMs,{predictedSql:asked.sql,conversationId:asked.conversationId??"",predictedTable:tableView(predicted.table),missKind}));continue}if(!predicted.table||!ground.table){cases.push(excludedCase(row,"No executed result table to score. Matching is result equivalence, not SQL text, and this workspace has no warehouse result to compare.",durationMs,{predictedSql:asked.sql,conversationId:asked.conversationId??"",predictedTable:tableView(predicted.table),groundTable:tableView(ground.table)}));continue}const compared=compareExecutedResults(predicted.table,ground.table);cases.push(scoredCase({row,outcome:compared.equivalent?"pass":"fail",predictedSql:asked.sql,note:compared.equivalent?asked.note:compared.reason,durationMs,missKind:null,excluded:false,conversationId:asked.conversationId,comparisonReason:compared.reason,predictedTable:predicted.table,groundTable:ground.table}))}catch(error){const durationMs=Math.max(0,now()-caseStarted);const note=messageOf(error);const missKind=classifyGenieMiss(note);const excluded2=isExcludedGenieMiss(missKind);cases.push(scoredCase({row,outcome:excluded2?"excluded":"error",predictedSql:"",note,durationMs,missKind,excluded:excluded2}))}}const passed=cases.filter(entry=>entry.outcome==="pass").length;const excluded=cases.filter(entry=>entry.excluded).length;const scored=cases.filter(entry=>!entry.excluded).length;return{id:newRunId(startedAtMs),spaceId:input.spaceId,spaceLabel:input.spaceLabel?.trim()||input.spaceId,startedAt,finishedAt:new Date(now()).toISOString(),suiteKind,datasetVersion:input.datasetVersion?.trim()||"unversioned",matchingPolicyId:MATCHING_POLICY_ID,matchingPolicyFact:MATCHING_POLICY_FACT,matchingPolicyHref:MATCHING_POLICY_REFERENCE,score:scoredAccuracy(passed,scored,excluded),cases}}var MLFLOW_SCORER_LIST_PATHS=["/api/2.0/mlflow/genai/scorers/list","/api/2.0/mlflow/scorers/list"];function parseScorerNames(body){if(!body||typeof body!=="object")return[];const record=body;const lists=[record.scorers,record.registered_scorers,record.entities];for(const list of lists){if(!Array.isArray(list))continue;const names=list.map(entry=>{if(typeof entry==="string")return entry.trim();if(entry&&typeof entry==="object"&&typeof entry.name==="string"){return entry.name.trim()}return""}).filter(Boolean);if(names.length>0)return[...new Set(names)]}return[]}function workspaceMonitorFromError(error){const message=error instanceof Error?error.message:String(error);const blocked=/403|401|PERMISSION|scope|UNAUTHORIZED|not found|404|does not exist|INVALID_PARAMETER/i.test(message);return{status:blocked?"blocked":"unknown",note:blocked?`Workspace monitoring is not available to this app: ${message} Production scorers have to be registered from a Databricks notebook. Sampled Ask turns are still scored here.`:`Workspace monitoring could not be checked: ${message} Sampled Ask turns are still scored here.`,scorers:[]}}async function probeWorkspaceMonitoring(client,experimentId){const named=experimentId.trim();if(!named){return{status:"unknown",note:"No MLflow experiment id is configured, so workspace monitoring cannot be listed. Sampled Ask turns are still scored here.",scorers:[]}}let lastError=new Error("No scorer list path answered.");for(const path of MLFLOW_SCORER_LIST_PATHS){try{const body=await client.apiClient.request({method:"GET",path,query:{experiment_id:named}});const scorers=parseScorerNames(body);if(scorers.length>0){return{status:"active",note:`Workspace monitoring already has ${scorers.length} scorer(s) on this experiment. Sampled Ask turns are also scored in this app.`,scorers}}return{status:"blocked",note:"The experiment has no registered production scorers. Apps cannot register Python scorers — that has to happen in a Databricks notebook. Sampled Ask turns are scored in this app instead.",scorers:[]}}catch(error){lastError=error}}return workspaceMonitorFromError(lastError)}var GenieAccuracyBody=external_exports.object({spaceId:external_exports.string().trim().min(1).max(200),spaceLabel:external_exports.string().trim().max(200).optional(),suiteKind:external_exports.enum(SUITE_KINDS).default("complete"),caseIds:external_exports.array(external_exports.string().trim().min(1).max(80)).max(200).optional()});var CurateBody=external_exports.object({questions:external_exports.array(external_exports.string().trim().max(2e3)).max(100)});function workspaceApiRequest(client,input){return client.apiClient.request({path:input.path,method:input.method,query:input.query,payload:input.payload,headers:new Headers({Accept:"application/json"}),raw:false})}var LastSuiteBody=LastSuiteSchema.extend({runIds:external_exports.array(external_exports.string().trim().min(1).max(80)).max(4).default([]),sides:external_exports.array(external_exports.string().trim().max(200)).max(4).default([])});function workspaceHost(){return normalizeWorkspaceHost(process.env.DATABRICKS_HOST)}function setupEvalDatasetRoutes(appkit){appkit.server.extend(app=>{app.get("/api/benchmarks/dataset",async(_req,res)=>{const envelope=await readEvalDatasetEnvelope(appkit,{maxAgeMs:0});res.json({dataset:envelope.dataset,lastGenieRun:envelope.lastGenieRun})});app.get("/api/benchmarks/flywheel",async(_req,res)=>{const flywheel=await readFlywheelState(appkit,{maxAgeMs:0});res.json({flywheel})});app.get("/api/benchmarks/live-scores",async(_req,res)=>{const[scores,settings]=await Promise.all([listLiveScores(appkit),readBenchmarkSettings(appkit,{maxAgeMs:0})]);res.json({scores,sampleRate:DEFAULT_LIVE_SAMPLE_RATE,alwaysOnTraces:settings.alwaysOnTraces,workspace:{status:"unknown",note:"Open “Check workspace monitoring” to list scorers already registered on the experiment. This list is the in-app hook, not a fabricated MLflow monitor.",scorers:[]}})});app.post("/api/admin/benchmarks/live-monitoring",async(req,res)=>{const actor=userEmail(req);const settings=await readBenchmarkSettings(appkit,{maxAgeMs:0});const experimentId=settings.experimentId.trim();let workspace;try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});workspace=await probeWorkspaceMonitoring({apiClient:{request:input=>workspaceApiRequest(client,input)}},experimentId)}catch(error){workspace={status:"blocked",note:`Workspace monitoring could not be reached: ${error.message} Sampled Ask turns are still scored in this app.`,scorers:[]}}await recordAdminAction(appkit.lakebase,{actor,action:"eval-live-monitoring-probed",subject:"eval-live-scores",detail:workspace.note});res.json({workspace,sampleRate:DEFAULT_LIVE_SAMPLE_RATE,alwaysOnTraces:settings.alwaysOnTraces})});app.put("/api/admin/benchmarks/dataset",async(req,res)=>{const parsed=EvalDatasetSchema.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_eval_dataset",detail:parsed.error.message});return}const actor=userEmail(req);try{const current=await readEvalDataset(appkit,{maxAgeMs:0});const state=await readLabState(appkit,{maxAgeMs:0});const dataset=await writeEvalDataset(appkit,parsed.data,actor);const heldOutAudit=[...auditHeldOutEdits({prior:current.rows.map(labCaseFromRow),next:dataset.rows.map(labCaseFromRow),actor,versionId:state.currentVersionId}),...state.heldOutAudit].slice(0,200);await snapshotWorkingCopy(appkit,dataset.rows,actor,{heldOutAudit});await recordAdminAction(appkit.lakebase,{actor,action:"eval-dataset-updated",subject:"eval-dataset",detail:`Updated evaluation dataset (${dataset.rows.length} row(s)).`});res.json({dataset})}catch(error){res.status(503).json({error:"eval_dataset_store_unavailable",detail:`The dataset was not saved: ${error.message}`})}});app.post("/api/admin/benchmarks/dataset/curate",async(req,res)=>{const parsed=CurateBody.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_curate",detail:"Send the questions to add."});return}const actor=userEmail(req);try{const current=await readEvalDataset(appkit,{maxAgeMs:0});const added=uniqueQuestionsToAdd(current.rows,parsed.data.questions);const dataset=await writeEvalDataset(appkit,{rows:[...current.rows,...added]},actor);await snapshotWorkingCopy(appkit,dataset.rows,actor);await recordAdminAction(appkit.lakebase,{actor,action:"eval-dataset-curated",subject:"eval-dataset",detail:`Added ${added.length} question(s) from traces.`});res.json({dataset,added:added.length})}catch(error){res.status(503).json({error:"eval_dataset_store_unavailable",detail:`Those questions were not added: ${error.message}`})}});app.post("/api/admin/benchmarks/align-guidelines",async(req,res)=>{const actor=userEmail(req);try{const dataset=await readEvalDataset(appkit,{maxAgeMs:0});const labeled=labeledRowCount(dataset.rows);if(labeled===0){res.status(400).json({error:"no_labels",message:"Label at least one row (thumbs or SQL correct?) before aligning the guidelines."});return}const settings=await readBenchmarkSettings(appkit,{maxAgeMs:0});const flywheel=await readFlywheelState(appkit,{maxAgeMs:0});const cases=await loadCasesForAlignment(appkit,flywheel.lastAgentRunIds);let alignClient;let invokeJudge;try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});alignClient={request:({method,path,payload})=>workspaceApiRequest(client,{method,path,payload})};const judgeEndpoint=settings.judgeEndpoint.trim();if(judgeEndpoint){invokeJudge=payload=>workspaceApiRequest(client,{path:servingInvocationPath(judgeEndpoint),method:"POST",payload})}}catch{}const aligned=await alignGuidelinesToHumans({base:settings.guidelinesText,rows:dataset.rows,cases,experimentId:settings.experimentId,alignClient,invokeJudge});if(req.body?.preview===true){res.json({preview:aligned.guidelinesText,labeled,agreement:aligned.agreement,method:aligned.method,note:`${aligned.note} Preview only. Nothing is saved until review.`,saved:false});return}const current=await readBenchmarkSettingsDocument(appkit,{maxAgeMs:0});const saved=await writeBenchmarkSettingsPatch(appkit,{guidelinesText:aligned.guidelinesText},current.revision,actor);await recordAdminAction(appkit.lakebase,{actor,action:"eval-guidelines-aligned",subject:"benchmark-settings",detail:aligned.note});res.json({guidelinesText:saved.settings.guidelinesText,labeled,agreement:aligned.agreement,method:aligned.method,note:aligned.note})}catch(error){res.status(503).json({error:"align_guidelines_unavailable",message:`Guidelines were not updated: ${error.message}`})}});app.post("/api/admin/benchmarks/promote",async(req,res)=>{const parsed=PromotedAgentSchema.safeParse(req.body);if(!parsed.success||!parsed.data.endpoint.trim()){res.status(400).json({error:"invalid_promote",message:"Pick a baseline or candidate endpoint to use for the next Ask."});return}if(!parsed.data.approver.trim()){res.status(400).json({error:"approver_required",message:"Name the approver before applying the candidate."});return}const actor=userEmail(req);try{const current=await readFlywheelState(appkit,{maxAgeMs:0});const settings=await readBenchmarkSettings(appkit,{maxAgeMs:0});const targetKind=parsed.data.targetKind;const promptName=current.promptRegistryName.trim();const template=promptTemplateFromPromote({side:parsed.data.side,endpoint:parsed.data.endpoint,guidelines:settings.guidelinesText});let promotedPrompt=current.promotedPrompt;if(targetKind==="prompt-registry"){try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});promotedPrompt=await promotePromptAlias({request:({method,path,payload})=>workspaceApiRequest(client,{method,path,payload})},{name:promptName,template})}catch(error){promotedPrompt={name:promptName,alias:"production",version:"",uri:promptName?`prompts:/${promptName}@production`:"",template,status:promptName?"blocked":"skipped",note:promptName?`The production alias was not moved: ${error.message} The next Ask still uses the guidance saved from this promote.`:"No Prompt Registry name is set. Next Ask still uses the saved guidance from this promote."}}}else{promotedPrompt={name:promptName,alias:"production",version:"",uri:"",template,status:"skipped",note:promoteTargetCaption(targetKind)}}const flywheel=await patchFlywheelState(appkit,{rollback:current.promoted,promoted:{...parsed.data,at:parsed.data.at||new Date().toISOString()},promotedPrompt},actor);await recordAdminAction(appkit.lakebase,{actor,action:"eval-agent-promoted",subject:"eval-flywheel",detail:`Next Ask will use ${parsed.data.endpoint}. Approver ${parsed.data.approver}. ${promotedPrompt?.note??""}`.trim()});res.json({flywheel,promotedPrompt})}catch(error){res.status(503).json({error:"promote_unavailable",message:`The winner was not saved: ${error.message}`})}});app.post("/api/admin/benchmarks/review-app",async(req,res)=>{const actor=userEmail(req);const settings=await readBenchmarkSettings(appkit,{maxAgeMs:0});const name=requestString(req.body,"name");let session;try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});session=await startLabelingSession({request:({method,path,payload})=>workspaceApiRequest(client,{method,path,payload})},{name,experimentId:settings.experimentId})}catch(error){session={name:name||"PIA SME review",sessionId:"",runId:"",url:"",status:"blocked",note:`Review App could not be started: ${error.message} SMEs can still label thumbs and SQL correct on this tab.`,at:new Date().toISOString()}}try{const flywheel=await patchFlywheelState(appkit,{labelingSession:session},actor);await recordAdminAction(appkit.lakebase,{actor,action:"eval-review-app-started",subject:"eval-flywheel",detail:session.note});res.status(session.status==="open"?200:503).json({session,flywheel})}catch(error){res.status(session.status==="open"?200:503).json({session,message:session.note||error.message})}});app.post("/api/admin/benchmarks/score-thread",async(req,res)=>{const actor=userEmail(req);const requested=requestString(req.body,"conversationId");try{const conversationId=requested||await findLatestAnsweredConversation(appkit);if(!conversationId){res.status(404).json({error:"no_thread",message:"No Ask thread to score yet. Ask a question first, then score the whole conversation."});return}const turns=await loadConversationTurns(appkit,conversationId);if(turns.length<2){res.status(400).json({error:"short_thread",message:"That thread does not have a full conversation yet."});return}const settings=await readBenchmarkSettings(appkit,{maxAgeMs:0});const lastUser=[...turns].reverse().find(turn=>!/assistant|agent/i.test(turn.role));const lastAssistant=[...turns].reverse().find(turn=>/assistant|agent/i.test(turn.role));let invokeJudge;const judgeEndpoint=settings.judgeEndpoint.trim();if(judgeEndpoint){try{const{WorkspaceClient}=await import("./vendor-databricks-sdk-experimental.mjs");const client=new WorkspaceClient({});invokeJudge=payload=>workspaceApiRequest(client,{path:servingInvocationPath(judgeEndpoint),method:"POST",payload})}catch{invokeJudge=void 0}}const score=await scoreSampledAskTurn({client:appkit,settings:{...settings,alwaysOnTraces:true},sampleRate:1,invokeJudge,turn:{conversationId,messageId:`thread-${conversationId}`.slice(0,80),question:lastUser?.content??"",response:lastAssistant?.content??"",sql:"",note:`${turns.length} turns`,turns}});await recordAdminAction(appkit.lakebase,{actor,action:"eval-thread-scored",subject:conversationId,detail:`Scored ${turns.length} turns in the Ask thread.`});res.json({conversationId,turnCount:turns.length,score,transcript:formatConversationTurns(turns).slice(0,4e3)})}catch(error){res.status(503).json({error:"score_thread_unavailable",message:`That thread was not scored: ${error.message}`})}});app.put("/api/admin/benchmarks/prompt-registry",async(req,res)=>{const name=requestString(req.body,"name");if(name&&!/^[a-zA-Z0-9_]+(\.[a-zA-Z0-9_]+){2}$/.test(name)){res.status(400).json({error:"invalid_prompt_name",message:"Use a Unity Catalog name: catalog.schema.prompt"});return}const actor=userEmail(req);try{const flywheel=await patchFlywheelState(appkit,{promptRegistryName:name},actor);res.json({flywheel})}catch(error){res.status(503).json({error:"prompt_registry_unavailable",message:`The Prompt Registry name was not saved: ${error.message}`})}});app.post("/api/admin/benchmarks/last-suite",async(req,res)=>{const parsed=LastSuiteBody.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_last_suite",message:"The last suite could not be remembered."});return}const actor=userEmail(req);try{const flywheel=await patchFlywheelState(appkit,{lastSuite:{kind:parsed.data.kind,spaceId:parsed.data.spaceId,spaceLabel:parsed.data.spaceLabel,at:parsed.data.at||new Date().toISOString()},lastAgentRunIds:parsed.data.runIds,lastAgentSides:parsed.data.sides},actor);res.json({flywheel})}catch(error){res.status(503).json({error:"last_suite_unavailable",message:`The last suite was not saved: ${error.message}`})}});app.post("/api/admin/benchmarks/cancel",async(req,res)=>{const runId=requestString(req.body,"runId");if(!runId){res.status(400).json({error:"invalid_cancel",message:"Name the run to cancel."});return}const actor=userEmail(req);const result=await requestBenchmarkCancel({store:appkit.lakebase,runId,userEmail:actor});if(!result.ok){res.status(result.status).json({error:"cancel_refused",message:result.message});return}await recordAdminAction(appkit.lakebase,{actor,action:"eval-suite-cancelled",subject:runId,detail:"Cancelled the in-progress judge suite after the current case."});res.json({runId,cancelled:true})});app.post("/api/admin/benchmarks/rollback",async(req,res)=>{const actor=userEmail(req);try{const current=await readFlywheelState(appkit,{maxAgeMs:0});if(!current.rollback?.endpoint){res.status(400).json({error:"no_rollback",message:"No earlier promote to roll back to."});return}const flywheel=await patchFlywheelState(appkit,{promoted:current.rollback,rollback:current.promoted},actor);await recordAdminAction(appkit.lakebase,{actor,action:"eval-agent-rolled-back",subject:"eval-flywheel",detail:`Rolled back the next Ask to ${current.rollback.endpoint}.`});res.json({flywheel})}catch(error){res.status(503).json({error:"rollback_unavailable",message:`The rollback was not saved: ${error.message}`})}});app.post("/api/admin/benchmarks/compare-history",async(req,res)=>{const parsed=BakeOffHistorySchema.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_compare_history",message:"That bake-off could not be saved to history."});return}const actor=userEmail(req);try{const current=await readFlywheelState(appkit,{maxAgeMs:0});const flywheel=await patchFlywheelState(appkit,{compareHistory:rememberBakeOff(current.compareHistory,parsed.data)},actor);res.json({flywheel})}catch(error){res.status(503).json({error:"compare_history_unavailable",message:`Bake-off history was not saved: ${error.message}`})}});app.post("/api/admin/benchmarks/known-failure",async(req,res)=>{const caseId=requestString(req.body,"caseId");if(!caseId){res.status(400).json({error:"invalid_known_failure",message:"Name the case to mark as a known failure."});return}const actor=userEmail(req);try{const current=await readFlywheelState(appkit,{maxAgeMs:0});const knownFailures=[...new Set([caseId,...current.knownFailures])].slice(0,200);const flywheel=await patchFlywheelState(appkit,{knownFailures},actor);res.json({flywheel})}catch(error){res.status(503).json({error:"known_failure_unavailable",message:`That case was not marked: ${error.message}`})}});app.post("/api/benchmarks/genie-accuracy",async(req,res)=>{const parsed=GenieAccuracyBody.safeParse(req.body);if(!parsed.success){res.status(400).json({error:"invalid_genie_accuracy",message:"Pick a connected Genie space before running accuracy."});return}const host=workspaceHost();const token=executionToken(req);if(!host||!token){res.status(503).json({error:"genie_accuracy_unavailable",message:"This app cannot ask the Genie space as you. Sign in to the workspace and try again. No score was invented."});return}const dataset=await readEvalDataset(appkit,{maxAgeMs:0});if(dataset.rows.every(row=>!row.question.trim()||!row.groundTruthSql.trim())){res.status(400).json({error:"no_sql_backed_questions",message:"Add questions with ground-truth SQL first. Accuracy is passed over those rows only."});return}const warehouseId=(process.env.DATABRICKS_SQL_WAREHOUSE_ID??"").trim();const labState=await readLabState(appkit,{maxAgeMs:0}).catch(()=>null);const actor=userEmail(req);let run;try{run=await runGenieAccuracy({spaceId:parsed.data.spaceId,spaceLabel:parsed.data.spaceLabel,rows:dataset.rows,suiteKind:parsed.data.suiteKind,caseIds:parsed.data.caseIds,datasetVersion:labState?.currentVersionId||"unversioned",asker:createGenieAsker({host,token}),executor:createSqlExecutor({host,token,warehouseId})})}catch(error){if(error instanceof MissingSqlGateError){res.status(400).json({error:"missing_sql_gate",message:error.message});return}throw error}try{await writeLastGenieRun(appkit,run,actor)}catch(error){console.warn("[eval-dataset] Last Genie run was not saved:",error.message)}try{const current=await readFlywheelState(appkit,{maxAgeMs:0});await patchFlywheelState(appkit,{lastSuite:{kind:"genie",spaceId:run.spaceId,spaceLabel:run.spaceLabel,at:run.finishedAt},history:rememberAccuracy(current.history,{at:run.finishedAt,spaceId:run.spaceId,spaceLabel:run.spaceLabel,passed:run.score.passed,scored:run.score.total,excluded:run.score.excluded,percent:run.score.percent,label:run.score.label,note:run.score.excluded>0?`${run.score.excluded} not scored (warehouse or timeout)`:""})},actor)}catch(error){console.warn("[eval-flywheel] Accuracy history was not saved:",error.message)}res.json({run})})})}export{setupEvalDatasetRoutes};
